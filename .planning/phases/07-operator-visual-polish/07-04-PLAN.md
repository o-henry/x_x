---
phase: 07-operator-visual-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 07-03
files_modified:
  - kaku-gui/src/overlay/task_center.rs
autonomous: true
requirements:
  - POLISH-02
  - POLISH-03
  - POLISH-04
must_haves:
  truths:
    - "Task Center now reads as the secondary operator surface that belongs to the new shell hierarchy rather than the primary visual analog for the whole app."
    - "Overlay rows, section rail, and inline actions share the same icon-first density language as the new persistent chrome."
    - "Phase 06 focus, clear unread, rerun, remain-on-exit, and metadata edit behavior still works exactly through the existing controller seam."
  artifacts:
    - path: "kaku-gui/src/overlay/task_center.rs"
      provides: "Secondary operator surface aligned to the new rail/chrome language without regressing the shipped workflows"
      contains: "task_center"
  key_links:
    - from: "kaku-gui/src/overlay/task_center.rs"
      to: "kaku-gui/src/termwindow/mod.rs"
      via: "all row actions continue to dispatch through existing TermWindow helpers"
      pattern: "TermWindowNotif::Apply"
---

<objective>
Polish Task Center only after the persistent structure is proven, so it becomes a coherent secondary operator surface instead of the whole visual strategy.

Purpose: Phase 06 already proved the overlay behavior. Phase 07 should only revisit it after the rail/chrome/hierarchy pass succeeds, and only to align it with the new native shell language.
Output: A denser, quieter Task Center with icon-first inline actions, a less card-like section rail, and preserved Phase 06 behavior.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/07-operator-visual-polish/07-RESEARCH.md
@.planning/phases/07-operator-visual-polish/07-VALIDATION.md
@.planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
@.planning/phases/06-operator-ui-surfaces/06-VERIFICATION.md
@.planning/phases/06-operator-ui-surfaces/06-operator-ui-surfaces-02-SUMMARY.md
@kaku-gui/src/overlay/task_center.rs
@/tmp/kaku_ref_015.png

<interfaces>
From `kaku-gui/src/overlay/task_center.rs`:
```rust
fn handle_list_mouse_event(&mut self, event: MouseEvent) -> bool
fn trigger_focus(&self)
fn trigger_clear_unread(&mut self)
fn trigger_rerun(&self)
```

From Phase 06 controller seam:
```rust
// Actions continue to dispatch through TermWindow helpers via TermWindowNotif::Apply
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align the Task Center section rail and row density with the new persistent shell language</name>
  <files>kaku-gui/src/overlay/task_center.rs</files>
  <behavior>
    - Test 1: the left section rail is visually subordinate to the new persistent rail and no longer carries the primary navigation burden.
    - Test 2: row formatting stays one-line dense and icon-first, matching the post-07-01/07-02 chrome language instead of the old text-heavy/operator-card feel.
    - Test 3: the overlay keeps serving as a scoped inspector/action surface, not a dashboard shell or substitute main layout.
  </behavior>
  <action>Refine `kaku-gui/src/overlay/task_center.rs` only after the structural gate passes. Tighten the section rail, header density, and row formatting so the overlay feels like a secondary operator surface belonging to the new shell hierarchy. Reuse the icon-first language from the persistent chrome and remove any remaining large-panel or helper-text cues that keep the overlay looking like the primary visual target. Do not repeat the failed overlay/card-first approach: this task should not re-introduce a floating dashboard aesthetic or widen Task Center into a persistent inspector.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <done>Task Center matches the new shell language while remaining a dense secondary operator surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve every Phase 06 operator action while converting overlay affordances to icon-first polish</name>
  <files>kaku-gui/src/overlay/task_center.rs</files>
  <behavior>
    - Test 1: focus, clear unread, rerun, remain-on-exit, and metadata edit actions all still dispatch through the existing controller seam.
    - Test 2: action affordances become more compact and icon-first without becoming ambiguous or mouse-only.
    - Test 3: the keyboard path remains first-class even after the overlay visual cleanup.
  </behavior>
  <action>Finish the overlay pass in `task_center.rs` by converting remaining action affordances and help text to the same compact icon-first language used by the persistent rail and top chrome. Preserve the current Phase 06 behavior exactly: no new state owner, no action rewrites, and no regression to keyboard or mouse parity. Keep the overlay behavior additive and Kaku-native even as its presentation becomes materially closer to the reference language.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui task_center -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <done>The overlay visually harmonizes with the new shell without regressing any shipped Phase 06 workflow.</done>
</task>

</tasks>

<verification>
Run the `task_center` slice plus a `kaku-gui` compile check and compare the refined overlay against the updated main-window chrome to ensure it reads as a secondary surface rather than the app’s primary structure.
</verification>

<success_criteria>
Wave 3 is complete when Task Center feels like a coherent secondary operator surface layered onto the new shell hierarchy, not the central visual workaround for it.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operator-visual-polish/07-operator-visual-polish-04-SUMMARY.md`
</output>
