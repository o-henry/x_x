---
phase: 07-operator-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kaku-gui/src/termwindow/mod.rs
  - kaku-gui/src/termwindow/render/paint.rs
  - kaku-gui/src/termwindow/mouseevent.rs
autonomous: true
requirements:
  - POLISH-02
  - POLISH-03
  - POLISH-04
must_haves:
  truths:
    - "The operator rail reads as a slim full-height edge-attached navigation surface instead of a floating card."
    - "Rail selection still opens the same scoped Task Center workflows Phase 06 shipped."
    - "Rail hit targets remain explicit and mouse-safe while the chrome gets visually quieter."
  artifacts:
    - path: "kaku-gui/src/termwindow/render/paint.rs"
      provides: "Persistent left-rail renderer with compact rows and subdued active treatment"
      contains: "paint_operator_nav"
    - path: "kaku-gui/src/termwindow/mouseevent.rs"
      provides: "Operator-rail hit-target routing that preserves scoped Task Center activation"
      contains: "mouse_event_operator_nav"
    - path: "kaku-gui/src/termwindow/mod.rs"
      provides: "Window-level rail width gating and operator-nav activation state"
      contains: "operator_nav_width_px"
  key_links:
    - from: "kaku-gui/src/termwindow/render/paint.rs"
      to: "kaku-gui/src/termwindow/mouseevent.rs"
      via: "UIItemType::OperatorNav hit regions remain clickable after the structural rewrite"
      pattern: "UIItemType::OperatorNav|mouse_event_operator_nav"
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "kaku-gui/src/overlay/task_center.rs"
      via: "activate_operator_nav continues to hand off into scoped Task Center views instead of inventing a new surface"
      pattern: "activate_operator_nav|show_task_center_with_scope"
---

<objective>
Rebuild the persistent left rail first so the main window stops reading like Phase 06’s card-heavy operator pass.

Purpose: Phase 07 only moves toward the reference if the always-visible rail becomes slim, edge-attached, and low-noise before any more overlay work happens.
Output: A narrower full-height rail, compact icon-first row treatment, and preserved nav-click handoff into the existing Phase 06 Task Center behavior.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/07-operator-visual-polish/07-RESEARCH.md
@.planning/phases/07-operator-visual-polish/07-VALIDATION.md
@.planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
@.planning/phases/06-operator-ui-surfaces/06-VERIFICATION.md
@.planning/phases/06-operator-ui-surfaces/06-operator-ui-surfaces-03-SUMMARY.md
@kaku-gui/src/termwindow/mod.rs
@kaku-gui/src/termwindow/render/paint.rs
@kaku-gui/src/termwindow/mouseevent.rs
@/tmp/kaku_ref_003.png
@/tmp/kaku_ref_015.png
@/tmp/kaku_ref_030.png

<interfaces>
From `kaku-gui/src/termwindow/mod.rs`:
```rust
pub(crate) fn operator_nav_enabled(&self) -> bool
pub(crate) fn operator_nav_width_px(&self) -> usize
pub(crate) fn activate_operator_nav(&mut self, item: OperatorNavItem)
```

From `kaku-gui/src/termwindow/render/paint.rs`:
```rust
fn paint_operator_nav(&mut self) -> anyhow::Result<()>
```

From `kaku-gui/src/termwindow/mouseevent.rs`:
```rust
fn mouse_event_operator_nav(
    &mut self,
    nav_item: OperatorNavItem,
    event: MouseEvent,
    context: &dyn WindowOps,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rail-specific render and hit-target regression coverage before changing the persistent navigation geometry</name>
  <files>kaku-gui/src/termwindow/render/paint.rs,kaku-gui/src/termwindow/mouseevent.rs</files>
  <behavior>
    - Test 1: operator-nav rows stay one-line compact items with explicit `UIItemType::OperatorNav` hit targets after the render refactor.
    - Test 2: clicking an operator-nav hit target still routes through the existing activation path instead of bypassing Phase 06 behavior.
    - Test 3: the tests protect against regressing into a wide labeled panel by asserting the compact rail structure, not the old `KAKU`/workspace header block.
  </behavior>
  <action>Extract or add local regression coverage in `paint.rs` and `mouseevent.rs` for the persistent operator rail before changing its structure. The assertions should protect the structural goals from `07-RESEARCH.md`: edge-attached rail, compact rows, explicit hit targets, and preserved Task Center handoff. Do not spend this task on overlay styling, and do not keep tests anchored to the failed Phase 06 card/panel treatment.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui operator_nav -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <done>There is automated guardrail coverage for the left-rail renderer and its mouse routing before the visual rewrite lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the persistent rail into the reference-first slim navigation surface while preserving scoped Task Center behavior</name>
  <files>kaku-gui/src/termwindow/mod.rs,kaku-gui/src/termwindow/render/paint.rs,kaku-gui/src/termwindow/mouseevent.rs</files>
  <behavior>
    - Test 1: the rail width and row composition move toward the reference: slim, full-height, icon-first, and visually quieter than the old panel.
    - Test 2: active state becomes subtle rather than a large filled block, but remains obvious enough for keyboard/mouse use.
    - Test 3: `activate_operator_nav` still opens the same scoped Task Center views and Phase 06 workflows keep working unchanged.
  </behavior>
  <action>Refactor the persistent operator rail in `paint.rs` and its window gating in `mod.rs` so it follows the research recommendation exactly: edge-attached, full-height, narrower than the Phase 06 strip, no branded `KAKU` header card, no uppercase workspace panel block, and no overlay-like rounded panel treatment. Use compact icon-first rows with subdued active treatment and restrained count emphasis. Update `mouseevent.rs` only as needed so the smaller rail still has reliable hit targets and preserves the existing scoped Task Center handoff. Explicitly avoid repeating the failed overlay/card-first approach: this plan should make the main window structure better even if Task Center never opens.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui operator_nav -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <done>The always-visible rail now reads as native window chrome aligned to the reference rather than a Phase 06 operator card, and its click path still opens the shipped Task Center scopes.</done>
</task>

</tasks>

<verification>
Run the dedicated operator-nav test slice plus a `kaku-gui` compile check, then capture a fresh runtime screenshot for the rail area to compare against `/tmp/kaku_ref_003.png` and `/tmp/kaku_ref_015.png` before proceeding.
</verification>

<success_criteria>
Wave 1A is complete when the left side of the main window is structurally reference-aligned: slim, edge-attached, persistent, and behavior-compatible with the Phase 06 operator flow.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operator-visual-polish/07-operator-visual-polish-01-SUMMARY.md`
</output>
