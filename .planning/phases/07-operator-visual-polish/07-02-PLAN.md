---
phase: 07-operator-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - kaku-gui/src/tabbar.rs
  - kaku-gui/src/termwindow/render/fancy_tab_bar.rs
  - kaku-gui/src/termwindow/render/tab_bar.rs
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
  - POLISH-04
must_haves:
  truths:
    - "The top chrome reads as restrained app chrome instead of stock Kaku tab capsules."
    - "Unread, status, progress, and operator state surface through compact icon-first markers instead of text-heavy suffixes."
    - "DM Mono remains the operator-facing default in the chrome surfaces rather than drifting back to a heavier legacy look."
  artifacts:
    - path: "kaku-gui/src/tabbar.rs"
      provides: "Compact operator/state marker composition for tabs and passive chrome"
      contains: "TabBarItem::OperatorMarker"
    - path: "kaku-gui/src/termwindow/render/fancy_tab_bar.rs"
      provides: "Purpose-built title/tab chrome with lower visual density and less capsule treatment"
      contains: "build_fancy_tab_bar"
    - path: "kaku-gui/src/termwindow/render/tab_bar.rs"
      provides: "Bar-height coupling that keeps the top strip thin and reference-aligned"
      contains: "tab_bar_pixel_height_impl"
  key_links:
    - from: "kaku-gui/src/tabbar.rs"
      to: "kaku-gui/src/termwindow/render/fancy_tab_bar.rs"
      via: "tabbar item composition feeds the quieter chrome renderer"
      pattern: "TabBarItem::OperatorMarker|workspace_metadata_suffix|build_fancy_tab_bar"
    - from: "kaku-gui/src/termwindow/render/tab_bar.rs"
      to: "kaku-gui/src/termwindow/render/fancy_tab_bar.rs"
      via: "tab_bar_pixel_height governs the vertical density of the chrome strip"
      pattern: "tab_bar_pixel_height|tab_bar_height"
---

<objective>
Rewrite the top chrome and tab/title composition so the window no longer reads like decorated stock Kaku tabs.

Purpose: The reference shows a thin quiet strip with compact state markers. Phase 07 cannot get there while `· ops`, verbose metadata suffixes, and padded tab capsules remain the dominant chrome language.
Output: A reduced-density fancy tab bar, tighter tab-bar height coupling, and icon-first tab/status marker composition that still preserves discoverability and Phase 06 behavior.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/07-operator-visual-polish/07-RESEARCH.md
@.planning/phases/07-operator-visual-polish/07-VALIDATION.md
@.planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
@.planning/phases/06-operator-ui-surfaces/06-VERIFICATION.md
@.planning/phases/06-operator-ui-surfaces/06-operator-ui-surfaces-03-SUMMARY.md
@kaku-gui/src/tabbar.rs
@kaku-gui/src/termwindow/render/fancy_tab_bar.rs
@kaku-gui/src/termwindow/render/tab_bar.rs
@config/src/font.rs
@assets/macos/Kaku.app/Contents/Resources/kaku.lua
@/tmp/kaku_ref_003.png
@/tmp/kaku_ref_030.png

<interfaces>
From `kaku-gui/src/tabbar.rs`:
```rust
pub enum TabBarItem {
    Tab { tab_idx: usize, active: bool },
    OperatorMarker { tab_idx: usize, active: bool },
    // ...
}

fn operator_marker_suffix(tab: &TabInformation) -> Option<String>
fn workspace_metadata_suffix(tab: &TabInformation) -> Option<String>
```

From `kaku-gui/src/termwindow/render/tab_bar.rs`:
```rust
pub fn tab_bar_pixel_height_impl(
    config: &ConfigHandle,
    fontconfig: &wezterm_font::FontConfiguration,
    render_metrics: &RenderMetrics,
) -> anyhow::Result<f32>
```

From `config/src/font.rs`:
```rust
impl Default for FontAttributes {
    fn default() -> Self {
        Self {
            family: "DM Mono".into(),
            // ...
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace text-heavy tabbar suffixes with compact icon-first operator and metadata markers</name>
  <files>kaku-gui/src/tabbar.rs</files>
  <behavior>
    - Test 1: actionable operator state is still discoverable, but the marker language is compact and icon-first instead of ` · ops` plus long text suffixes.
    - Test 2: unread, status, and progress still surface passively without taking over the tab title or turning the tabbar into a badge strip.
    - Test 3: plain-title fallbacks stay stable enough for existing behavior while the rendered chrome becomes denser and quieter.
  </behavior>
  <action>Refactor `kaku-gui/src/tabbar.rs` so operator, unread, status, and progress state are conveyed through tighter marker composition. Preserve Phase 06 discoverability and Phase 04/05 lifecycle truth, but remove the text-heavy feel flagged in `07-RESEARCH.md`. Keep the result additive and Kaku-native: no dashboard badges, no full text labels stapled to every actionable tab, and no regression to CLI-only discoverability. Make sure the chrome still reads with the DM Mono-first typographic rhythm already defined in `config/src/font.rs` and the bundled `kaku.lua` defaults.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui tabbar -- --nocapture</automated>
  </verify>
  <done>Rendered tab titles and passive markers are materially quieter, more icon-first, and still behaviorally compatible with Phase 06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten the fancy tab bar into restrained top chrome with lower height and less capsule treatment</name>
  <files>kaku-gui/src/termwindow/render/fancy_tab_bar.rs,kaku-gui/src/termwindow/render/tab_bar.rs</files>
  <behavior>
    - Test 1: the top strip becomes thinner and more reference-like by reducing tab-bar height and padded capsule styling.
    - Test 2: integrated title buttons, active-tab indication, and window-button layout still work after the chrome density rewrite.
    - Test 3: the main work surface can visually begin sooner because the chrome no longer consumes excess vertical and decorative weight.
  </behavior>
  <action>Rewrite the fancy tab bar styling in `fancy_tab_bar.rs` and the height policy in `tab_bar.rs` so the top strip reads like restrained application chrome rather than decorated terminal tabs. Reduce the current pill/capsule feel, lower the height multiplier where it makes sense, and keep active-state emphasis subtle. Preserve integrated title button behavior and Phase 06 tabbar hit regions, but explicitly avoid repeating the failed overlay/card-first mindset by treating this as persistent window structure, not a cosmetic accent pass.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui tabbar -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <done>The top chrome is thin, quieter, and more reference-aligned while preserving native tab/window interactions and the DM Mono-first visual cadence.</done>
</task>

</tasks>

<verification>
Run the existing `tabbar` slice plus a `kaku-gui` compile check, then compare a fresh screenshot of the top strip to `/tmp/kaku_ref_003.png` and `/tmp/kaku_ref_030.png` with structure and density prioritized over color tweaks.
</verification>

<success_criteria>
Wave 1B is complete when the rail and top chrome together read like a persistent operator shell hierarchy instead of Phase 06’s utility chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operator-visual-polish/07-operator-visual-polish-02-SUMMARY.md`
</output>
