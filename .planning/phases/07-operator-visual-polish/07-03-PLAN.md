---
phase: 07-operator-visual-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - kaku-gui/src/termwindow/mod.rs
  - kaku-gui/src/termwindow/render/paint.rs
  - kaku-gui/src/termwindow/render/tab_bar.rs
  - .planning/phases/07-operator-visual-polish/07-UAT.md
  - .planning/phases/07-operator-visual-polish/07-LIMITATIONS.md
autonomous: false
requirements:
  - POLISH-03
  - POLISH-04
must_haves:
  truths:
    - "The primary terminal/editor surface begins immediately to the right of the rail and stays visually dominant."
    - "Any secondary context treatment reads as a persistent split/context region rather than an overlay pretending to be structure."
    - "The executor stops and escalates if the current renderer still cannot make the window read like the reference after this structural pass."
  artifacts:
    - path: "kaku-gui/src/termwindow/render/paint.rs"
      provides: "Viewport/chrome composition that emphasizes the main work surface instead of boxed chrome panels"
      contains: "paint_impl"
    - path: "kaku-gui/src/termwindow/mod.rs"
      provides: "Window-level layout thresholds and structure-aware behavior preservation"
      contains: "effective_vertical_padding"
    - path: "kaku-gui/src/termwindow/render/tab_bar.rs"
      provides: "Top-strip coupling that keeps viewport origin aligned with the new hierarchy"
      contains: "paint_tab_bar"
  key_links:
    - from: "kaku-gui/src/termwindow/render/paint.rs"
      to: "kaku-gui/src/termwindow/render/tab_bar.rs"
      via: "viewport origin and chrome offsets produce the persistent hierarchy together"
      pattern: "paint_impl|tab_bar_pixel_height|paint_tab_bar"
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "kaku-gui/src/termwindow/render/paint.rs"
      via: "layout thresholds and edge-to-edge padding decisions govern the hierarchy pass"
      pattern: "effective_vertical_padding|operator_nav_width_px|paint_operator_nav"
---

<objective>
Rebalance the main window hierarchy around the rewritten rail and chrome, then stop for a structural go/no-go check before any more overlay polish.

Purpose: The reference gap is not just row styling; the primary work surface must dominate and any secondary context must read as persistent structure. This is also the honest stop point if the existing renderer still fights the layout goal.
Output: A window composition pass that gives the main work surface priority, a minimal honest secondary context treatment if the renderer can support it, and a blocking screenshot gate before Plan 07-04.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/07-operator-visual-polish/07-RESEARCH.md
@.planning/phases/07-operator-visual-polish/07-VALIDATION.md
@.planning/phases/06-operator-ui-surfaces/06-VERIFICATION.md
@.planning/phases/07-operator-visual-polish/07-operator-visual-polish-01-SUMMARY.md
@.planning/phases/07-operator-visual-polish/07-operator-visual-polish-02-SUMMARY.md
@kaku-gui/src/termwindow/mod.rs
@kaku-gui/src/termwindow/render/paint.rs
@kaku-gui/src/termwindow/render/tab_bar.rs
@/tmp/kaku_ref_003.png
@/tmp/kaku_ref_015.png
@/tmp/kaku_ref_030.png

<interfaces>
From `kaku-gui/src/termwindow/mod.rs`:
```rust
pub fn effective_vertical_padding(&self) -> (usize, usize)
pub(crate) fn operator_nav_width_px(&self) -> usize
```

From `kaku-gui/src/termwindow/render/tab_bar.rs`:
```rust
pub fn tab_bar_pixel_height(&self) -> anyhow::Result<f32>
pub fn paint_tab_bar(&mut self, layers: &mut TripleLayerQuadAllocator) -> anyhow::Result<()>
```

From `kaku-gui/src/termwindow/render/paint.rs`:
```rust
fn paint_operator_nav(&mut self) -> anyhow::Result<()>
pub fn paint_impl(&mut self) -> anyhow::Result<PaintScreenLineOpenGLParams>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reframe the viewport so the main work surface dominates and any secondary context treatment is honest</name>
  <files>kaku-gui/src/termwindow/mod.rs,kaku-gui/src/termwindow/render/paint.rs,kaku-gui/src/termwindow/render/tab_bar.rs</files>
  <behavior>
    - Test 1: the primary work area begins immediately after the rail instead of reading as a boxed dashboard panel.
    - Test 2: if a secondary context region is introduced, it behaves like persistent structure or split framing, not a popup impersonating structure.
    - Test 3: Phase 06 focus, rerun, remain-on-exit, metadata editing, and discoverability behavior remain untouched because the state ownership stays in `TermWindow` and `mux`.
  </behavior>
  <action>Use the rewritten rail and chrome from 07-01/07-02 to rebalance the persistent window hierarchy in `paint.rs`, `tab_bar.rs`, and `mod.rs`. Prioritize the visual facts from `07-RESEARCH.md`: the main work surface should dominate, the viewport should start immediately to the right of the rail, and any secondary context treatment must be honest inside the existing renderer. Prefer reserved split framing, disciplined spacing, or a modest persistent context strip only if it can be rendered as real structure. Do not fake the reference with another overlay-driven panel, and do not widen the scope into a new widget toolkit during this phase.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui tabbar -- --nocapture && cargo test --locked -p kaku-gui task_center -- --nocapture && cargo test --locked -p kaku-gui operator_nav -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <done>The main window hierarchy is structurally closer to the reference and still preserves all shipped Phase 06 operator behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Structural screenshot gate before any overlay-only polish</name>
  <files>.planning/phases/07-operator-visual-polish/07-UAT.md,.planning/phases/07-operator-visual-polish/07-LIMITATIONS.md</files>
  <action>Capture fresh runtime screenshots after Task 1, compare them to the reference frames, and record the decision in `07-UAT.md`. If the rail still reads as a card or the top chrome still reads like stock Kaku tabs, stop execution here and record that `07-LIMITATIONS.md` requires a dedicated native container/widget architecture follow-up instead of continuing to 07-04. If the structure is approved, record that the persistent shell pass cleared the gate and proceed to 07-04.</action>
  <what-built>Persistent left rail, top chrome, and main work-surface hierarchy rewritten inside the native renderer.</what-built>
  <how-to-verify>
    1. Launch a fresh runtime and capture screenshots that show the full main window with the rail, chrome, and primary work area visible.
    2. Compare against `/tmp/kaku_ref_003.png`, `/tmp/kaku_ref_015.png`, and `/tmp/kaku_ref_030.png`.
    3. Confirm all of the following:
       - the rail is slim, edge-attached, and no longer reads as a card or branded side panel
       - the top strip no longer reads like stock Kaku tab UI with padded capsules
       - the main work surface is the dominant visual plane
       - any secondary context treatment reads as persistent structure, not an overlay
       - Phase 06 actions still open/focus/rerun/edit metadata as before
    4. If the rail still reads as a card or the top chrome still reads as stock Kaku tabs, stop here and record that the current renderer needs a dedicated native container/widget follow-up instead of proceeding to 07-04.
  </how-to-verify>
  <verify>
    <automated>cargo check --locked -p kaku-gui</automated>
  </verify>
  <resume-signal>Type `approved` to continue to 07-04, or describe the structural mismatch so the phase can stop and escalate honestly.</resume-signal>
  <done>The structural pass is either approved for further polish or explicitly stopped with an honest renderer-escalation note in the phase docs.</done>
</task>

</tasks>

<verification>
Automated verification must be green before the screenshot gate. The human gate decides whether the current renderer is still a viable Phase 07 path or whether execution stops and documents the need for a dedicated native container architecture follow-up.
</verification>

<success_criteria>
Wave 2 is complete only if the structural pass makes the window read materially closer to the reference. If it does not, the correct outcome is to stop, document the mismatch, and not proceed to overlay-only polish.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operator-visual-polish/07-operator-visual-polish-03-SUMMARY.md`
</output>
