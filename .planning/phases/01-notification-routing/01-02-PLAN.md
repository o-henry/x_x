---
phase: 01-notification-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/codec/src/lib.rs
  - crates/wezterm-client/src/client.rs
  - crates/wezterm-mux-server-impl/src/sessionhandler.rs
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - NOTF-04
  - NOTF-05
must_haves:
  truths:
    - "A developer can send workspace-only, tab-only, or pane-scoped notification requests through typed transport calls and get the same scope back in the response."
    - "A developer can list, clear, mark, and route unread notifications through typed transport without parsing human text."
    - "Tooling can call `identify` and `capabilities` and receive stable machine-readable responses."
  artifacts:
    - path: "crates/codec/src/lib.rs"
      provides: "Phase 1 notification PDUs and scope-aware payload structs"
      exports: ["CreateNotification", "ListNotifications", "JumpUnread", "GetNotificationCapabilitiesResponse"]
    - path: "crates/wezterm-client/src/client.rs"
      provides: "Typed client RPC wrappers for notification flows"
      contains: "create_notification"
    - path: "crates/wezterm-mux-server-impl/src/sessionhandler.rs"
      provides: "Server dispatch into mux notification APIs"
      contains: "Pdu::CreateNotification"
  key_links:
    - from: "crates/codec/src/lib.rs"
      to: "crates/wezterm-client/src/client.rs"
      via: "rpc! wrappers for notification commands"
      pattern: "rpc!"
    - from: "crates/wezterm-client/src/client.rs"
      to: "crates/wezterm-mux-server-impl/src/sessionhandler.rs"
      via: "typed request/response PDU dispatch"
      pattern: "Pdu::CreateNotification"
    - from: "crates/wezterm-mux-server-impl/src/sessionhandler.rs"
      to: "mux/src/lib.rs"
      via: "Mux notification wrapper methods from plan 01"
      pattern: "create_notification|jump_next_unread_pane|jump_prev_unread_pane"
---

<objective>
Define and implement the typed transport layer for Phase 1 notification features so the CLI can call mux-owned notification state through stable machine-readable contracts.

Purpose: Implement D-03, D-04, and D-05 by adding explicit Phase 1 PDUs rather than overloading existing commands.
Output: Notification request/response structs in `codec`, client RPC wrappers, and mux-server dispatch handlers.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/01-notification-routing/01-CONTEXT.md
@.planning/phases/01-notification-routing/01-RESEARCH.md
@.planning/phases/01-notification-routing/01-01-PLAN.md
@crates/codec/src/lib.rs
@crates/wezterm-client/src/client.rs
@crates/wezterm-mux-server-impl/src/sessionhandler.rs

<interfaces>
From `crates/codec/src/lib.rs`:
```rust
macro_rules! pdu {
    ($( $name:ident:$vers:expr),* $(,)?) => { /* ... */ }
}

#[derive(Deserialize, Serialize, PartialEq, Debug)]
pub struct SetFocusedPane {
    pub pane_id: PaneId,
}
```

From `crates/wezterm-client/src/client.rs`:
```rust
rpc!(list_panes, ListPanes = (), ListPanesResponse);
rpc!(set_focused_pane_id, SetFocusedPane, UnitResponse);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed notification PDUs and stable payload shapes</name>
  <files>crates/codec/src/lib.rs</files>
  <read_first>
    - crates/codec/src/lib.rs
    - .planning/phases/01-notification-routing/01-01-PLAN.md
    - .planning/phases/01-notification-routing/01-CONTEXT.md
    - kaku/src/cli/list.rs
  </read_first>
  <behavior>
    - Test 1: request/response payloads serialize workspace-only, tab-only, and pane-scoped notifications with the exact field names expected by later CLI JSON output.
    - Test 2: the PDU enum can encode/decode all new notification request and response types.
  </behavior>
  <action>Extend `crates/codec/src/lib.rs` with serializable payload types for Phase 1. Add a `NotificationRecordState` struct that mirrors the mux record fields `notification_id`, `workspace`, `window_id`, `tab_id`, `pane_id`, `kind`, `title`, `body`, `unread`, `unread_mode`, `created_at`, and `updated_at`, with `pane_id: Option<PaneId>` so workspace-only and tab-only records round-trip. Add `CreateNotification` with fields `workspace: String`, `window_id: Option<WindowId>`, `tab_id: Option<TabId>`, `pane_id: Option<PaneId>`, `kind: String`, `title: String`, `body: Option<String>`, `unread_mode: String`. Add `CreateNotificationResponse { notification: NotificationRecordState }`, `ListNotifications` with optional filters `workspace`, `window_id`, `tab_id`, `pane_id`, and `unread_only: bool`, `ListNotificationsResponse { notifications: Vec<NotificationRecordState> }`, `ClearNotifications` with optional repeated-id/filter support encoded as `notification_ids: Vec<String>`, `workspace`, `tab_id`, and `pane_id`, `ClearNotificationsResponse { cleared_count: usize, notification_ids: Vec<String> }`, `MarkNotificationsRead` and `MarkNotificationsUnread` with `notification_ids: Vec<String>`, `MarkNotificationsResponse { updated_count: usize, notification_ids: Vec<String> }`, `JumpUnread { pane_id: PaneId, direction: String }`, `JumpUnreadResponse { pane_id: Option<PaneId> }`, `IdentifyNotificationTarget { pane_id: PaneId }`, `IdentifyNotificationTargetResponse { workspace: String, window_id: WindowId, tab_id: TabId, pane_id: PaneId }`, `GetNotificationCapabilities`, and `GetNotificationCapabilitiesResponse { notification_commands: Vec<String>, unread_modes: Vec<String>, supports_tabbar_markers: bool }`. Add matching `Pdu` entries for every request and response.</action>
  <verify>
    <automated>cargo test --locked -p codec -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/codec/src/lib.rs` contains `NotificationRecordState`.
    - `crates/codec/src/lib.rs` contains `CreateNotification`, `ListNotifications`, `ClearNotifications`, `MarkNotificationsRead`, `MarkNotificationsUnread`, `JumpUnread`, `IdentifyNotificationTarget`, and `GetNotificationCapabilities`.
    - `crates/codec/src/lib.rs` contains `CreateNotificationResponse`, `ListNotificationsResponse`, `ClearNotificationsResponse`, `MarkNotificationsResponse`, `JumpUnreadResponse`, `IdentifyNotificationTargetResponse`, and `GetNotificationCapabilitiesResponse`.
    - `crates/codec/src/lib.rs` contains `supports_tabbar_markers: bool`.
    - `crates/codec/src/lib.rs` supports `pane_id: Option<PaneId>` in both create and record payloads so workspace-only and tab-only notifications are first-class transport cases.
    - `cargo test --locked -p codec -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>The transport layer has stable, explicit Phase 1 payload types for workspace/tab/pane-scoped notifications with no need for downstream code to invent ad hoc text parsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add client wrappers and mux-server dispatch for notification RPCs</name>
  <files>crates/wezterm-client/src/client.rs, crates/wezterm-mux-server-impl/src/sessionhandler.rs</files>
  <read_first>
    - crates/wezterm-client/src/client.rs
    - crates/wezterm-mux-server-impl/src/sessionhandler.rs
    - crates/codec/src/lib.rs
    - mux/src/lib.rs
    - .planning/phases/01-notification-routing/01-01-PLAN.md
  </read_first>
  <behavior>
    - Test 1: each new client wrapper sends the expected request type and returns the matching response type.
    - Test 2: server dispatch calls the mux notification APIs from plan 01, resolves anchor panes for workspace-only and tab-only create requests, and reuses `SetFocusedPane`-style focus behavior for unread jumps.
  </behavior>
  <action>In `crates/wezterm-client/src/client.rs`, add `rpc!` wrappers named `create_notification`, `list_notifications`, `clear_notifications`, `mark_notifications_read`, `mark_notifications_unread`, `jump_unread`, `identify_notification_target`, and `get_notification_capabilities`. In `crates/wezterm-mux-server-impl/src/sessionhandler.rs`, add `match` arms for each new `Pdu`. For create/list/mutation handlers, call the exact `Mux` wrapper methods from plan 01 and return the typed response structs. For `CreateNotification`, preserve the requested workspace/tab/pane scope fields exactly as sent, and when `pane_id` is absent resolve the anchor pane from the existing mux graph using the requested tab or workspace instead of rejecting the request as pane-only. For `JumpUnread`, accept only `"next"` and `"prev"` directions, resolve the target pane via `mux.jump_next_unread_pane(Some(pane_id))` or `mux.jump_prev_unread_pane(Some(pane_id))`, call the existing focus path by setting the focused pane when a target exists, and return `JumpUnreadResponse { pane_id: Some(target) }` or `None` when no unread target exists. For `IdentifyNotificationTarget`, resolve `workspace`, `window_id`, and `tab_id` from the supplied pane id using the existing mux graph instead of a parallel identity map. For `GetNotificationCapabilities`, return exactly these command names: `notify`, `list-notifications`, `clear-notifications`, `mark-read`, `mark-unread`, `jump-next-unread`, `jump-prev-unread`, `identify`, `capabilities`, and exactly these unread modes: `clear-on-focus`, `sticky`.</action>
  <verify>
    <automated>cargo check --locked -p wezterm-client -p wezterm-mux-server-impl</automated>
  </verify>
  <acceptance_criteria>
    - `crates/wezterm-client/src/client.rs` contains `rpc!(create_notification`, `rpc!(list_notifications`, `rpc!(clear_notifications`, `rpc!(mark_notifications_read`, `rpc!(mark_notifications_unread`, `rpc!(jump_unread`, `rpc!(identify_notification_target`, and `rpc!(get_notification_capabilities`.
    - `crates/wezterm-mux-server-impl/src/sessionhandler.rs` contains `Pdu::CreateNotification`, `Pdu::ListNotifications`, `Pdu::ClearNotifications`, `Pdu::MarkNotificationsRead`, `Pdu::MarkNotificationsUnread`, `Pdu::JumpUnread`, `Pdu::IdentifyNotificationTarget`, and `Pdu::GetNotificationCapabilities`.
    - `crates/wezterm-mux-server-impl/src/sessionhandler.rs` contains both string literals `"next"` and `"prev"`.
    - `crates/wezterm-mux-server-impl/src/sessionhandler.rs` references `set_focused_pane`.
    - `crates/wezterm-mux-server-impl/src/sessionhandler.rs` resolves workspace-only and tab-only create requests through existing mux identities instead of rejecting missing `pane_id`.
    - `cargo check --locked -p wezterm-client -p wezterm-mux-server-impl` exits successfully.
  </acceptance_criteria>
  <done>Transport calls can create workspace/tab/pane-scoped notifications, query and mutate them, identify panes, and route unread state entirely through typed request/response paths.</done>
</task>

</tasks>

<verification>
Build the client and mux-server crates together and confirm every Phase 1 notification RPC has a typed request, typed response, and server dispatch.
</verification>

<success_criteria>
Every Phase 1 notification action required by the CLI has a typed codec payload, a client wrapper, and a server dispatch path into mux-owned state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-notification-routing/01-notification-routing-02-SUMMARY.md`
</output>
