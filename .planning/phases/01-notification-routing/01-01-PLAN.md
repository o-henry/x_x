---
phase: 01-notification-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mux/src/lib.rs
  - mux/src/notification_store.rs
  - mux/src/tab.rs
autonomous: true
requirements:
  - NOTF-01
  - NOTF-03
  - NOTF-04
  - NOTF-05
must_haves:
  truths:
    - "A developer can create notifications scoped to a workspace, tab, or pane and later list those same scope fields back from mux-owned state."
    - "A developer can jump to the next or previous unread pane using existing Kaku order even when the unread notification was created at workspace-only, tab-only, or pane scope."
    - "A developer sees `clear-on-focus` notifications become read on focus while `sticky` notifications stay unread until an explicit mutation."
  artifacts:
    - path: "mux/src/notification_store.rs"
      provides: "Notification record model, scope-aware unread indexes, and mutation/query helpers"
      exports: ["NotificationRecord", "NotificationUnreadMode", "NotificationStore"]
    - path: "mux/src/lib.rs"
      provides: "Mux-owned notification APIs and mutation fan-out"
      contains: "NotificationsChanged"
    - path: "mux/src/tab.rs"
      provides: "Window/tab-order traversal helpers reused by unread routing"
      contains: "iter_panes_ignoring_zoom"
  key_links:
    - from: "mux/src/lib.rs"
      to: "mux/src/notification_store.rs"
      via: "Mux notification_store field and wrapper methods"
      pattern: "notification_store"
    - from: "mux/src/lib.rs"
      to: "MuxNotification::PaneFocused"
      via: "auto-read hook for clear-on-focus notifications"
      pattern: "PaneFocused"
    - from: "mux/src/lib.rs"
      to: "mux/src/tab.rs"
      via: "existing pane iteration order reused for next/previous unread routing"
      pattern: "iter_panes_ignoring_zoom"
---

<objective>
Create the mux-owned notification foundation for Phase 1 so every later surface can rely on one authoritative unread model.

Purpose: Implement D-01, D-02, D-06, D-07, and D-08 by making notification records, unread indexes, and routing live in `mux`.
Output: A new notification store module plus `Mux` APIs for create/list/mutate/jump and focus-driven auto-read behavior.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/01-notification-routing/01-CONTEXT.md
@.planning/phases/01-notification-routing/01-RESEARCH.md
@mux/src/lib.rs
@mux/src/tab.rs

<interfaces>
From `mux/src/lib.rs`:
```rust
#[derive(Clone, Debug)]
pub enum MuxNotification {
    PaneOutput(PaneId),
    PaneAdded(PaneId),
    PaneRemoved(PaneId),
    WindowCreated(WindowId),
    WindowRemoved(WindowId),
    WindowInvalidated(WindowId),
    WindowWorkspaceChanged(WindowId),
    ActiveWorkspaceChanged(Arc<ClientId>),
    Alert { pane_id: PaneId, alert: wezterm_term::Alert },
    Empty,
    AssignClipboard { pane_id: PaneId, selection: ClipboardSelection, clipboard: Option<Arc<str>> },
    SaveToDownloads { name: Option<String>, data: Arc<Vec<u8>> },
    TabAddedToWindow { tab_id: TabId, window_id: WindowId },
    PaneFocused(PaneId),
    TabResized(TabId),
    TabTitleChanged { tab_id: TabId, title: String },
    WindowTitleChanged { window_id: WindowId, title: String },
    WorkspaceRenamed { old_workspace: String, new_workspace: String },
}
```

From `mux/src/tab.rs`:
```rust
pub fn iter_panes(&self) -> Vec<PositionedPane>;
pub fn iter_panes_ignoring_zoom(&self) -> Vec<PositionedPane>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the mux notification store contract and tests</name>
  <files>mux/src/notification_store.rs</files>
  <read_first>
    - mux/src/notification_store.rs
    - mux/src/lib.rs
    - mux/src/tab.rs
    - .planning/phases/01-notification-routing/01-CONTEXT.md
    - .planning/phases/01-notification-routing/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: creating workspace-only, tab-only, and pane-scoped notifications preserves the requested `workspace`, `window_id`, `tab_id`, and `pane_id` scope fields while indexing unread truth against a resolved anchor pane.
    - Test 2: `clear-on-focus` notifications anchored to pane `P` become read when `apply_pane_focused(P)` runs, while `sticky` notifications anchored to the same pane stay unread.
    - Test 3: next/previous unread traversal returns anchor pane ids in the same order yielded by existing mux window/tab/pane iteration even when the unread records are workspace-only or tab-only scoped.
  </behavior>
  <action>Per D-01, D-02, and D-06, add `mux/src/notification_store.rs` with a serializable `NotificationUnreadMode` enum containing exactly `ClearOnFocus` and `Sticky`, plus a serializable `NotificationRecord` struct with exactly these externally visible scope fields: `notification_id: String`, `workspace: String`, `window_id: Option<WindowId>`, `tab_id: Option<TabId>`, `pane_id: Option<PaneId>`, `kind: String`, `title: String`, `body: Option<String>`, `unread: bool`, `unread_mode: NotificationUnreadMode`, `created_at: DateTime<Utc>`, and `updated_at: DateTime<Utc>`. Inside `NotificationStore`, preserve D-02 by keeping unread truth pane-targeted via an internal anchor-pane index keyed by a resolved `PaneId`; do not turn `workspace` or `tab_id` into separate unread truth tables. Implement `create_notification` so it accepts workspace-only, tab-only, and pane-scoped records plus the resolved anchor pane id that unread routing uses internally. Implement `list_notifications`, `clear_notifications`, `mark_notifications_read`, `mark_notifications_unread`, `apply_pane_focused`, `unread_count_for_tab`, `unread_count_for_workspace`, `next_unread_pane`, and `prev_unread_pane` against that scope-plus-anchor model.</action>
  <verify>
    <automated>cargo test --locked -p mux notification_store -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/notification_store.rs` contains `pub enum NotificationUnreadMode` and both `ClearOnFocus` and `Sticky`.
    - `mux/src/notification_store.rs` contains `pub struct NotificationRecord` with the exact field names `notification_id`, `workspace`, `window_id`, `tab_id`, `pane_id`, `kind`, `title`, `body`, `unread`, `unread_mode`, `created_at`, and `updated_at`, with `pane_id` optional so workspace-only and tab-only scope can round-trip.
    - `mux/src/notification_store.rs` contains `pub struct NotificationStore`.
    - `mux/src/notification_store.rs` contains methods named `create_notification`, `list_notifications`, `clear_notifications`, `mark_notifications_read`, `mark_notifications_unread`, `apply_pane_focused`, `next_unread_pane`, and `prev_unread_pane`.
    - `mux/src/notification_store.rs` contains tests covering workspace-only, tab-only, and pane-scoped notification creation.
    - `cargo test --locked -p mux notification_store -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>Mux has a tested notification store contract that preserves workspace/tab/pane scope fields, keeps unread truth pane-targeted internally, and can create, query, mutate, auto-read, and route unread state without any CLI or GUI code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the notification store into Mux and preserve existing navigation order</name>
  <files>mux/src/lib.rs, mux/src/tab.rs</files>
  <read_first>
    - mux/src/lib.rs
    - mux/src/tab.rs
    - mux/src/notification_store.rs
    - .planning/phases/01-notification-routing/01-CONTEXT.md
    - docs/KAKU_CONTROL_PLANE_SPEC.md
  </read_first>
  <behavior>
    - Test 1: `Mux` can create, list, clear, mark read, and mark unread workspace-only, tab-only, and pane-scoped notifications through wrapper methods without the caller touching store internals.
    - Test 2: receiving `MuxNotification::PaneFocused(pane_id)` marks only `clear-on-focus` notifications for that pane as read.
    - Test 3: unread routing helpers return pane ids using current window and tab ordering rather than a new custom ordering.
  </behavior>
  <action>Add `notification_store: RwLock<NotificationStore>` to `Mux` in `mux/src/lib.rs`, add a new `MuxNotification::NotificationsChanged` variant, and expose wrapper methods named `create_notification`, `list_notifications`, `clear_notifications`, `mark_notifications_read`, `mark_notifications_unread`, `notification_unread_count_for_tab`, `notification_unread_count_for_workspace`, `jump_next_unread_pane`, and `jump_prev_unread_pane`. For D-01 and NOTF-01, `Mux::create_notification` must accept workspace-only, tab-only, and pane-scoped requests, resolve the anchor pane from the current mux graph when the scope does not include an explicit `pane_id`, and persist only the requested scope fields on the record. Per D-07 and D-08, compute next/previous unread panes by iterating existing windows and tabs in their current order and using `tab.iter_panes_ignoring_zoom()` for pane order, then return the target anchor `PaneId` for callers that will reuse normal focus APIs later. Hook `MuxNotification::PaneFocused(pane_id)` so `clear-on-focus` records anchored to that pane are marked read and `MuxNotification::NotificationsChanged` is emitted only when unread state actually changes.</action>
  <verify>
    <automated>cargo test --locked -p mux notification_store -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/lib.rs` contains `NotificationsChanged`.
    - `mux/src/lib.rs` contains a `notification_store` field on `Mux`.
    - `mux/src/lib.rs` contains methods named `create_notification`, `list_notifications`, `clear_notifications`, `mark_notifications_read`, `mark_notifications_unread`, `jump_next_unread_pane`, and `jump_prev_unread_pane`.
    - `mux/src/lib.rs` references `PaneFocused` and `apply_pane_focused`.
    - `mux/src/lib.rs` references `iter_panes_ignoring_zoom` while computing unread routing order.
    - `mux/src/lib.rs` contains anchor-pane resolution for workspace-only and tab-only notification creation rather than requiring every caller to pass a pane id.
    - `cargo test --locked -p mux notification_store -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>`Mux` owns notification state, preserves workspace/tab/pane scope end-to-end, and exposes stable internal APIs whose auto-read plus next/previous unread routing still respect the current Kaku focus model.</done>
</task>

</tasks>

<verification>
Run the mux-focused test target and confirm the new notification APIs exist before any CLI or GUI plan begins.
</verification>

<success_criteria>
`mux` contains a notification store module, `Mux` exposes create/list/mutate/jump helpers, and focus-driven unread transitions work without introducing GUI-owned notification truth.
</success_criteria>

<output>
After completion, create `.planning/phases/01-notification-routing/01-notification-routing-01-SUMMARY.md`
</output>
