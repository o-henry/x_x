---
phase: 01-notification-routing
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - kaku/src/cli/mod.rs
  - kaku/src/cli/jump_next_unread.rs
  - kaku/src/cli/jump_prev_unread.rs
  - kaku/src/cli/identify.rs
  - kaku/src/cli/capabilities.rs
  - kaku/src/cli/list.rs
  - kaku/src/cli/activate_pane.rs
  - kaku/src/cli/activate_tab.rs
  - kaku/src/cli/rename_workspace.rs
  - mux/src/tab.rs
  - docs/KAKU_CONTROL_PLANE_SPEC.md
  - PLANS.md
  - .planning/phases/01-notification-routing/01-CHANGED-FILES.md
  - .planning/phases/01-notification-routing/01-LIMITATIONS.md
autonomous: true
requirements:
  - NOTF-04
  - NOTF-05
must_haves:
  truths:
    - "A developer can run `jump-next-unread` and `jump-prev-unread` and get stable JSON that names the pane Kaku focused, or `null` when nothing is unread."
    - "Local tooling can run `identify` and `capabilities` and receive stable JSON that describes the resolved pane target and supported notification feature set."
    - "Phase 1 closes with formatted code, documented command contracts, and a written list of known limitations instead of leaving those quality gates implicit."
  artifacts:
    - path: "kaku/src/cli/jump_next_unread.rs"
      provides: "Next-unread CLI command with stable JSON output"
      contains: "\"next\""
    - path: "kaku/src/cli/capabilities.rs"
      provides: "Machine-readable capabilities command"
      contains: "supports_tabbar_markers"
    - path: ".planning/phases/01-notification-routing/01-CHANGED-FILES.md"
      provides: "Phase 1 changed-files audit for closeout"
      contains: "Changed files"
    - path: ".planning/phases/01-notification-routing/01-LIMITATIONS.md"
      provides: "Known limitations captured at phase closeout"
      contains: "Known limitations"
  key_links:
    - from: "kaku/src/cli/jump_next_unread.rs"
      to: "codec::JumpUnread"
      via: "direction=\"next\" request and JSON response"
      pattern: "\"next\""
    - from: "kaku/src/cli/identify.rs"
      to: "codec::IdentifyNotificationTarget"
      via: "typed identify request/response"
      pattern: "IdentifyNotificationTarget"
    - from: "docs/KAKU_CONTROL_PLANE_SPEC.md"
      to: "PLANS.md"
      via: "documented command names, JSON fields, and closeout limitations"
      pattern: "capabilities|notification_commands|Known limitations"
---

<objective>
Finish the machine-oriented CLI surface for unread routing and discovery, then close Phase 1 with explicit contract docs and quality-gate coverage.

Purpose: Implement D-03, D-04, and D-05 for jump/discovery commands while satisfying the repo’s phase closeout rules for formatting, targeted verification, command-contract docs, and known limitations.
Output: Dedicated jump/discovery CLI modules with targeted JSON-contract tests, updated spec/plan docs, and a phase limitations file.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/01-notification-routing/01-CONTEXT.md
@.planning/phases/01-notification-routing/01-RESEARCH.md
@.planning/phases/01-notification-routing/01-VALIDATION.md
@.planning/phases/01-notification-routing/01-03-PLAN.md
@.planning/phases/01-notification-routing/01-04-PLAN.md
@kaku/src/cli/mod.rs
@kaku/src/cli/activate_pane.rs

<interfaces>
From `kaku/src/cli/activate_pane.rs`:
```rust
client
    .set_focused_pane_id(codec::SetFocusedPane { pane_id })
    .await?;
```

From `crates/codec/src/lib.rs`:
```rust
pub struct JumpUnread {
    pub pane_id: PaneId,
    pub direction: String,
}

pub struct IdentifyNotificationTargetResponse {
    pub workspace: String,
    pub window_id: WindowId,
    pub tab_id: TabId,
    pub pane_id: PaneId,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unread jump and machine-oriented discovery commands with targeted JSON tests</name>
  <files>kaku/src/cli/mod.rs, kaku/src/cli/jump_next_unread.rs, kaku/src/cli/jump_prev_unread.rs, kaku/src/cli/identify.rs, kaku/src/cli/capabilities.rs</files>
  <read_first>
    - kaku/src/cli/mod.rs
    - kaku/src/cli/activate_pane.rs
    - kaku/src/cli/jump_next_unread.rs
    - kaku/src/cli/jump_prev_unread.rs
    - kaku/src/cli/identify.rs
    - kaku/src/cli/capabilities.rs
    - crates/wezterm-client/src/client.rs
    - crates/codec/src/lib.rs
    - .planning/phases/01-notification-routing/01-RESEARCH.md
    - .planning/phases/01-notification-routing/01-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `jump-next-unread` and `jump-prev-unread` resolve the current pane id, call the typed unread jump RPC with direction `next` or `prev`, and print `{\"pane_id\":<id|null>}` with a stable JSON shape.
    - Test 2: `identify` prints `workspace`, `window_id`, `tab_id`, and `pane_id` for the resolved pane using a stable JSON object.
    - Test 3: `capabilities` prints the stable Phase 1 command list and unread modes as JSON.
  </behavior>
  <action>Add and register `jump-next-unread`, `jump-prev-unread`, `identify`, and `capabilities`. Both jump commands must accept optional `--pane-id`; when omitted resolve the current pane using the same logic as `activate-pane`; send `codec::JumpUnread { pane_id, direction: "next".to_string() }` or `"prev".to_string()`, and print `{\"pane_id\":<id|null>}`. `identify` must accept optional `--pane-id`; resolve the pane id the same way and print exactly `{\"workspace\":\"...\",\"window_id\":N,\"tab_id\":N,\"pane_id\":N}`. `capabilities` must call the typed capabilities RPC and print exactly the response object with `notification_commands`, `unread_modes`, and `supports_tabbar_markers`. Do not reuse existing `list` or pane commands for these flows; each command gets its own module and clap entry per D-03 and D-05. Add focused Rust tests in these modules that assert the list/mutation/jump/identify/capabilities JSON contract stays stable, specifically covering `identify` and `capabilities` here to satisfy 01-RESEARCH.md and 01-VALIDATION.md.</action>
  <verify>
    <automated>cargo test --locked -p kaku notification_discovery_contracts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku/src/cli/mod.rs` contains `mod jump_next_unread;`, `mod jump_prev_unread;`, `mod identify;`, and `mod capabilities;`.
    - `kaku/src/cli/mod.rs` contains the command names `jump-next-unread`, `jump-prev-unread`, `identify`, and `capabilities`.
    - `kaku/src/cli/jump_next_unread.rs` contains the string literal `"next"`.
    - `kaku/src/cli/jump_prev_unread.rs` contains the string literal `"prev"`.
    - `kaku/src/cli/capabilities.rs` contains `notification_commands`, `unread_modes`, and `supports_tabbar_markers`.
    - `kaku/src/cli/identify.rs` and `kaku/src/cli/capabilities.rs` contain focused JSON contract tests.
    - `cargo test --locked -p kaku notification_discovery_contracts -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>The navigation and discovery commands exist with stable JSON output and targeted automated coverage for jump, identify, and capabilities behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Close Phase 1 with contract docs, formatting coverage, and known limitations</name>
  <files>kaku/src/cli/list.rs, kaku/src/cli/activate_pane.rs, kaku/src/cli/activate_tab.rs, kaku/src/cli/rename_workspace.rs, mux/src/tab.rs, docs/KAKU_CONTROL_PLANE_SPEC.md, PLANS.md, .planning/phases/01-notification-routing/01-CHANGED-FILES.md, .planning/phases/01-notification-routing/01-LIMITATIONS.md</files>
  <read_first>
    - docs/KAKU_CONTROL_PLANE_SPEC.md
    - PLANS.md
    - .planning/phases/01-notification-routing/01-03-PLAN.md
    - kaku/src/cli/list.rs
    - kaku/src/cli/activate_pane.rs
    - kaku/src/cli/activate_tab.rs
    - kaku/src/cli/rename_workspace.rs
    - kaku/src/cli/notify.rs
    - kaku/src/cli/list_notifications.rs
    - kaku/src/cli/jump_next_unread.rs
    - kaku/src/cli/capabilities.rs
  </read_first>
  <action>Update `docs/KAKU_CONTROL_PLANE_SPEC.md` and `PLANS.md` so Phase 1 documents the concrete CLI contract created across plans 03 and 05. Add the exact command names `notify`, `list-notifications`, `clear-notifications`, `mark-read`, `mark-unread`, `jump-next-unread`, `jump-prev-unread`, `identify`, and `capabilities`; document the exact JSON record fields `notification_id`, `workspace`, `window_id`, `tab_id`, `pane_id`, `kind`, `title`, `body`, `unread`, `unread_mode`, `created_at`, and `updated_at`; document the mutation response fields `cleared_count`, `updated_count`, and `notification_ids`; and document the capabilities response fields `notification_commands`, `unread_modes`, and `supports_tabbar_markers`. Create `.planning/phases/01-notification-routing/01-CHANGED-FILES.md` with a short `Changed files` section that lists every Phase 1 implementation file touched across plans 01-05 plus one-line reasons each changed. Create `.planning/phases/01-notification-routing/01-LIMITATIONS.md` with a short `Known limitations` section covering any intentionally deferred behavior such as pane-adjacent notification hints, bell-bridge follow-up work, or other verified gaps that remain after Phase 1. Finish this closeout by adding or extending targeted regression tests for baseline Kaku behavior affected by Phase 1 in `kaku/src/cli/list.rs`, `kaku/src/cli/activate_pane.rs`, `kaku/src/cli/activate_tab.rs`, `kaku/src/cli/rename_workspace.rs`, and `mux/src/tab.rs`, then run formatting plus the phase’s targeted notification test slices and those regression tests. The closeout summary must call out the exact test targets used to verify pane listing, pane focus/navigation, tab switching/order, workspace switching, and mux tab focus/order behavior still work.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p mux notification_store -- --nocapture && cargo test --locked -p mux tab::tab_splitting -- --exact --nocapture && cargo test --locked -p kaku notification_contracts -- --nocapture && cargo test --locked -p kaku notification_discovery_contracts -- --nocapture && cargo test --locked -p kaku existing_cli_regressions -- --nocapture && cargo test --locked -p kaku-gui tabbar -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `docs/KAKU_CONTROL_PLANE_SPEC.md` contains `list-notifications`, `jump-next-unread`, `identify`, and `capabilities`.
    - `docs/KAKU_CONTROL_PLANE_SPEC.md` contains `notification_id`, `unread_mode`, and `supports_tabbar_markers`.
    - `PLANS.md` contains `list-notifications`, `mark-read`, `mark-unread`, `jump-prev-unread`, and `notification_commands`.
    - `.planning/phases/01-notification-routing/01-CHANGED-FILES.md` exists and contains `Changed files`.
    - `.planning/phases/01-notification-routing/01-LIMITATIONS.md` exists and contains `Known limitations`.
    - The closeout verification explicitly includes baseline regression coverage for existing pane listing, pane focus, tab switching/order, workspace switching, and mux tab focus/order behavior affected by Phase 1.
    - `cargo fmt --all --check` and the targeted phase test commands exit successfully.
  </acceptance_criteria>
  <done>Phase 1 closes with explicit command-contract docs, a changed-files audit, a recorded limitations list, and an automated format-plus-targeted-regression gate that matches the repo’s done definition while protecting existing Kaku behavior.</done>
</task>

</tasks>

<verification>
Run the jump/discovery JSON-contract tests, then run the explicit phase closeout command that checks formatting plus the targeted mux, CLI, and GUI test slices for Phase 1.
</verification>

<success_criteria>
Phase 1 finishes with stable jump/discovery CLI contracts, documented command behavior, formatted code, and a written list of known limitations that makes the phase closeout auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-notification-routing/01-notification-routing-05-SUMMARY.md`
</output>
