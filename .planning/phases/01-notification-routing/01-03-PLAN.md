---
phase: 01-notification-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - kaku/src/cli/mod.rs
  - kaku/src/cli/notify.rs
  - kaku/src/cli/list_notifications.rs
  - kaku/src/cli/clear_notifications.rs
  - kaku/src/cli/mark_read.rs
  - kaku/src/cli/mark_unread.rs
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
must_haves:
  truths:
    - "A developer can create, list, clear, mark read, and mark unread workspace-only, tab-only, and pane-scoped notifications through `kaku cli`."
    - "A developer gets stable JSON shapes for notification creation, list, and mutation commands instead of parsing human text."
    - "The core notification command group stays small enough to implement and verify without bundling jump/discovery and docs work into one oversized plan."
  artifacts:
    - path: "kaku/src/cli/mod.rs"
      provides: "Core Phase 1 notification subcommand registration"
      contains: "list-notifications"
    - path: "kaku/src/cli/notify.rs"
      provides: "Create notification CLI surface"
      contains: "CreateNotification"
    - path: "kaku/src/cli/list_notifications.rs"
      provides: "Stable JSON and table list output for notifications"
      contains: "CliOutputFormatKind"
  key_links:
    - from: "kaku/src/cli/mod.rs"
      to: "crates/wezterm-client/src/client.rs"
      via: "subcommand dispatch into typed notification RPC wrappers"
      pattern: "run(client).await"
    - from: "kaku/src/cli/notify.rs"
      to: "codec::CreateNotification"
      via: "workspace/tab/pane scope flags into typed request fields"
      pattern: "CreateNotification"
    - from: "kaku/src/cli/list_notifications.rs"
      to: "codec::ListNotifications"
      via: "stable JSON output built from typed record payloads"
      pattern: "ListNotifications"
---

<objective>
Expose the Phase 1 notification system through dedicated `kaku cli` subcommands and document the exact machine-readable contract.

Purpose: Implement D-03, D-04, and D-05 in the user-facing CLI without overloading existing pane commands.
Output: Registered core notification subcommands plus targeted JSON-contract tests for create/list/mutation behavior.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-notification-routing/01-CONTEXT.md
@.planning/phases/01-notification-routing/01-RESEARCH.md
@.planning/phases/01-notification-routing/01-02-PLAN.md
@kaku/src/cli/mod.rs
@kaku/src/cli/list.rs
@kaku/src/cli/activate_pane.rs

<interfaces>
From `kaku/src/cli/mod.rs`:
```rust
#[derive(Debug, Parser, Clone)]
enum CliSubCommand {
    #[command(name = "list", about = "list windows, tabs and panes")]
    List(list::ListCommand),
}
```

From `kaku/src/cli/list.rs`:
```rust
#[derive(Debug, Parser, Clone, Copy)]
pub struct ListCommand {
    #[arg(long = "format", default_value = "table")]
    format: CliOutputFormatKind,
}
```

From `kaku/src/cli/activate_pane.rs`:
```rust
client
    .set_focused_pane_id(codec::SetFocusedPane { pane_id })
    .await?;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add notification create/list/mutation CLI modules and register them</name>
  <files>kaku/src/cli/mod.rs, kaku/src/cli/notify.rs, kaku/src/cli/list_notifications.rs, kaku/src/cli/clear_notifications.rs, kaku/src/cli/mark_read.rs, kaku/src/cli/mark_unread.rs</files>
  <read_first>
    - kaku/src/cli/mod.rs
    - kaku/src/cli/list.rs
    - kaku/src/cli/notify.rs
    - kaku/src/cli/list_notifications.rs
    - kaku/src/cli/clear_notifications.rs
    - kaku/src/cli/mark_read.rs
    - kaku/src/cli/mark_unread.rs
    - crates/wezterm-client/src/client.rs
    - crates/codec/src/lib.rs
  </read_first>
  <behavior>
    - Test 1: `kaku cli notify` can create workspace-only, tab-only, and pane-scoped notifications, sending the requested scope ids plus `kind`, `title`, `body`, and `unread_mode`, then printing the created notification JSON object.
    - Test 2: `kaku cli list-notifications --format json` prints an array of notification records with stable field names and `null` scope fields where the notification was not pane-scoped.
    - Test 3: `kaku cli clear-notifications`, `mark-read`, and `mark-unread` print JSON objects containing `updated_count` or `cleared_count` plus `notification_ids`.
  </behavior>
  <action>Per D-03 and D-04, add dedicated modules and register them in `kaku/src/cli/mod.rs` with these exact command names: `notify`, `list-notifications`, `clear-notifications`, `mark-read`, and `mark-unread`. `notify` must accept `--workspace <string>`, optional `--window-id <id>`, optional `--tab-id <id>`, optional `--pane-id <id>`, `--kind <string>`, `--title <string>`, optional `--body <string>`, and `--unread-mode <clear-on-focus|sticky>`; default `--unread-mode` to `clear-on-focus`; and allow requests that are workspace-only (`window/tab/pane` absent), tab-only (`tab-id` present, `pane-id` absent), or pane-scoped. Print the created record as JSON with the exact record fields from plan 02. `list-notifications` must accept `--workspace`, `--window-id`, `--tab-id`, `--pane-id`, `--unread-only`, and `--format <table|json>`; in JSON mode print the notifications array directly, and in table mode include columns `NOTIFICATION_ID`, `WORKSPACE`, `WINDOW_ID`, `TAB_ID`, `PANE_ID`, `UNREAD`, `UNREAD_MODE`, `KIND`, and `TITLE`. `clear-notifications` must accept repeated `--notification-id` plus optional `--workspace`, `--tab-id`, and `--pane-id` filters and print `{\"cleared_count\":N,\"notification_ids\":[...]}`. `mark-read` and `mark-unread` must accept repeated `--notification-id` and print `{\"updated_count\":N,\"notification_ids\":[...]}`. Add focused Rust tests in these CLI modules that assert the JSON response shapes stay stable for create/list/mutate flows per 01-RESEARCH.md and 01-VALIDATION.md, rather than verifying only with `cargo check`.</action>
  <verify>
    <automated>cargo test --locked -p kaku notification_contracts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku/src/cli/mod.rs` contains `mod notify;`, `mod list_notifications;`, `mod clear_notifications;`, `mod mark_read;`, and `mod mark_unread;`.
    - `kaku/src/cli/mod.rs` contains the command names `notify`, `list-notifications`, `clear-notifications`, `mark-read`, and `mark-unread`.
    - `kaku/src/cli/list_notifications.rs` contains the table column names `NOTIFICATION_ID`, `WORKSPACE`, `WINDOW_ID`, `TAB_ID`, `PANE_ID`, `UNREAD`, `UNREAD_MODE`, `KIND`, and `TITLE`.
    - `kaku/src/cli/notify.rs` contains the flag names `--kind`, `--title`, and `--unread-mode`.
    - `kaku/src/cli/notify.rs` and `kaku/src/cli/list_notifications.rs` contain focused tests for workspace-only, tab-only, and pane-scoped JSON contract stability.
    - `cargo test --locked -p kaku notification_contracts -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>The core Phase 1 notification create/list/mutation commands exist as dedicated `kaku cli` subcommands with stable JSON/table output and targeted contract tests.</done>
</task>

</tasks>

<verification>
Run the focused `kaku` CLI contract tests for create/list/mutation flows and confirm the core notification command group stays independently verifiable before jump/discovery and docs follow in the next plan.
</verification>

<success_criteria>
`kaku cli` exposes the core Phase 1 notification create/list/mutation commands as dedicated subcommands, and those commands are guarded by targeted JSON-contract tests rather than compile-only checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-notification-routing/01-notification-routing-03-SUMMARY.md`
</output>
