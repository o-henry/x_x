---
phase: 01-notification-routing
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - kaku-gui/src/termwindow/mod.rs
  - kaku-gui/src/tabbar.rs
autonomous: true
requirements:
  - NOTF-06
must_haves:
  truths:
    - "Unread notification state is visible on existing Kaku tab surfaces without introducing a new shell element."
    - "Tabbar markers are derived from mux-owned unread counts, not a GUI-only notification cache."
    - "Custom tab-title formatters can read unread marker data from `TabInformation`."
  artifacts:
    - path: "kaku-gui/src/termwindow/mod.rs"
      provides: "Unread notification fields on tab snapshot data"
      contains: "unread_notification_count"
    - path: "kaku-gui/src/tabbar.rs"
      provides: "Default tabbar unread marker rendering"
      contains: "! "
  key_links:
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "mux/src/lib.rs"
      via: "tab snapshot reads notification unread counts from mux"
      pattern: "notification_unread_count_for_tab"
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "MuxNotification::NotificationsChanged"
      via: "window invalidation when unread state changes"
      pattern: "NotificationsChanged"
    - from: "kaku-gui/src/tabbar.rs"
      to: "TabInformation"
      via: "default title prefix when unread_notification_count > 0"
      pattern: "unread_notification_count"
---

<objective>
Surface Phase 1 unread state through the existing tabbar in a minimal, Kaku-native way.

Purpose: Implement D-09 and D-10 by rendering unread markers on existing tab surfaces and nowhere broader.
Output: `TabInformation` unread fields plus default tabbar marker behavior backed by mux unread counts.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/01-notification-routing/01-CONTEXT.md
@.planning/phases/01-notification-routing/01-RESEARCH.md
@.planning/phases/01-notification-routing/01-01-PLAN.md
@kaku-gui/src/termwindow/mod.rs
@kaku-gui/src/tabbar.rs

<interfaces>
From `kaku-gui/src/termwindow/mod.rs`:
```rust
pub struct TabInformation {
    pub tab_id: TabId,
    pub tab_index: usize,
    pub is_active: bool,
    pub is_last_active: bool,
    pub active_pane: Option<PaneInformation>,
    pub window_id: MuxWindowId,
    pub tab_title: String,
}
```

From `kaku-gui/src/tabbar.rs`:
```rust
pub fn compute_tab_plain_title(tab: &TabInformation) -> String {
    if !tab.tab_title.is_empty() {
        return tab.tab_title.clone();
    }
    /* ... */
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mux-backed unread fields to tab snapshots and invalidate on notification changes</name>
  <files>kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - kaku-gui/src/termwindow/mod.rs
    - mux/src/lib.rs
    - .planning/phases/01-notification-routing/01-01-PLAN.md
    - .planning/phases/01-notification-routing/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: each `TabInformation` snapshot includes a boolean unread flag and numeric unread count derived from mux notification state.
    - Test 2: GUI invalidates and recomputes tabbar state when `MuxNotification::NotificationsChanged` is received.
  </behavior>
  <action>Extend `TabInformation` in `kaku-gui/src/termwindow/mod.rs` with exactly two new public fields: `has_unread_notifications: bool` and `unread_notification_count: usize`. Populate both fields from `Mux::notification_unread_count_for_tab(tab_id)` when building tab snapshots, expose both fields through `UserData`, and update the mux notification subscription path so `MuxNotification::NotificationsChanged` invalidates the relevant window/tabbar state just like other lightweight redraw triggers. Do not add a sidebar, overlay, or pane-adjacent UI in this phase per D-10.</action>
  <verify>
    <automated>cargo check --locked -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/termwindow/mod.rs` contains `has_unread_notifications: bool`.
    - `kaku-gui/src/termwindow/mod.rs` contains `unread_notification_count: usize`.
    - `kaku-gui/src/termwindow/mod.rs` exposes `has_unread_notifications` and `unread_notification_count` through `add_field_method_get`.
    - `kaku-gui/src/termwindow/mod.rs` references `notification_unread_count_for_tab`.
    - `kaku-gui/src/termwindow/mod.rs` references `NotificationsChanged`.
    - `cargo check --locked -p kaku-gui` exits successfully.
  </acceptance_criteria>
  <done>GUI tab snapshot data carries mux-backed unread information and refreshes when notification state changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render a minimal default tabbar unread marker and cover it with tests</name>
  <files>kaku-gui/src/tabbar.rs</files>
  <read_first>
    - kaku-gui/src/tabbar.rs
    - kaku-gui/src/termwindow/mod.rs
    - .planning/phases/01-notification-routing/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: default tab titles for tabs with `unread_notification_count > 0` are prefixed with `! `.
    - Test 2: tabs with `unread_notification_count == 0` keep the current title behavior unchanged.
    - Test 3: custom Lua tab-title formatters can inspect `has_unread_notifications` and `unread_notification_count`.
  </behavior>
  <action>Update `kaku-gui/src/tabbar.rs` so the default title path prefixes `! ` to the computed default tab title whenever `tab.unread_notification_count > 0`. Keep the marker additive and text-only; do not add a new panel, badge strip, or broader visual redesign. Ensure the custom formatter path still receives the new `TabInformation` fields untouched, and add focused tests in `kaku-gui/src/tabbar.rs` that assert the `! ` prefix appears only when unread notifications are present.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui tabbar -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/tabbar.rs` contains `unread_notification_count`.
    - `kaku-gui/src/tabbar.rs` contains the literal `! ` in the default title path.
    - `kaku-gui/src/tabbar.rs` contains tests that reference `unread_notification_count`.
    - `cargo test --locked -p kaku-gui tabbar -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>Unread notification state is visible on the existing tabbar with a minimal marker that preserves the current Kaku shell feel.</done>
</task>

</tasks>

<verification>
Compile the GUI crate and run the focused tabbar tests to prove unread markers only affect the intended tabbar path.
</verification>

<success_criteria>
Phase 1 unread state appears in the tabbar through mux-backed snapshot data and a minimal default marker, with no new shell surface introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-notification-routing/01-notification-routing-04-SUMMARY.md`
</output>
