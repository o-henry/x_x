---
phase: 04-task-pane-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mux/src/lib.rs
  - mux/src/localpane.rs
  - mux/src/task_panes.rs
autonomous: true
requirements:
  - LIFE-01
  - LIFE-05
must_haves:
  truths:
    - "Task-pane lifecycle truth is mux-owned pane-scoped state rather than a GUI-local cache."
    - "Remain-on-exit extends existing Kaku exit behavior instead of creating a detached task shell."
    - "Failed-pane metadata persists long enough for later CLI and Task Center consumers to inspect rerun affordances honestly."
  artifacts:
    - path: "mux/src/task_panes.rs"
      provides: "Mux-owned lifecycle registry for task panes"
      contains: "TaskPaneRecord"
    - path: "mux/src/localpane.rs"
      provides: "Lifecycle transition hooks from running to dead/held state"
      contains: "ExitBehavior"
    - path: "mux/src/lib.rs"
      provides: "Public mux accessors and notifications for task-pane lifecycle state"
      contains: "task_panes"
  key_links:
    - from: "mux/src/localpane.rs"
      to: "mux/src/task_panes.rs"
      via: "pane exit transitions persist lifecycle metadata"
      pattern: "is_dead|DeadPendingClose"
    - from: "mux/src/task_panes.rs"
      to: "mux/src/task_center.rs"
      via: "durable failed/rerun lifecycle data will later replace limited-mode heuristics"
      pattern: "rerun"
---

<objective>
Create the mux-owned lifecycle foundation for task panes.

Purpose: Satisfy the locked ownership decisions by introducing durable pane lifecycle metadata and wiring it into existing exit behavior rather than inventing a second runtime model.
Output: A focused `task_panes` store, lifecycle transition hooks from `LocalPane`, and mux accessors/notifications that later waves can consume.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/04-task-pane-lifecycle/04-CONTEXT.md
@.planning/phases/04-task-pane-lifecycle/04-RESEARCH.md
@mux/src/lib.rs
@mux/src/localpane.rs
@mux/src/pane.rs
@mux/src/task_center.rs

<interfaces>
From `mux/src/pane.rs`:
```rust
fn is_dead(&self) -> bool;
fn copy_user_vars(&self) -> HashMap<String, String>;
fn exit_behavior(&self) -> Option<ExitBehavior>;
fn get_current_working_dir(&self, policy: CachePolicy) -> Option<Url>;
```

From `mux/src/localpane.rs`:
```rust
enum ProcessState { Running { .. }, DeadPendingClose { .. }, Dead }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define a mux-owned task-pane lifecycle record model and unit tests</name>
  <files>mux/src/task_panes.rs</files>
  <read_first>
    - mux/src/pane.rs
    - mux/src/task_center.rs
    - mux/src/lib.rs
    - .planning/phases/04-task-pane-lifecycle/04-CONTEXT.md
    - .planning/phases/04-task-pane-lifecycle/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the lifecycle store can persist pane-scoped remain-on-exit intent, failure/dead state, rerun metadata, silence flags, and tee metadata with stable record fields.
    - Test 2: lifecycle updates are keyed by `PaneId` and survive long enough to support failed-pane inspection after the underlying process exits.
    - Test 3: pruning/removal rules are explicit so stale pane lifecycle state does not live forever.
  </behavior>
  <action>Add `mux/src/task_panes.rs` with a focused store and record model such as `TaskPaneRecord`, `TaskPaneStore`, and typed lifecycle mutation/query helpers. Include fields for `pane_id`, workspace/window/tab links if available, remain-on-exit intent, failed/dead flags, durable rerun metadata copied from pane user vars, watchdog silence state, and optional tee destination metadata. Keep this mux-owned and in-memory like the existing notification/workspace stores; do not add GUI-only state or detached session semantics. Add unit tests covering creation, mutation on pane failure, rerun metadata persistence, and deterministic pruning/removal behavior.</action>
  <verify>
    <automated>cargo test --locked -p mux task_panes -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/task_panes.rs` contains `TaskPaneRecord`.
    - `mux/src/task_panes.rs` contains `remain_on_exit`.
    - `mux/src/task_panes.rs` contains `rerun` metadata fields.
    - `mux/src/task_panes.rs` contains tests for lifecycle persistence and pruning.
    - `cargo test --locked -p mux task_panes -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>A tested mux-owned lifecycle store exists for task panes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate localpane exit transitions with the lifecycle store and mux notifications</name>
  <files>mux/src/localpane.rs,mux/src/lib.rs</files>
  <read_first>
    - mux/src/localpane.rs
    - mux/src/lib.rs
    - mux/src/task_panes.rs
    - .planning/phases/03-task-center-overlay/03-LIMITATIONS.md
  </read_first>
  <behavior>
    - Test 1: when a pane transitions into held or failed exit states, mux lifecycle state is updated without regressing existing hold/close behavior.
    - Test 2: existing exit trailer messaging still works after lifecycle integration.
    - Test 3: mux emits a typed lifecycle notification/event that later GUI consumers can react to without polling.
  </behavior>
  <action>Extend `mux/src/lib.rs` to own and expose the new `task_panes` store plus a typed lifecycle notification such as `TaskPaneLifecycleChanged`. Update `mux/src/localpane.rs` so the existing transition logic in `is_dead()` records remain-on-exit/dead/failure metadata into the task-pane store when a pane exits or enters a held dead-pending-close state. Preserve current `ExitBehavior::{Close, CloseOnCleanExit, Hold}` semantics and exit trailer output exactly; the only new behavior is durable lifecycle recording and mux notification fan-out.</action>
  <verify>
    <automated>cargo test --locked -p mux task_panes -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/lib.rs` contains `pub mod task_panes;`.
    - `mux/src/lib.rs` contains `TaskPaneLifecycleChanged`.
    - `mux/src/localpane.rs` references the new lifecycle recording path near existing exit transitions.
    - `mux/src/localpane.rs` still contains `ExitBehavior::Hold` and `ExitBehavior::CloseOnCleanExit`.
    - `cargo test --locked -p mux task_panes -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>LocalPane exit transitions now persist durable lifecycle metadata through mux without changing baseline Kaku exit behavior.</done>
</task>

</tasks>

<verification>
Run the mux task-pane test slice and confirm lifecycle state is mux-owned, durable across pane exit, and additive to existing exit behavior.
</verification>

<success_criteria>
Phase 4 has a tested mux-owned lifecycle foundation that preserves current Kaku hold/close semantics while making failed-pane metadata durable for later consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-task-pane-lifecycle/04-task-pane-lifecycle-01-SUMMARY.md`
</output>

