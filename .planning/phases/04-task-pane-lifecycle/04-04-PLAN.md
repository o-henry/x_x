---
phase: 04-task-pane-lifecycle
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - crates/codec/src/lib.rs
  - crates/wezterm-client/src/client.rs
  - crates/wezterm-mux-server-impl/src/sessionhandler.rs
  - kaku/src/cli/mod.rs
  - kaku/src/cli/pipe_pane.rs
  - mux/src/lib.rs
  - mux/src/task_panes.rs
autonomous: true
requirements:
  - LIFE-04
must_haves:
  truths:
    - "Pipe-pane / tee duplicates pane output to a file without altering normal pane rendering or interaction."
    - "Tee state is pane-scoped lifecycle metadata rather than a global switch."
    - "The output duplication hooks into mux output flow rather than creating a second PTY path."
  artifacts:
    - path: "kaku/src/cli/pipe_pane.rs"
      provides: "Explicit CLI contract for enabling/disabling tee output"
      contains: "pipe-pane"
    - path: "mux/src/task_panes.rs"
      provides: "Per-pane tee destination metadata"
      contains: "tee"
    - path: "mux/src/lib.rs"
      provides: "Output duplication hook on the mux output path"
      contains: "send_actions_to_mux"
  key_links:
    - from: "mux/src/lib.rs"
      to: "mux/src/task_panes.rs"
      via: "pane output fan-out consults tee metadata to duplicate output"
      pattern: "PaneOutput"
---

<objective>
Add pipe-pane / tee output behavior that preserves normal pane behavior.

Purpose: Satisfy LIFE-04 by duplicating pane output to a file through existing mux output plumbing instead of redirecting or replacing pane I/O.
Output: Typed transport + CLI for tee control, pane-scoped tee metadata, and additive output duplication to file.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/04-task-pane-lifecycle/04-CONTEXT.md
@.planning/phases/04-task-pane-lifecycle/04-RESEARCH.md
@mux/src/lib.rs
@mux/src/localpane.rs
@mux/src/task_panes.rs
@kaku/src/cli/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tee metadata, typed pipe-pane transport, and additive output duplication</name>
  <files>mux/src/task_panes.rs,mux/src/lib.rs,crates/codec/src/lib.rs,crates/wezterm-client/src/client.rs,crates/wezterm-mux-server-impl/src/sessionhandler.rs,kaku/src/cli/mod.rs,kaku/src/cli/pipe_pane.rs</files>
  <read_first>
    - mux/src/lib.rs
    - mux/src/task_panes.rs
    - mux/src/localpane.rs
    - crates/codec/src/lib.rs
    - kaku/src/cli/list.rs
    - .planning/phases/04-task-pane-lifecycle/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `pipe-pane` can enable tee output for a pane to a file path and report stable JSON result fields.
    - Test 2: enabling tee does not suppress existing pane output notifications or normal pane rendering.
    - Test 3: disabling tee stops file duplication for that pane without affecting unrelated panes.
  </behavior>
  <action>Extend the task-pane store with per-pane tee destination metadata and expose explicit transport/CLI support for `pipe-pane`. Add typed request/response payloads in `crates/codec/src/lib.rs`, client wrappers, and mux-server dispatch, then register `pipe-pane` in `kaku/src/cli/mod.rs` with a dedicated `kaku/src/cli/pipe_pane.rs` implementation that accepts a pane id and target file path. In `mux/src/lib.rs`, hook tee duplication into the existing output flow used by `send_actions_to_mux(...)`/pane output notifications so output is additionally written to the configured file while pane rendering and interaction remain unchanged. Do not redirect the pane's PTY writer or replace pane output handling.</action>
  <verify>
    <automated>cargo test --locked -p mux task_panes -- --nocapture && cargo test --locked -p kaku lifecycle_contracts -- --nocapture && cargo check --locked -p wezterm-client -p wezterm-mux-server-impl</automated>
  </verify>
  <acceptance_criteria>
    - `kaku/src/cli/mod.rs` contains `pipe-pane`.
    - `kaku/src/cli/pipe_pane.rs` exists and contains a file path argument.
    - `mux/src/task_panes.rs` contains tee destination metadata.
    - `mux/src/lib.rs` references tee duplication near the pane output path.
    - `cargo test --locked -p mux task_panes -- --nocapture` exits successfully.
    - `cargo test --locked -p kaku lifecycle_contracts -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>Pipe-pane tee output works as additive file duplication without regressing pane behavior.</done>
</task>

</tasks>

<verification>
Run the mux lifecycle slice, CLI contract slice, and transport check to confirm tee output is additive and pane-scoped.
</verification>

<success_criteria>
Pane output can be duplicated to file through an explicit pane-scoped lifecycle contract while preserving normal pane interaction and rendering flow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-task-pane-lifecycle/04-task-pane-lifecycle-04-SUMMARY.md`
</output>

