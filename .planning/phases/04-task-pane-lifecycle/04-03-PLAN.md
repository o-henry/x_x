---
phase: 04-task-pane-lifecycle
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - mux/src/task_center.rs
  - kaku-gui/src/termwindow/mod.rs
  - kaku-gui/src/overlay/task_center.rs
autonomous: true
requirements:
  - LIFE-02
  - LIFE-05
must_haves:
  truths:
    - "Task Center remains a consumer overlay and does not become the owner of lifecycle state."
    - "Failed/rerun visibility is upgraded from Phase 3 heuristics to durable mux lifecycle snapshots."
    - "Rerun affordances are shown only when durable rerun metadata still exists."
  artifacts:
    - path: "mux/src/task_center.rs"
      provides: "Task Center entries enriched from durable lifecycle state"
      contains: "rerun_available"
    - path: "kaku-gui/src/termwindow/mod.rs"
      provides: "Consumer-side focus/clear/rerun wiring against mux-backed lifecycle state"
      contains: "rerun_task_center_entry"
  key_links:
    - from: "mux/src/task_center.rs"
      to: "mux/src/task_panes.rs"
      via: "failed/running/rerun state sourced from durable lifecycle records"
      pattern: "is_failed|rerun_available"
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "mux/src/lib.rs"
      via: "Task Center cache refresh on lifecycle change notifications"
      pattern: "TaskPaneLifecycleChanged"
---

<objective>
Upgrade GUI and Task Center lifecycle consumers to use durable mux-backed task-pane state.

Purpose: Close the Phase 3 limitation where failed/rerun rows depend only on live pane heuristics and user vars.
Output: Task Center rows and rerun actions that consume durable lifecycle state and refresh on lifecycle notifications.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/04-task-pane-lifecycle/04-CONTEXT.md
@.planning/phases/04-task-pane-lifecycle/04-RESEARCH.md
@.planning/phases/03-task-center-overlay/03-LIMITATIONS.md
@mux/src/task_center.rs
@mux/src/task_panes.rs
@kaku-gui/src/termwindow/mod.rs
@kaku-gui/src/overlay/task_center.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace Task Center lifecycle heuristics with durable task-pane snapshot data</name>
  <files>mux/src/task_center.rs,kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - mux/src/task_center.rs
    - mux/src/task_panes.rs
    - mux/src/lib.rs
    - kaku-gui/src/termwindow/mod.rs
    - .planning/phases/03-task-center-overlay/03-LIMITATIONS.md
  </read_first>
  <behavior>
    - Test 1: failed/running/rerun flags in Task Center come from mux lifecycle records when available, not only from live pane dead/progress/user-var heuristics.
    - Test 2: `TaskPaneLifecycleChanged` refreshes the Task Center cache without polling.
    - Test 3: `rerun_task_center_entry` no longer fails merely because the original pane's live user vars disappeared after exit.
  </behavior>
  <action>Update `mux/src/task_center.rs` so pane/tab/workspace lifecycle flags and rerun availability are sourced from the durable task-pane store introduced in plan 01. Keep the existing normalized row model, but replace Phase 3's limited-mode rerun/failure heuristic path when durable lifecycle data exists. In `kaku-gui/src/termwindow/mod.rs`, refresh the Task Center cache on the new lifecycle notification and update `rerun_task_center_entry(...)` to consume durable lifecycle rerun metadata rather than depending exclusively on live pane user vars.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui task_center -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/task_center.rs` references `task_panes` or equivalent lifecycle snapshot access.
    - `kaku-gui/src/termwindow/mod.rs` references `TaskPaneLifecycleChanged`.
    - `kaku-gui/src/termwindow/mod.rs` no longer requires only live `KAKU_RERUN_COMMAND` user vars for rerun when durable metadata exists.
    - `cargo test --locked -p kaku-gui task_center -- --nocapture` exits successfully.
    - `cargo check --locked -p kaku-gui` exits successfully.
  </acceptance_criteria>
  <done>Task Center and its rerun action now consume durable lifecycle state instead of Phase 3's transient heuristics.</done>
</task>

</tasks>

<verification>
Run the Task Center GUI slice and confirm lifecycle rows now refresh from durable mux notifications rather than stale live-pane heuristics.
</verification>

<success_criteria>
Failed-pane metadata remains available long enough for Task Center and other GUI consumers to show honest rerun affordances after pane exit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-task-pane-lifecycle/04-task-pane-lifecycle-03-SUMMARY.md`
</output>

