---
phase: 04-task-pane-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - crates/codec/src/lib.rs
  - crates/wezterm-client/src/client.rs
  - crates/wezterm-mux-server-impl/src/sessionhandler.rs
  - kaku/src/cli/mod.rs
  - kaku/src/cli/list_task_panes.rs
  - kaku/src/cli/set_remain_on_exit.rs
  - kaku/src/cli/rerun_pane.rs
  - kaku/src/cli/respawn_pane.rs
  - kaku/src/cli/silence_watchdog.rs
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-05
must_haves:
  truths:
    - "Lifecycle control is CLI-first and machine-readable."
    - "Rerun and respawn are explicit contracts rather than implicit GUI-only actions."
    - "Failed-pane metadata can be inspected through stable CLI output instead of only through Task Center heuristics."
  artifacts:
    - path: "crates/codec/src/lib.rs"
      provides: "Typed lifecycle PDUs for task-pane queries and mutations"
      contains: "TaskPane"
    - path: "kaku/src/cli/list_task_panes.rs"
      provides: "Stable inspection path for durable lifecycle state"
      contains: "list-task-panes"
    - path: "kaku/src/cli/set_remain_on_exit.rs"
      provides: "Explicit remain-on-exit mutation contract"
      contains: "set-remain-on-exit"
  key_links:
    - from: "kaku/src/cli/rerun_pane.rs"
      to: "mux/src/task_panes.rs"
      via: "durable rerun metadata consumed through transport instead of live GUI-only user vars"
      pattern: "rerun"
    - from: "kaku/src/cli/silence_watchdog.rs"
      to: "mux/src/task_panes.rs"
      via: "pane-scoped silence state mutation"
      pattern: "watchdog"
---

<objective>
Expose Phase 4 lifecycle behavior through explicit typed transport and CLI contracts.

Purpose: Make remain-on-exit, rerun/respawn, silence, and lifecycle inspection available through stable machine-readable commands, keeping lifecycle control out of GUI-only paths.
Output: Typed codec/client/server support plus dedicated `kaku cli` lifecycle subcommands.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/04-task-pane-lifecycle/04-CONTEXT.md
@.planning/phases/04-task-pane-lifecycle/04-RESEARCH.md
@mux/src/task_panes.rs
@crates/codec/src/lib.rs
@crates/wezterm-client/src/client.rs
@crates/wezterm-mux-server-impl/src/sessionhandler.rs
@kaku/src/cli/mod.rs

<interfaces>
Follow the existing Phase 1/2/3 transport pattern:
```rust
rpc!(client_method, RequestType, ResponseType);
```

CLI outputs must remain table-or-JSON stable like existing `list` and `list-notifications` commands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed lifecycle request/response PDUs and server dispatch</name>
  <files>crates/codec/src/lib.rs,crates/wezterm-client/src/client.rs,crates/wezterm-mux-server-impl/src/sessionhandler.rs</files>
  <read_first>
    - crates/codec/src/lib.rs
    - crates/wezterm-client/src/client.rs
    - crates/wezterm-mux-server-impl/src/sessionhandler.rs
    - mux/src/task_panes.rs
    - .planning/phases/04-task-pane-lifecycle/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: typed PDUs exist for listing task panes and mutating remain-on-exit, rerun/respawn, and watchdog silence state.
    - Test 2: server dispatch routes the new PDUs into mux-owned lifecycle helpers rather than GUI code paths.
    - Test 3: codec tests cover round-trip serialization for the lifecycle payloads.
  </behavior>
  <action>Extend `crates/codec/src/lib.rs` with explicit request/response payloads for `ListTaskPanes`, `SetRemainOnExit`, `RerunPane`, `RespawnPane`, and `SilenceWatchdog`, and add matching `rpc!` wrappers in `crates/wezterm-client/src/client.rs`. Update `crates/wezterm-mux-server-impl/src/sessionhandler.rs` to dispatch those PDUs into new mux task-pane helpers from plan 01. Keep the payloads explicit and machine-readable: pane id, remain-on-exit boolean, rerun/respawn result ids, silence boolean, and stable lifecycle status fields. Do not tunnel these actions through generic string maps or GUI-only commands.</action>
  <verify>
    <automated>cargo check --locked -p wezterm-client -p wezterm-mux-server-impl && cargo test --locked -p codec -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/codec/src/lib.rs` contains `ListTaskPanes`, `SetRemainOnExit`, `RerunPane`, `RespawnPane`, and `SilenceWatchdog`.
    - `crates/wezterm-client/src/client.rs` contains typed wrappers for those lifecycle requests.
    - `crates/wezterm-mux-server-impl/src/sessionhandler.rs` references the new lifecycle request types.
    - `cargo check --locked -p wezterm-client -p wezterm-mux-server-impl` exits successfully.
    - `cargo test --locked -p codec -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>The transport layer exposes typed lifecycle requests and routes them into mux-owned task-pane state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add stable lifecycle CLI commands and contract tests</name>
  <files>kaku/src/cli/mod.rs,kaku/src/cli/list_task_panes.rs,kaku/src/cli/set_remain_on_exit.rs,kaku/src/cli/rerun_pane.rs,kaku/src/cli/respawn_pane.rs,kaku/src/cli/silence_watchdog.rs</files>
  <read_first>
    - kaku/src/cli/mod.rs
    - kaku/src/cli/list_notifications.rs
    - kaku/src/cli/list_status.rs
    - kaku/src/cli/notify.rs
    - .planning/phases/04-task-pane-lifecycle/04-CONTEXT.md
    - mux/src/task_panes.rs
  </read_first>
  <behavior>
    - Test 1: `list-task-panes` prints stable JSON/table lifecycle records including pane id, workspace, remain-on-exit, failed/dead state, rerun availability, silence state, and tee metadata summary if present.
    - Test 2: `set-remain-on-exit`, `rerun-pane`, `respawn-pane`, and `silence-watchdog` each print stable JSON payloads with exact mutation/result fields.
    - Test 3: contract tests cover JSON output keys and avoid transport wrapper leakage.
  </behavior>
  <action>Add dedicated lifecycle subcommands in `kaku/src/cli/` and register them in `kaku/src/cli/mod.rs` using the existing one-command-per-file pattern. Use exact command names `list-task-panes`, `set-remain-on-exit`, `rerun-pane`, `respawn-pane`, and `silence-watchdog`. `list-task-panes` should support table and JSON output with stable fields. Mutation commands should print only their direct result payload fields, such as `pane_id`, `remain_on_exit`, `silenced`, `spawned_pane_id`, or `status`, without extra transport wrapper nesting.</action>
  <verify>
    <automated>cargo test --locked -p kaku lifecycle_contracts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku/src/cli/mod.rs` contains `list-task-panes`, `set-remain-on-exit`, `rerun-pane`, `respawn-pane`, and `silence-watchdog`.
    - `kaku/src/cli/list_task_panes.rs` contains `CliOutputFormatKind`.
    - Mutation CLI files print stable JSON fields without transport wrapper keys.
    - `cargo test --locked -p kaku lifecycle_contracts -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>Lifecycle behavior is reachable through stable typed CLI contracts.</done>
</task>

</tasks>

<verification>
Run transport and CLI lifecycle slices to confirm typed request/response coverage and stable JSON contracts.
</verification>

<success_criteria>
Phase 4 lifecycle controls are available through explicit typed transport and stable CLI commands, not only through GUI affordances.
</success_criteria>

<output>
After completion, create `.planning/phases/04-task-pane-lifecycle/04-task-pane-lifecycle-02-SUMMARY.md`
</output>

