# Deferred Items

- `cargo fmt --all --check` currently fails because of pre-existing formatting drift in unrelated files:
  - `kaku-gui/src/main.rs`
  - `crates/kaku-runtime/src/lib.rs`
  These are outside Plan 08-03 scope and were not modified here.
