---
phase: 05-hardening-and-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mux/src/task_panes.rs
  - mux/src/lib.rs
  - assets/macos/Kaku.app/Contents/Resources/kaku.lua
autonomous: true
requirements:
  - COMP-02
  - COMP-04
must_haves:
  truths:
    - "The failing Phase 4 quick suite becomes trustworthy again before later hardening work depends on it."
    - "A failing pane spawned through Kaku's normal path no longer destabilizes the GUI with the Phase 4 recursion/EOF failure chain."
    - "Lifecycle inspection remains usable after a failing spawn, so Phase 4's blocked rerun-after-exit path can be verified later."
  artifacts:
    - path: "mux/src/task_panes.rs"
      provides: "Canonical rerun fallback normalization and lifecycle baseline tests"
      contains: "rerun_metadata_from_command_builder_falls_back_to_joined_argv"
    - path: "mux/src/lib.rs"
      provides: "Spawn/failure-path lifecycle invariants and record retention hooks"
      contains: "TaskPaneLifecycleChanged"
    - path: "assets/macos/Kaku.app/Contents/Resources/kaku.lua"
      provides: "Safe bundled config behavior on the failing spawn path"
      contains: "is_low_resolution_screen"
  key_links:
    - from: "mux/src/task_panes.rs"
      to: "kaku/src/cli/rerun_pane.rs"
      via: "canonical rerun command reconstruction used by lifecycle commands"
      pattern: "rerun"
    - from: "assets/macos/Kaku.app/Contents/Resources/kaku.lua"
      to: "mux/src/lib.rs"
      via: "spawn/failure-path hardening must preserve GUI survivability while mux lifecycle state stays inspectable"
      pattern: "is_low_resolution_screen|TaskPaneLifecycleChanged"
---

<objective>
Repair the hardening baseline before any broader compatibility claims.

Purpose: Phase 5 starts with the exact Phase 4 failure surfaces: the red `mux task_panes` quick suite and the blocked failing-pane spawn path that previously produced `kaku.lua` recursion followed by GUI socket EOF.
Output: Green lifecycle baseline tests, a reproducible failing-pane safety path through existing Kaku spawn/config behavior, and preserved lifecycle inspection after that path runs.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/04-task-pane-lifecycle/04-VALIDATION.md
@.planning/phases/04-task-pane-lifecycle/04-LIMITATIONS.md
@.planning/phases/04-task-pane-lifecycle/04-UAT.md
@.planning/phases/05-hardening-and-compatibility/05-RESEARCH.md
@mux/src/task_panes.rs
@mux/src/lib.rs
@assets/macos/Kaku.app/Contents/Resources/kaku.lua

<interfaces>
From `mux/src/lib.rs`:
```rust
pub fn list_task_panes(&self) -> Vec<TaskPaneRecord>;
pub fn task_pane_record(&self, pane_id: PaneId) -> Option<TaskPaneRecord>;
pub fn set_task_pane_remain_on_exit(&self, pane_id: PaneId, remain_on_exit: bool) -> anyhow::Result<TaskPaneRecord>;
pub fn set_task_pane_silenced(&self, pane_id: PaneId, silenced: bool) -> anyhow::Result<TaskPaneRecord>;
pub fn set_task_pane_tee_path(&self, pane_id: PaneId, tee_path: Option<String>) -> anyhow::Result<TaskPaneRecord>;
```

From `assets/macos/Kaku.app/Contents/Resources/kaku.lua`:
```lua
local function is_low_resolution_screen()
  local forced = os.getenv('KAKU_LOW_RESOLUTION_SCREEN')
  if forced == '1' or forced == 'true' then
    return true
  end
  return false
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the red lifecycle quick suite and lock the canonical rerun fallback shape</name>
  <files>mux/src/task_panes.rs,mux/src/lib.rs</files>
  <read_first>
    - mux/src/task_panes.rs
    - mux/src/lib.rs
    - .planning/phases/04-task-pane-lifecycle/04-VALIDATION.md
    - .planning/phases/05-hardening-and-compatibility/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `rerun_metadata_from_command_builder_falls_back_to_joined_argv` passes with one explicitly chosen fallback representation that later CLI contracts can rely on.
    - Test 2: immediate dead/failing pane transitions keep task-pane records inspectable long enough for `list-task-panes` and later rerun/respawn validation.
    - Test 3: pruning/removal rules remain explicit rather than silently dropping the record during baseline repair.
  </behavior>
  <action>Use the currently failing `cargo test --locked -p mux task_panes -- --nocapture` slice as the source of truth and resolve the rerun-command fallback mismatch directly in `mux/src/task_panes.rs` per COMP-04. Choose one canonical command-string shape and keep it stable across `rerun-pane` and `respawn-pane`; do not leave the test green by weakening it into a vague substring assertion. If the fallback normalization requires mux-side adjustment instead of a test expectation change, make that change in `mux/src/lib.rs` while preserving existing Phase 4 lifecycle ownership and additive behavior. Keep all work Kaku-native: no new daemon, no shell-model rewrite, no GUI-owned lifecycle cache.</action>
  <verify>
    <automated>cargo test --locked -p mux task_panes -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --locked -p mux task_panes -- --nocapture` exits successfully.
    - `mux/src/task_panes.rs` still contains a dedicated rerun fallback test with an exact expected command shape.
    - lifecycle-store tests continue to cover dead/failed record persistence instead of only happy-path live records.
  </acceptance_criteria>
  <done>The Phase 4 quick suite is green again and defines a stable rerun fallback contract for later waves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the failing spawn path so GUI/runtime survives and lifecycle inspection stays available</name>
  <files>mux/src/lib.rs,assets/macos/Kaku.app/Contents/Resources/kaku.lua</files>
  <read_first>
    - .planning/phases/04-task-pane-lifecycle/04-UAT.md
    - assets/macos/Kaku.app/Contents/Resources/kaku.lua
    - mux/src/lib.rs
    - mux/src/task_panes.rs
    - .planning/phases/05-hardening-and-compatibility/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a minimal failing-pane fixture that goes through the existing spawn path does not recreate the known recursion-triggering config behavior.
    - Test 2: the GUI process remains healthy enough that `list-task-panes` can still inspect lifecycle state after the failing spawn path.
    - Test 3: no new shell concepts, daemons, or alternate config loaders are introduced to avoid the blocker.
  </behavior>
  <action>Address the specific Phase 4 UAT blocker path per COMP-02 and COMP-04. Start from the documented failing spawn fixture shape (`/bin/sh -lc 'echo PHASE4_FAILING_TASK; false'`) and keep the investigation inside Kaku's normal spawn/config/runtime path. In `assets/macos/Kaku.app/Contents/Resources/kaku.lua`, only touch the `is_low_resolution_screen()` guard or immediately adjacent bundled-config logic if the reproducer still proves that config evaluation can recurse during spawn/reload; do not broaden this into unrelated UI tuning. In `mux/src/lib.rs`, harden the failure-path lifecycle bookkeeping so the record remains queryable even when a pane fails immediately after spawn. The goal is not to hide the path, but to make the fork survive it cleanly so later waves can verify compatibility on top of a stable baseline.</action>
  <verify>
    <automated>cargo test --locked -p mux task_panes -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - the known quick suite remains green after the spawn-path hardening change.
    - `assets/macos/Kaku.app/Contents/Resources/kaku.lua` stays limited to safe bundled-config guard logic and does not introduce new shell/UI behavior.
    - `mux/src/lib.rs` preserves `TaskPaneLifecycleChanged` notification flow while keeping failed-spawn lifecycle records inspectable.
  </acceptance_criteria>
  <done>The previously blocked failing-pane spawn path is contained well enough that later lifecycle and compatibility validation can proceed without the old recursion/EOF chain.</done>
</task>

</tasks>

<verification>
Phase 5 cannot claim hardening progress until the red mux quick suite is green and the failing spawn path has a stable baseline that keeps lifecycle inspection usable.
</verification>

<success_criteria>
Wave 1 leaves Phase 5 with a trustworthy mux quick suite and a repaired spawn/failure baseline instead of a blocked lifecycle verification path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-and-compatibility/05-hardening-and-compatibility-01-SUMMARY.md`
</output>
