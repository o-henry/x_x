---
phase: 05-hardening-and-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - mux/src/lib.rs
  - mux/src/task_panes.rs
  - crates/codec/src/lib.rs
  - crates/wezterm-client/src/client.rs
  - crates/wezterm-mux-server-impl/src/sessionhandler.rs
  - crates/wezterm-mux-server-impl/src/dispatch.rs
  - kaku/src/cli/list_task_panes.rs
  - kaku/src/cli/set_remain_on_exit.rs
  - kaku/src/cli/rerun_pane.rs
  - kaku/src/cli/respawn_pane.rs
  - kaku/src/cli/silence_watchdog.rs
  - kaku/src/cli/pipe_pane.rs
autonomous: true
requirements:
  - COMP-03
  - COMP-04
must_haves:
  truths:
    - "All new control-plane lifecycle commands have explicit and stable machine-readable contracts."
    - "Lifecycle notifications and record cleanup rules are consistent enough that downstream consumers refresh without guessing."
    - "Rerun and respawn semantics are distinct and documented by behavior rather than ambiguous naming."
  artifacts:
    - path: "mux/src/task_panes.rs"
      provides: "Documented retention and pruning rules for live/dead task-pane records"
      contains: "prune"
    - path: "crates/codec/src/lib.rs"
      provides: "Typed lifecycle request/response schema that preserves stable field names"
      contains: "TaskPaneState"
    - path: "kaku/src/cli/rerun_pane.rs"
      provides: "Stable rerun command request/response contract tests"
      contains: "lifecycle_contracts"
  key_links:
    - from: "mux/src/lib.rs"
      to: "crates/wezterm-mux-server-impl/src/sessionhandler.rs"
      via: "lifecycle mutations and list queries flow through typed transport without losing semantics"
      pattern: "list_task_panes|rerun|respawn|silence|tee"
    - from: "crates/codec/src/lib.rs"
      to: "kaku/src/cli/list_task_panes.rs"
      via: "CLI renders only the documented payload fields from the typed schema"
      pattern: "TaskPaneState|render_json"
---

<objective>
Harden lifecycle contracts and event consistency now that the baseline is stable.

Purpose: Phase 5 must make the control-plane CLI trustworthy for scripts and future maintenance, not just compilable.
Output: Stable lifecycle transport/CLI contracts, explicit retention rules, and consistent event behavior for dead/live task-pane transitions.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/04-task-pane-lifecycle/04-LIMITATIONS.md
@.planning/phases/05-hardening-and-compatibility/05-RESEARCH.md
@mux/src/lib.rs
@mux/src/task_panes.rs
@crates/codec/src/lib.rs
@crates/wezterm-client/src/client.rs
@crates/wezterm-mux-server-impl/src/sessionhandler.rs
@crates/wezterm-mux-server-impl/src/dispatch.rs
@kaku/src/cli/list_task_panes.rs
@kaku/src/cli/set_remain_on_exit.rs
@kaku/src/cli/rerun_pane.rs
@kaku/src/cli/respawn_pane.rs
@kaku/src/cli/silence_watchdog.rs
@kaku/src/cli/pipe_pane.rs

<interfaces>
From `mux/src/lib.rs`:
```rust
pub fn list_task_panes(&self) -> Vec<TaskPaneRecord>;
pub fn task_pane_record(&self, pane_id: PaneId) -> Option<TaskPaneRecord>;
```

From `crates/wezterm-client/src/client.rs`:
```rust
rpc!(list_task_panes, ListTaskPanes, ListTaskPanesResponse);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define lifecycle event, retention, and transport invariants across mux and RPC layers</name>
  <files>mux/src/lib.rs,mux/src/task_panes.rs,crates/codec/src/lib.rs,crates/wezterm-client/src/client.rs,crates/wezterm-mux-server-impl/src/sessionhandler.rs,crates/wezterm-mux-server-impl/src/dispatch.rs</files>
  <read_first>
    - mux/src/lib.rs
    - mux/src/task_panes.rs
    - crates/codec/src/lib.rs
    - crates/wezterm-client/src/client.rs
    - crates/wezterm-mux-server-impl/src/sessionhandler.rs
    - crates/wezterm-mux-server-impl/src/dispatch.rs
    - .planning/phases/04-task-pane-lifecycle/04-LIMITATIONS.md
  </read_first>
  <behavior>
    - Test 1: live-to-dead and dead-to-rerun/respawn transitions emit consistent lifecycle refresh signals for local consumers.
    - Test 2: stale task-pane records are pruned by explicit mux rules instead of silently leaking forever or disappearing too early.
    - Test 3: typed transport responses preserve the same lifecycle fields and semantics that the mux layer owns.
  </behavior>
  <action>Harden the mux and transport contract center of gravity per COMP-03 and COMP-04. In `mux/src/task_panes.rs` and `mux/src/lib.rs`, codify retention and pruning rules for dead/failed/live records, and make lifecycle notifications deterministic around refresh, rerun, respawn, tee, and silence mutations. Then align `crates/codec/src/lib.rs`, `crates/wezterm-client/src/client.rs`, `crates/wezterm-mux-server-impl/src/sessionhandler.rs`, and `crates/wezterm-mux-server-impl/src/dispatch.rs` so the typed RPC layer mirrors those semantics without adding wrapper ambiguity or widening into multi-client architecture work. Keep the scope local and additive; do not introduce a new daemon or detached control plane.</action>
  <verify>
    <automated>cargo test --locked -p mux task_panes -- --nocapture && cargo check --locked -p wezterm-client -p wezterm-mux-server-impl</automated>
  </verify>
  <acceptance_criteria>
    - lifecycle record cleanup/retention rules are explicit in mux code and covered by tests.
    - transport code compiles cleanly against the hardened mux lifecycle schema.
    - lifecycle notifications remain `TaskPaneLifecycleChanged`-driven rather than becoming a polling workaround.
  </acceptance_criteria>
  <done>Mux, codec, client, and server layers agree on lifecycle retention and event semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize every Phase 4 lifecycle CLI contract around the hardened semantics</name>
  <files>kaku/src/cli/list_task_panes.rs,kaku/src/cli/set_remain_on_exit.rs,kaku/src/cli/rerun_pane.rs,kaku/src/cli/respawn_pane.rs,kaku/src/cli/silence_watchdog.rs,kaku/src/cli/pipe_pane.rs</files>
  <read_first>
    - kaku/src/cli/list_task_panes.rs
    - kaku/src/cli/set_remain_on_exit.rs
    - kaku/src/cli/rerun_pane.rs
    - kaku/src/cli/respawn_pane.rs
    - kaku/src/cli/silence_watchdog.rs
    - kaku/src/cli/pipe_pane.rs
    - .planning/phases/05-hardening-and-compatibility/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: lifecycle command request-shape tests still prove the CLI argument contracts exactly.
    - Test 2: JSON-shape tests prove stable payload fields for list, remain-on-exit, rerun, respawn, silence, and tee commands.
    - Test 3: rerun/respawn status wording is explicit enough that scripts can distinguish semantics without inference.
  </behavior>
  <action>Update the Phase 4 lifecycle command modules so their request-shape and JSON-shape tests reflect the hardened semantics from Task 1. Preserve existing command names unless a targeted ambiguity absolutely requires a wording fix, and if that happens, keep the machine-readable field names stable while documenting the behavior difference in test names and comments. Treat these six command files as the source of truth for Phase 4/5 lifecycle CLI stability: no hidden wrapper keys, no transport-only field leaks, and no speculative fields beyond the documented contract.</action>
  <verify>
    <automated>cargo test --locked -p kaku lifecycle_contracts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --locked -p kaku lifecycle_contracts -- --nocapture` exits successfully.
    - each lifecycle command file retains explicit request-shape and JSON-shape coverage.
    - rerun vs respawn behavior is testable from command output without relying on prose alone.
  </acceptance_criteria>
  <done>The full lifecycle CLI surface is stable, machine-readable, and backed by focused contract tests.</done>
</task>

</tasks>

<verification>
Run the mux quick suite, then the lifecycle CLI contract slice, then transport compile checks so lifecycle invariants are proven across all non-GUI control-plane surfaces.
</verification>

<success_criteria>
Wave 2 leaves the control plane with explicit retention/event rules and stable lifecycle CLI contracts that downstream consumers can rely on without guesswork.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-and-compatibility/05-hardening-and-compatibility-02-SUMMARY.md`
</output>
