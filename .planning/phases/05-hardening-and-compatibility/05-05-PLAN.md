---
phase: 05-hardening-and-compatibility
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - kaku/src/cli/respawn_pane.rs
  - crates/codec/src/lib.rs
  - crates/wezterm-mux-server-impl/src/sessionhandler.rs
  - mux/src/lib.rs
  - .planning/phases/05-hardening-and-compatibility/05-UAT.md
  - .planning/phases/05-hardening-and-compatibility/05-LIMITATIONS.md
  - docs/KAKU_CONTROL_PLANE_SPEC.md
  - PLANS.md
autonomous: true
requirements:
  - COMP-03
  - COMP-04
gap_closure: true
must_haves:
  truths:
    - "A fresh live `respawn-pane` run returns the documented machine-readable `status: \"respawn\"` contract instead of the previously observed `respawned` mismatch."
    - "If a divergent runtime/server emitter still exists, it is normalized at the remaining source of truth rather than papered over in the CLI."
    - "Phase 5 closeout docs describe the current verified respawn behavior honestly, without carrying the stale limitation once the live run passes."
  artifacts:
    - path: ".planning/phases/05-hardening-and-compatibility/05-UAT.md"
      provides: "Fresh live respawn-path evidence from rebuilt binaries"
      contains: "respawn-pane"
    - path: ".planning/phases/05-hardening-and-compatibility/05-LIMITATIONS.md"
      provides: "Updated residual hardening limits after the respawn recheck"
      contains: "Known limitations"
    - path: "docs/KAKU_CONTROL_PLANE_SPEC.md"
      provides: "Canonical shipped contract and boundary statement for Phase 5"
      contains: "Phase 5"
  key_links:
    - from: "mux/src/lib.rs"
      to: "crates/wezterm-mux-server-impl/src/sessionhandler.rs"
      via: "respawn status returned by mux and forwarded by the server path"
      pattern: "respawn"
    - from: "kaku/src/cli/respawn_pane.rs"
      to: ".planning/phases/05-hardening-and-compatibility/05-UAT.md"
      via: "live CLI output must match the enforced machine-readable contract"
      pattern: "status"
---

<objective>
Close the remaining Phase 5 end-to-end respawn contract gap without reopening unrelated hardening work.

Purpose: Verification already narrowed the failure to one contradiction: the shipped CLI contract requires `status: "respawn"` while the checked-in live UAT recorded `respawned`. This plan exists only to recheck that live path, normalize the remaining emitter if it still diverges, and then update the closeout docs to match the verified result.
Output: Either a fresh passing live respawn UAT with updated closeout docs, or the smallest code change needed to make the runtime/server path emit `respawn` followed by that same verified closeout update.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hardening-and-compatibility/05-VERIFICATION.md
@.planning/phases/05-hardening-and-compatibility/05-UAT.md
@.planning/phases/05-hardening-and-compatibility/05-LIMITATIONS.md
@.planning/phases/05-hardening-and-compatibility/05-04-PLAN.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@kaku/src/cli/respawn_pane.rs
@crates/codec/src/lib.rs
@crates/wezterm-mux-server-impl/src/sessionhandler.rs
@mux/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild the live respawn path, prove the runtime status, and normalize the remaining emitter only if the mismatch still reproduces</name>
  <files>kaku/src/cli/respawn_pane.rs,crates/codec/src/lib.rs,crates/wezterm-mux-server-impl/src/sessionhandler.rs,mux/src/lib.rs,.planning/phases/05-hardening-and-compatibility/05-UAT.md</files>
  <read_first>
    - .planning/phases/05-hardening-and-compatibility/05-VERIFICATION.md
    - .planning/phases/05-hardening-and-compatibility/05-UAT.md
    - kaku/src/cli/respawn_pane.rs
    - crates/codec/src/lib.rs
    - crates/wezterm-mux-server-impl/src/sessionhandler.rs
    - mux/src/lib.rs
  </read_first>
  <behavior>
    - Test 1: a fresh rebuild plus live `./target/debug/kaku cli respawn-pane --pane-id <id>` run returns JSON with `status: "respawn"` and no unexpected-status failure.
    - Test 2: if the fresh run still emits `respawned`, the executor traces the last divergent emitter in the mux/server/runtime path and changes that source to `respawn` rather than weakening the CLI contract.
    - Test 3: `.planning/phases/05-hardening-and-compatibility/05-UAT.md` is rewritten from the new live run, including exact commands, rebuilt runtime details, and whether code changes were needed.
  </behavior>
  <action>Build fresh binaries before trusting any prior runtime evidence. Recreate the smallest live fixture needed to exercise the verified gap: launch the real `kaku-gui` runtime, create or reuse a retained task pane with rerun metadata, and run `respawn-pane` against that live session. If the live response now already returns `status: "respawn"`, do not widen scope into code cleanup; just capture the exact passing evidence in `05-UAT.md`. If the live response still fails with `respawned`, inspect the mux->server->codec path and normalize the remaining source that still emits `respawned` so the runtime/server contract matches the enforced CLI contract from Phase 5. Keep the fix additive and limited to the respawn status path per the verification gap; do not touch unrelated lifecycle behavior, Task Center behavior, or other Phase 5 surfaces.</action>
  <verify>
    <automated>cargo test --locked -p kaku lifecycle_contracts -- --nocapture && cargo test --locked -p mux task_panes -- --nocapture && cargo check --locked -p wezterm-client -p wezterm-mux-server-impl -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - the live rebuilt `respawn-pane` path is re-run and recorded in `.planning/phases/05-hardening-and-compatibility/05-UAT.md`
    - the live output proves `status: "respawn"` end-to-end instead of the previously recorded `respawned`
    - if a code change was required, it is limited to the remaining respawn status emitter and preserves the existing CLI contract
    - the targeted lifecycle contract and mux hardening checks still pass after any normalization
  </acceptance_criteria>
  <done>The checked-in repository no longer contains contradictory source-vs-live evidence for the `respawn-pane` machine-readable contract.</done>
</task>

<task type="auto">
  <name>Task 2: Update the closeout docs and limitations to match the verified respawn result</name>
  <files>.planning/phases/05-hardening-and-compatibility/05-LIMITATIONS.md,docs/KAKU_CONTROL_PLANE_SPEC.md,PLANS.md,.planning/phases/05-hardening-and-compatibility/05-UAT.md</files>
  <read_first>
    - .planning/phases/05-hardening-and-compatibility/05-UAT.md
    - .planning/phases/05-hardening-and-compatibility/05-LIMITATIONS.md
    - docs/KAKU_CONTROL_PLANE_SPEC.md
    - PLANS.md
  </read_first>
  <action>After Task 1 resolves the live respawn contradiction, update only the closeout artifacts that currently preserve that limitation. Remove or revise the `respawn-pane` limitation in `.planning/phases/05-hardening-and-compatibility/05-LIMITATIONS.md`, `docs/KAKU_CONTROL_PLANE_SPEC.md`, and `PLANS.md` so they reflect the new verified state from `05-UAT.md`. If the live rerun proves the mismatch was already gone, remove the stale limitation language entirely. If Task 1 needed a narrow runtime/server normalization, describe the shipped outcome succinctly and keep any remaining limitations focused on still-open boundaries such as raw tee output or broader manual coverage; do not reopen unrelated Phase 5 narratives.</action>
  <verify>
    <automated>rg -n "respawned|unexpected respawn-pane status|not yet fully proven end-to-end" .planning/phases/05-hardening-and-compatibility/05-UAT.md .planning/phases/05-hardening-and-compatibility/05-LIMITATIONS.md docs/KAKU_CONTROL_PLANE_SPEC.md PLANS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-hardening-and-compatibility/05-LIMITATIONS.md` no longer claims the stale `respawned` mismatch once Task 1 proves the live path
    - `docs/KAKU_CONTROL_PLANE_SPEC.md` and `PLANS.md` describe the Phase 5 outcome consistently with the fresh UAT evidence
    - the closeout docs change only the respawn-contract story and any directly dependent limitation wording, without expanding back into unrelated Phase 5 work
  </acceptance_criteria>
  <done>Phase 5 closeout materials are aligned with the current verified respawn behavior and no longer preserve a contradiction that has already been resolved.</done>
</task>

</tasks>

<verification>
Rebuild and rerun the live `respawn-pane` path first, then make the closeout docs match that fresh evidence exactly. The phase gap is closed only when the runtime, CLI contract, and Phase 5 closeout docs all agree on `status: "respawn"`.
</verification>

<success_criteria>
The remaining Phase 5 gap is closed with one honest follow-up: fresh live respawn evidence matches the shipped CLI contract, any necessary status normalization is limited to the remaining runtime/server emitter, and the limitations/spec/plan docs stop carrying the stale mismatch.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-and-compatibility/05-hardening-and-compatibility-05-SUMMARY.md`
</output>
