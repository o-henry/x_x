---
phase: 05-hardening-and-compatibility
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - kaku/tests/compatibility_cli.rs
  - kaku-gui/src/termwindow/mod.rs
  - kaku-gui/src/overlay/task_center.rs
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-05
must_haves:
  truths:
    - "Existing pane-management CLI commands still survive and remain usable after the control-plane hardening work."
    - "Pane spawn, split, focus, tab navigation, and workspace switching still behave like Kaku before and after lifecycle operations."
    - "Task Center remains a consumer overlay on top of mux state rather than becoming a new shell/controller."
  artifacts:
    - path: "kaku/tests/compatibility_cli.rs"
      provides: "Targeted regression coverage for legacy pane-management CLI surfaces"
      contains: "split-pane"
    - path: "kaku-gui/src/termwindow/mod.rs"
      provides: "GUI refresh/focus compatibility after lifecycle mutations"
      contains: "TaskPaneLifecycleChanged"
    - path: "kaku-gui/src/overlay/task_center.rs"
      provides: "Consumer-only Task Center behavior preserved during hardening"
      contains: "task center"
  key_links:
    - from: "kaku/tests/compatibility_cli.rs"
      to: "kaku/src/cli/mod.rs"
      via: "baseline pane-management commands remain callable alongside lifecycle additions"
      pattern: "list|spawn|split-pane|send-text|get-text|set-tab-title"
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "kaku-gui/src/overlay/task_center.rs"
      via: "GUI refresh and overlay behavior stay additive after lifecycle changes"
      pattern: "TaskPaneLifecycleChanged|task_center"
---

<objective>
Prove the control plane did not break baseline Kaku behavior.

Purpose: Hardening is incomplete unless the fork still behaves like Kaku for normal pane/tab/workspace workflows while Task Center remains an additive consumer surface.
Output: Targeted compatibility regression coverage for baseline CLI commands plus GUI-side guardrails around lifecycle refresh and Task Center consumer behavior.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/04-task-pane-lifecycle/04-UAT.md
@.planning/phases/05-hardening-and-compatibility/05-RESEARCH.md
@kaku/src/cli/mod.rs
@kaku/src/cli/list.rs
@kaku/src/cli/spawn_command.rs
@kaku/src/cli/split_pane.rs
@kaku/src/cli/send_text.rs
@kaku/src/cli/get_text.rs
@kaku/src/cli/set_tab_title.rs
@kaku-gui/src/termwindow/mod.rs
@kaku-gui/src/overlay/task_center.rs

<interfaces>
From `kaku/src/cli/mod.rs`:
```rust
SubCommand::Cli(CliCommand)
```

From `kaku-gui/src/termwindow/mod.rs`:
```rust
MuxNotification::TaskPaneLifecycleChanged(_)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a focused compatibility regression slice for legacy pane-management CLI commands</name>
  <files>kaku/tests/compatibility_cli.rs</files>
  <read_first>
    - kaku/src/cli/mod.rs
    - kaku/src/cli/list.rs
    - kaku/src/cli/spawn_command.rs
    - kaku/src/cli/split_pane.rs
    - kaku/src/cli/send_text.rs
    - kaku/src/cli/get_text.rs
    - kaku/src/cli/set_tab_title.rs
    - .planning/phases/05-hardening-and-compatibility/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: baseline pane-management commands (`list`, `spawn`, `split-pane`, `send-text`, `get-text`, `set-tab-title`) still expose the expected clap/help/request shape after lifecycle additions.
    - Test 2: at least one narrow behavior-level slice proves the legacy command path still dispatches correctly against an existing mux/session-style harness instead of stopping at parser survival.
    - Test 3: `list-task-panes` continues to coexist with those baseline commands without altering their JSON/table contract expectations.
    - Test 4: compatibility coverage stays narrowly about preservation, not about adding new features to legacy commands.
  </behavior>
  <action>Create `kaku/tests/compatibility_cli.rs` as the dedicated COMP-01 regression slice. Keep it fast and targeted, but do not stop at clap/help parsing: cover command survival, stable argument/help shape, and one behavior-level dispatch path for the legacy pane-management surface using existing mux/session-style test seams already present in the repo. Prefer proving that `spawn`, `split-pane`, `send-text`, `get-text`, and `set-tab-title` still reach the expected request/dispatch behavior without launching a full GUI runtime. Include `list-task-panes` only as a coexistence check, not as a replacement for the legacy commands. Do not widen this into a comprehensive CLI integration harness for the whole app.</action>
  <verify>
    <automated>cargo test --locked -p kaku compatibility_cli -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku/tests/compatibility_cli.rs` exists and explicitly covers `list`, `spawn`, `split-pane`, `send-text`, `get-text`, and `set-tab-title`.
    - the compatibility slice contains at least one behavior-level legacy-command check, not only parser/help assertions.
    - `cargo test --locked -p kaku compatibility_cli -- --nocapture` exits successfully.
    - the tests remain compatibility-focused and do not introduce new behavior requirements for those baseline commands.
  </acceptance_criteria>
  <done>Baseline pane-management CLI behavior has a dedicated compatibility regression slice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard GUI refresh behavior so Task Center stays additive and baseline navigation remains intact</name>
  <files>kaku-gui/src/termwindow/mod.rs,kaku-gui/src/overlay/task_center.rs</files>
  <read_first>
    - kaku-gui/src/termwindow/mod.rs
    - kaku-gui/src/overlay/task_center.rs
    - .planning/phases/04-task-pane-lifecycle/04-UAT.md
    - .planning/phases/05-hardening-and-compatibility/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: lifecycle-change notifications refresh Task Center state without hijacking normal pane focus/navigation behavior.
    - Test 2: Task Center still acts as a consumer overlay over mux snapshots rather than owning lifecycle state or baseline workspace routing.
    - Test 3: GUI-side changes remain Kaku-native and do not add new shell surfaces or broad redesign behavior.
  </behavior>
  <action>Add focused GUI-side regression coverage in `kaku-gui/src/termwindow/mod.rs` and `kaku-gui/src/overlay/task_center.rs` for the compatibility-sensitive paths surfaced by Phase 4 and the Phase 5 research. Preserve `TaskPaneLifecycleChanged` refresh behavior, but ensure pane focus, tab navigation, and workspace interactions keep their existing Kaku feel before and after lifecycle mutations. Treat Task Center strictly as a consumer overlay per COMP-05; do not widen it into a controller, dashboard, or detached task shell.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui task_center -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --locked -p kaku-gui task_center -- --nocapture` exits successfully.
    - `cargo check --locked -p kaku-gui` exits successfully.
    - the Task Center code still reads as a consumer of mux-backed state rather than a new ownership layer.
  </acceptance_criteria>
  <done>GUI compatibility guardrails exist for lifecycle refresh paths while baseline Kaku navigation behavior remains intact.</done>
</task>

</tasks>

<verification>
Run the new CLI compatibility slice plus the existing Task Center test slice and `kaku-gui` compile check so both behavior-level baseline command preservation and GUI additivity are verified together.
</verification>

<success_criteria>
Wave 3 proves that the hardening work preserved baseline pane-management behavior at both parser and behavior level, and kept Task Center in its intended Kaku-native consumer role.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-and-compatibility/05-hardening-and-compatibility-03-SUMMARY.md`
</output>
