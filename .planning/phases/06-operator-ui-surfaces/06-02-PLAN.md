---
phase: 06-operator-ui-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - kaku-gui/src/overlay/task_center.rs
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04
  - UI-05
must_haves:
  truths:
    - "Task Center can be operated with keyboard and mouse using the same mental model."
    - "Common actions are visible directly on the current row instead of requiring CLI recall."
    - "Workspace metadata edits and remain-on-exit style toggles happen from Task Center without turning it into a multi-pane dashboard."
  artifacts:
    - path: "kaku-gui/src/overlay/task_center.rs"
      provides: "Single-surface operator workflow with row selection, inline actions, and compact metadata display"
      contains: "Task Center"
  key_links:
    - from: "kaku-gui/src/overlay/task_center.rs"
      to: "kaku-gui/src/termwindow/mod.rs"
      via: "row actions call TermWindow helpers for focus, unread clearing, rerun/respawn, remain-on-exit style toggles, and metadata edits"
      pattern: "TermWindowNotif::Apply"
---

<objective>
Turn Task Center into the primary operator surface for Phase 06 without widening it into a dashboard.

Purpose: The UI contract says the common path starts in Task Center, so the overlay must gain mouse parity, visible row actions, and compact metadata-edit affordances while preserving one-row density and the existing Kaku full-tab overlay model.
Output: An upgraded `task_center.rs` overlay with single-click selection, double-click primary action, inline action hit targets, compact workspace metadata edit affordances, and remain-on-exit style toggles where they apply.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
@.planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
@.planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
@.planning/phases/03-task-center-overlay/03-task-center-overlay-03-SUMMARY.md
@.planning/phases/03-task-center-overlay/03-task-center-overlay-04-SUMMARY.md
@.planning/phases/05-hardening-and-compatibility/05-hardening-and-compatibility-03-SUMMARY.md
@kaku-gui/src/overlay/task_center.rs
@kaku-gui/src/overlay/selector.rs
@kaku-gui/src/overlay/launcher.rs
@kaku-gui/src/overlay/confirm.rs
@kaku-gui/src/termwindow/mod.rs

<interfaces>
From `kaku-gui/src/overlay/task_center.rs`:
```rust
fn can_clear_unread(&self) -> bool
fn can_rerun(&self) -> bool
fn trigger_focus(&self)
fn trigger_clear_unread(&mut self)
fn trigger_rerun(&self)
fn format_row(entry: &TaskCenterEntry) -> String
```

From `kaku-gui/src/termwindow/mod.rs` after Plan 01:
```rust
pub(crate) fn focus_task_center_entry(&mut self, entry: &TaskCenterEntry) -> anyhow::Result<()>
pub(crate) fn clear_task_center_entry_unread(&mut self, entry: &TaskCenterEntry) -> usize
pub(crate) fn rerun_task_center_entry(&self, entry: &TaskCenterEntry) -> bool
// plus the new scoped-open / workspace-metadata helpers introduced in 06-01
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign Task Center rows and help text around visible common-path operator actions</name>
  <files>kaku-gui/src/overlay/task_center.rs</files>
  <read_first>
    - .planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
    - .planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
    - .planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
    - kaku-gui/src/overlay/task_center.rs
    - kaku-gui/src/overlay/selector.rs
    - kaku-gui/src/overlay/launcher.rs
  </read_first>
  <behavior>
    - Test 1: each Task Center row remains one line tall while still exposing compact action labels or glyphs for focus, clear unread, rerun/respawn, remain-on-exit style toggles, and metadata editing when those actions are actually available.
    - Test 2: the header/help copy stays within the three-line overlay contract from the UI spec and advertises the mouse+keyboard operator path per D-01 and D-02.
    - Test 3: the visual model stays additive per D-03 and D-05: no card stacks, no secondary description blocks, and no dashboard panel treatment.
  </behavior>
  <action>Refactor `kaku-gui/src/overlay/task_center.rs` so row formatting and header/help text match the approved UI contract. Keep every result to one row with the primary label first and compact inline suffixes/actions second. Add visible, conditional action affordances for the common path: focus, clear unread, rerun/respawn, a remain-on-exit style toggle for task-pane rows where lifecycle state makes that action meaningful, edit status, edit progress, and clear metadata only when the row/workspace supports them. Reuse the terse text-first Kaku style from the UI spec and preserve the existing normalized-list architecture; do not introduce cards, multi-line row bodies, or inspector panels. Explicitly avoid copying launcher click-to-launch semantics as-is because the research flags that as a mismatch for D-02 and the single-click-select/double-click-activate contract.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/overlay/task_center.rs` renders compact inline action affordances without breaking one-row density.
    - task-pane rows expose a remain-on-exit style toggle only when the current entry supports that lifecycle action.
    - Task Center header/help text advertises the approved keyboard and mouse contract instead of the old keyboard-only contract.
    - the `task_center`-filtered `kaku-gui` tests pass and include coverage for the row-format/action-visibility behavior.
    - `cargo fmt --all --check` and `cargo test --locked -p kaku-gui task_center -- --nocapture` succeed.
  </acceptance_criteria>
  <done>Task Center visibly advertises the common operator path while remaining a compact Kaku overlay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mouse parity and inline-action hit testing to Task Center without creating a second interaction model</name>
  <files>kaku-gui/src/overlay/task_center.rs</files>
  <read_first>
    - .planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
    - .planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
    - .planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
    - kaku-gui/src/overlay/task_center.rs
    - kaku-gui/src/overlay/selector.rs
    - kaku-gui/src/overlay/confirm.rs
    - kaku-gui/src/termwindow/mod.rs
  </read_first>
  <behavior>
    - Test 1: single click selects a row, double click performs the primary action, and wheel scrolling keeps selection/top-row state coherent.
    - Test 2: inline action hit targets dispatch through existing TermWindow helpers for focus, clear unread, rerun/respawn, remain-on-exit style toggles, and metadata edit/clear actions.
    - Test 3: the overlay tracks click streak locally or via existing window input state rather than assuming `MouseEvent` already carries double-click metadata, as warned in the research.
  </behavior>
  <action>Implement Task Center mouse semantics in `kaku-gui/src/overlay/task_center.rs` so they match the UI spec exactly: single-click select, double-click primary action, and clickable inline labels for clear unread, rerun/respawn, remain-on-exit style toggles, and workspace metadata edits. Keep the keyboard path authoritative and equivalent: Enter still focuses, Esc still closes, and the same TermWindow helpers from 06-01 perform the underlying work. Handle click streak intentionally instead of assuming `termwiz::input::MouseEvent` carries a double-click count, and keep wheel scroll behavior aligned with `selector.rs` patterns. Do not add right-click-only affordances or a separate mouse-only workflow because D-02 requires parity.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui task_center -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - mouse selection, double-click activation, wheel scrolling, and inline action hit testing are implemented in `kaku-gui/src/overlay/task_center.rs`.
    - inline action dispatch includes the remain-on-exit style toggle path for eligible task-pane rows.
    - inline action dispatch flows through `TermWindowNotif::Apply` into existing or Plan 01 helper methods rather than mutating overlay-owned state.
    - `cargo fmt --all --check`, `cargo test --locked -p kaku-gui task_center -- --nocapture`, and `cargo check --locked -p kaku-gui` succeed.
  </acceptance_criteria>
  <done>Task Center offers one consistent keyboard+mouse operator workflow instead of separate input models.</done>
</task>

</tasks>

<verification>
Run the Task Center test slice and a `kaku-gui` compile check after the mouse/action refactor so the upgraded overlay stays compact, event-driven, and additive.
</verification>

<success_criteria>
Wave 2A leaves Task Center as the obvious Phase 06 operator surface: compact, mouse+keyboard operable, and still recognizably Kaku rather than a dashboard shell.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operator-ui-surfaces/06-operator-ui-surfaces-02-SUMMARY.md`
</output>
