---
phase: 06-operator-ui-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codec/src/lib.rs
  - crates/wezterm-client/src/client.rs
  - crates/wezterm-mux-server-impl/src/sessionhandler.rs
  - kaku-gui/src/termwindow/mod.rs
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
must_haves:
  truths:
    - "The GUI can bootstrap common operator flows from native Kaku surfaces without shelling out to CLI recall."
    - "Workspace status and progress edits can travel through the existing client-domain path and refresh the visible UI state immediately."
    - "Task Center and tabbar surfaces can ask TermWindow for scoped operator views and task-pane toggles without inventing a second state owner."
  artifacts:
    - path: "crates/codec/src/lib.rs"
      provides: "Typed workspace-metadata transport contract needed by GUI common-path edits"
      contains: "ListWorkspaceProgress"
    - path: "crates/wezterm-client/src/client.rs"
      provides: "Client RPC wrappers for workspace metadata reads/writes used by the GUI"
      contains: "list_workspace_progress"
    - path: "kaku-gui/src/termwindow/mod.rs"
      provides: "Operator controller helpers for scoped Task Center opening, metadata refresh, metadata mutation, and remain-on-exit style toggles"
      contains: "show_task_center"
  key_links:
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "crates/wezterm-client/src/client.rs"
      via: "GUI metadata read/write flows stay on the existing client-domain RPC path"
      pattern: "set_workspace_status|clear_workspace_status|set_workspace_progress|clear_workspace_progress|list_workspace_progress"
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "kaku-gui/src/overlay/task_center.rs"
      via: "Scoped Task Center bootstrap and row actions flow through TermWindow instead of overlay-owned state"
      pattern: "show_task_center|set_remain_on_exit"
---

<objective>
Create the operator-controller seams that every Phase 06 UI surface will depend on.

Purpose: Phase 06 only stays additive if Task Center, tabbar affordances, and metadata edit flows all route through existing TermWindow/client-domain seams instead of inventing a new controller layer or mutating mux state directly from overlays.
Output: Typed workspace-metadata transport parity plus TermWindow helpers for scoped Task Center opening, metadata refresh, metadata mutation, and remain-on-exit style toggles.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
@.planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
@.planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
@.planning/phases/03-task-center-overlay/03-task-center-overlay-02-SUMMARY.md
@.planning/phases/05-hardening-and-compatibility/05-hardening-and-compatibility-03-SUMMARY.md
@crates/codec/src/lib.rs
@crates/wezterm-client/src/client.rs
@crates/wezterm-mux-server-impl/src/sessionhandler.rs
@kaku-gui/src/termwindow/mod.rs

<interfaces>
From `kaku-gui/src/termwindow/mod.rs`:
```rust
fn show_task_center(&mut self)
fn refresh_workspace_metadata_cache(&self)
fn apply_workspace_metadata_cache(&mut self, statuses: Vec<WorkspaceStatusState>)
pub(crate) fn rerun_task_center_entry(&self, entry: &TaskCenterEntry) -> bool
pub(crate) fn clear_task_center_entry_unread(&mut self, entry: &TaskCenterEntry) -> usize
pub(crate) fn focus_task_center_entry(&mut self, entry: &TaskCenterEntry) -> anyhow::Result<()>
```

From `crates/wezterm-client/src/client.rs`:
```rust
rpc!(set_workspace_status, SetWorkspaceStatus, SetWorkspaceStatusResponse);
rpc!(clear_workspace_status, ClearWorkspaceStatus, ClearWorkspaceStatusResponse);
rpc!(list_workspace_status, ListWorkspaceStatus, ListWorkspaceStatusResponse);
rpc!(set_workspace_progress, SetWorkspaceProgress, SetWorkspaceProgressResponse);
rpc!(clear_workspace_progress, ClearWorkspaceProgress, ClearWorkspaceProgressResponse);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the typed workspace-metadata transport so the GUI can read and refresh progress as well as status</name>
  <files>crates/codec/src/lib.rs,crates/wezterm-client/src/client.rs,crates/wezterm-mux-server-impl/src/sessionhandler.rs</files>
  <read_first>
    - .planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
    - .planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
    - .planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
    - crates/codec/src/lib.rs
    - crates/wezterm-client/src/client.rs
    - crates/wezterm-mux-server-impl/src/sessionhandler.rs
    - kaku/src/cli/set_progress.rs
    - kaku-gui/src/termwindow/mod.rs
  </read_first>
  <behavior>
    - Test 1: the client/server transport exposes a typed `list_workspace_progress` request/response so GUI code can refresh progress without reading mux state directly.
    - Test 2: the new transport shape matches the existing status/progress contract style rather than inventing a Phase 06-only RPC format.
    - Test 3: the transport remains additive per D-03 and D-05: no new daemon, no polling loop, and no dashboard-specific API.
  </behavior>
  <action>Add the missing typed progress read path in `crates/codec/src/lib.rs`, `crates/wezterm-client/src/client.rs`, and `crates/wezterm-mux-server-impl/src/sessionhandler.rs` so the GUI can refresh status and progress symmetrically over the existing client-domain channel. Mirror the existing `ListWorkspaceStatus` contract style and reuse the already shipped `WorkspaceProgressState` shape instead of creating a Phase 06-only payload. Keep the implementation event-driven and additive per D-03: no sidecar service, no polling transport, and no direct overlay->mux mutation path.</action>
  <verify>
    <automated>cargo check --locked -p wezterm-client -p wezterm-mux-server-impl -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - `crates/codec/src/lib.rs` defines a typed list-progress request/response path consumable by the GUI.
    - `crates/wezterm-client/src/client.rs` exposes a `list_workspace_progress` wrapper alongside the existing workspace-status/progress mutation wrappers.
    - `crates/wezterm-mux-server-impl/src/sessionhandler.rs` handles the new request using the existing mux progress records.
    - `cargo check --locked -p wezterm-client -p wezterm-mux-server-impl -p kaku-gui` succeeds.
  </acceptance_criteria>
  <done>The GUI has a stable transport contract for both workspace status and workspace progress reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TermWindow operator helpers for scoped Task Center opening and common-path metadata edits</name>
  <files>kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - .planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
    - .planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
    - .planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
    - kaku-gui/src/termwindow/mod.rs
    - kaku-gui/src/overlay/task_center.rs
    - kaku-gui/src/overlay/prompt.rs
    - kaku-gui/src/overlay/confirm.rs
    - crates/wezterm-client/src/client.rs
  </read_first>
  <behavior>
    - Test 1: TermWindow can open Task Center with a supplied initial query or workspace scope so later UI surfaces do not hand-roll their own snapshot filtering.
    - Test 2: TermWindow owns common-path workspace metadata edits for status/progress through the client-domain RPC path, with immediate cache refresh after success.
    - Test 3: TermWindow exposes a remain-on-exit style task-pane toggle helper that UI surfaces can call without shelling out to CLI or mutating overlay-owned state.
    - Test 4: the controller seams stay Kaku-native per D-01, D-02, and D-03: overlays and tabbar affordances ask TermWindow to act; they do not become new state owners.
  </behavior>
  <action>Refactor `kaku-gui/src/termwindow/mod.rs` into a reusable Phase 06 controller seam. Add helper methods for opening Task Center with an initial filter/query, resolving current-workspace metadata context, refreshing both status and progress caches, performing UI-initiated status/progress set-or-clear mutations through the typed client wrappers from Task 1, and applying a remain-on-exit style task-pane toggle through the existing lifecycle/state path. Reuse existing prompt/confirm overlay plumbing for user input instead of inventing a new editor surface, and keep mutation success paths event-driven by refreshing the same caches/title updates the tabbar already consumes. Do not mutate mux workspace metadata or task-center overlay state directly from the GUI because D-03 and the research require the common path to stay aligned with shipped client-domain and lifecycle behavior.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui workspace_metadata -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/termwindow/mod.rs` exposes reusable helpers for scoped Task Center opening, workspace metadata mutation, and remain-on-exit style toggles.
    - workspace status and workspace progress cache refreshes both exist and are driven by the client-domain path.
    - a remain-on-exit style toggle helper exists for later Task Center row actions without requiring CLI recall.
    - new tests cover the helper-level metadata and scoped-open behavior under a `workspace_metadata`-filtered `kaku-gui` test run.
    - `cargo fmt --all --check`, `cargo test --locked -p kaku-gui workspace_metadata -- --nocapture`, and `cargo check --locked -p kaku-gui` succeed.
  </acceptance_criteria>
  <done>TermWindow becomes the single controller seam for Phase 06 operator surfaces, metadata edits, and remain-on-exit style toggles.</done>
</task>

</tasks>

<verification>
Compile the GUI/client/server crates together and run the new `workspace_metadata`-filtered `kaku-gui` tests so the Phase 06 controller seam is proven before any overlay or tabbar surface starts consuming it.
</verification>

<success_criteria>
Wave 1 leaves the repo with one authoritative Phase 06 controller seam: typed workspace metadata transport parity plus TermWindow helpers that later plans can call without inventing second-owner UI state.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operator-ui-surfaces/06-operator-ui-surfaces-01-SUMMARY.md`
</output>
