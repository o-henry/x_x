---
phase: 06-operator-ui-surfaces
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - kaku-gui/src/tabbar.rs
  - kaku-gui/src/termwindow/mouseevent.rs
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-05
must_haves:
  truths:
    - "Developers can discover that operator state exists from always-visible Kaku surfaces, not only after opening Task Center."
    - "The passive operator rail stays compact and text-led instead of becoming a new workflow hub."
    - "Clicking an actionable operator marker opens the correct scoped Task Center view rather than a separate shell."
  artifacts:
    - path: "kaku-gui/src/tabbar.rs"
      provides: "Compact operator markers and metadata-affordance hit regions in the existing tabbar path"
      contains: "TabBarItem"
    - path: "kaku-gui/src/termwindow/mouseevent.rs"
      provides: "Mouse routing for operator-marker clicks without disturbing normal tab drag or tab activation behavior"
      contains: "mouse_event_tab_bar"
  key_links:
    - from: "kaku-gui/src/tabbar.rs"
      to: "kaku-gui/src/termwindow/mouseevent.rs"
      via: "operator marker hit regions become scoped Task Center entry points"
      pattern: "TabBarItem"
    - from: "kaku-gui/src/termwindow/mouseevent.rs"
      to: "kaku-gui/src/termwindow/mod.rs"
      via: "tabbar clicks reuse the scoped Task Center bootstrap from Plan 01"
      pattern: "show_task_center"
---

<objective>
Expose operator discoverability from the always-visible tabbar without turning the tabbar into a dashboard.

Purpose: The UI contract keeps Task Center as the workflow surface, but UI-01 still requires everyday discoverability from native Kaku UI. The tabbar should advertise unread/status/progress/operator state and offer a compact click path into scoped Task Center views.
Output: Compact operator markers in the tabbar and matching mouse routing that opens Task Center prefiltered to the current workspace/tab when appropriate.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
@.planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
@.planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
@.planning/phases/02-workspace-metadata-plane/02-workspace-metadata-plane-04-SUMMARY.md
@.planning/phases/03-task-center-overlay/03-task-center-overlay-04-SUMMARY.md
@.planning/phases/05-hardening-and-compatibility/05-hardening-and-compatibility-03-SUMMARY.md
@kaku-gui/src/tabbar.rs
@kaku-gui/src/termwindow/mouseevent.rs
@kaku-gui/src/termwindow/mod.rs

<interfaces>
From `kaku-gui/src/tabbar.rs`:
```rust
pub enum TabBarItem {
    None,
    LeftStatus,
    RightStatus,
    Tab { tab_idx: usize, active: bool },
    NewTabButton,
    WindowButton(IntegratedTitleButton),
}
```

From `kaku-gui/src/termwindow/mouseevent.rs`:
```rust
fn mouse_event_ui_item(&mut self, item: UIItem, pane: Arc<dyn Pane>, _y: i64, event: MouseEvent, context: &dyn WindowOps)
fn mouse_event_tab_bar(&mut self, tab_bar_item: TabBarItem, item: UIItem, event: MouseEvent, context: &dyn WindowOps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the tabbar title path with compact operator markers that stay passive and Kaku-native</name>
  <files>kaku-gui/src/tabbar.rs</files>
  <read_first>
    - .planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
    - .planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
    - .planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
    - kaku-gui/src/tabbar.rs
    - kaku-gui/src/termwindow/mod.rs
  </read_first>
  <behavior>
    - Test 1: the tabbar still renders compact text-led titles, unread markers, and workspace metadata without becoming pill-heavy or button-heavy.
    - Test 2: actionable operator state is exposed through explicit marker regions or suffixes that can later be hit-tested, while non-actionable rows remain passive.
    - Test 3: the operator rail reflects D-03 and D-05 by staying additive to the existing title path rather than replacing it with a sidebar or inspector model.
  </behavior>
  <action>Adjust `kaku-gui/src/tabbar.rs` so the existing unread/status/progress suffix path also exposes a compact operator affordance for actionable control-plane state. The result should stay text-first and one-line, using the approved dark, tool-like, terminal-native rhythm from the UI spec. If you need new `TabBarItem` variants or UI item boundaries for operator markers, add only the minimum needed for hit testing and hover state. Do not turn the tabbar into a workflow hub: no large badges, no button strips, and no inline metadata editors.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui tabbar -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/tabbar.rs` preserves compact title composition while exposing an explicit operator affordance for actionable control-plane state.
    - tabbar tests cover the operator-marker/title composition behavior under a `tabbar`-filtered `kaku-gui` run.
    - `cargo fmt --all --check` and `cargo test --locked -p kaku-gui tabbar -- --nocapture` succeed.
  </acceptance_criteria>
  <done>The always-visible tabbar advertises operator state without losing its Kaku-style density.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route operator-marker clicks through existing tabbar mouse handling into scoped Task Center views</name>
  <files>kaku-gui/src/termwindow/mouseevent.rs</files>
  <read_first>
    - .planning/phases/06-operator-ui-surfaces/06-CONTEXT.md
    - .planning/phases/06-operator-ui-surfaces/06-RESEARCH.md
    - .planning/phases/06-operator-ui-surfaces/06-UI-SPEC.md
    - kaku-gui/src/termwindow/mouseevent.rs
    - kaku-gui/src/tabbar.rs
    - kaku-gui/src/termwindow/mod.rs
  </read_first>
  <behavior>
    - Test 1: clicking the new operator marker opens Task Center prefiltered to the relevant current workspace or tab instead of activating a separate shell.
    - Test 2: normal tab activation, tab drag, close-tab, and window-button behavior remain unchanged.
    - Test 3: hover emphasis appears only when the marker is actionable, matching the UI spec’s “hover is informative, not decorative” rule.
  </behavior>
  <action>Update `kaku-gui/src/termwindow/mouseevent.rs` to recognize the new tabbar operator marker from Task 1 and route left-click into the scoped Task Center bootstrap created in 06-01. Preserve every existing tabbar behavior first: tab activation, drag-reorder, rename, close-tab, and new-tab must continue to work exactly as before. Add only the minimum hover/cursor behavior needed to communicate actionability, and keep the click result inside Kaku’s existing frame by opening Task Center rather than any new inspector or context menu. This is the D-01 discoverability path, not a second workflow surface.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku-gui tabbar -- --nocapture && cargo check --locked -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - operator-marker clicks in `kaku-gui/src/termwindow/mouseevent.rs` route into scoped Task Center opening rather than a new surface.
    - existing tabbar mouse behaviors continue to compile and remain in place.
    - `cargo fmt --all --check`, `cargo test --locked -p kaku-gui tabbar -- --nocapture`, and `cargo check --locked -p kaku-gui` succeed.
  </acceptance_criteria>
  <done>The tabbar becomes a compact discovery rail that hands off to Task Center without becoming a dashboard.</done>
</task>

</tasks>

<verification>
Run the `tabbar` test slice and a `kaku-gui` compile check after the tabbar/mouse routing changes so discoverability improves without regressing baseline tabbar behavior.
</verification>

<success_criteria>
Wave 2B makes operator state discoverable from the always-visible Kaku frame while keeping the actual workflow inside Task Center and preserving normal tabbar behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operator-ui-surfaces/06-operator-ui-surfaces-03-SUMMARY.md`
</output>
