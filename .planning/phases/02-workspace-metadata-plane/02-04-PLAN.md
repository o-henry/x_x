---
phase: 02-workspace-metadata-plane
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - kaku-gui/src/termwindow/mod.rs
  - kaku-gui/src/tabbar.rs
autonomous: true
requirements:
  - META-05
must_haves:
  truths:
    - "A developer can see workspace status and progress in existing Kaku tab surfaces without opening a new app shell."
    - "GUI updates for workspace metadata remain event-driven through mux notifications rather than polling loops."
    - "Custom Lua tab title formatters can read workspace status and progress through existing `TabInformation` data."
  artifacts:
    - path: "kaku-gui/src/termwindow/mod.rs"
      provides: "Workspace metadata snapshot wiring and refresh triggers"
      contains: "WorkspaceMetadataChanged"
    - path: "kaku-gui/src/tabbar.rs"
      provides: "Default tab title composition for workspace status and progress"
      contains: "compute_tab_plain_title"
  key_links:
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "mux/src/lib.rs"
      via: "workspace metadata lookups while building `TabInformation`"
      pattern: "workspace_status_for_workspace|workspace_progress_for_workspace"
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "MuxNotification::WorkspaceMetadataChanged"
      via: "event-driven tabbar/status invalidation"
      pattern: "WorkspaceMetadataChanged"
    - from: "kaku-gui/src/tabbar.rs"
      to: "TabInformation"
      via: "default title composition using workspace status/progress fields"
      pattern: "workspace_status|workspace_progress"
---

<objective>
Surface Phase 2 workspace metadata in existing Kaku UI paths without introducing a new shell or overlay-centric workflow.

Purpose: Implement D-06, D-08, D-09, and D-10 by showing workspace status and progress on the existing tabbar/title path and keeping refreshes mux-driven.
Output: Metadata-aware `TabInformation`, event-driven GUI refresh wiring, and compact default tab title rendering.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
@.planning/phases/02-workspace-metadata-plane/02-RESEARCH.md
@.planning/phases/01-notification-routing/01-notification-routing-04-SUMMARY.md
@kaku-gui/src/termwindow/mod.rs
@kaku-gui/src/tabbar.rs

<interfaces>
From `kaku-gui/src/termwindow/mod.rs`:
```rust
pub struct TabInformation {
    pub tab_title: String,
    pub has_unread_notifications: bool,
    pub unread_notification_count: usize,
}
```

From `kaku-gui/src/tabbar.rs`:
```rust
pub fn compute_tab_plain_title(tab: &TabInformation) -> String;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend termwindow metadata snapshots and refresh triggers</name>
  <files>kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - kaku-gui/src/termwindow/mod.rs
    - kaku-gui/src/tabbar.rs
    - mux/src/lib.rs
    - .planning/phases/01-notification-routing/01-notification-routing-04-SUMMARY.md
    - .planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `TabInformation` exposes `workspace_status` and `workspace_progress` fields to Rust callers and Lua userdata.
    - Test 2: `get_tab_information()` resolves the current window workspace, then reads workspace status and progress from mux-owned state instead of pane user vars or GUI-local caches.
    - Test 3: `WorkspaceMetadataChanged` triggers the same tabbar/status invalidation path that existing unread metadata uses, without adding a polling loop.
  </behavior>
  <action>Update `kaku-gui/src/termwindow/mod.rs` so `TabInformation` includes `workspace_status: Option<String>` and `workspace_progress: Option<u8>`, and expose both fields through the existing `impl UserData for TabInformation`. In `get_tab_information()`, use the current window workspace string as the lookup key and populate the new fields through `Mux::workspace_status_for_workspace` and `Mux::workspace_progress_for_workspace` from plan 01; do not read pane `Progress`, pane user vars, or any GUI-local cache for this workspace-level data. Extend `mux_notification_requires_tabbar_refresh` so `MuxNotification::WorkspaceMetadataChanged` returns `true`, and make the main notification handler call `emit_status_event()` plus `update_title()` when metadata changes so existing title/status surfaces refresh through the same event-driven path as notifications.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui tabbar -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/termwindow/mod.rs` contains `workspace_status: Option<String>` and `workspace_progress: Option<u8>`.
    - `kaku-gui/src/termwindow/mod.rs` exposes `workspace_status` and `workspace_progress` in `impl UserData for TabInformation`.
    - `kaku-gui/src/termwindow/mod.rs` references `WorkspaceMetadataChanged`.
    - `kaku-gui/src/termwindow/mod.rs` references `workspace_status_for_workspace` and `workspace_progress_for_workspace`.
    - `cargo test --locked -p kaku-gui tabbar -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>`TermWindow` builds metadata-aware tab snapshots from mux-owned state and refreshes existing UI surfaces when workspace metadata changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose a compact default tab title for workspace status and progress</name>
  <files>kaku-gui/src/tabbar.rs</files>
  <read_first>
    - kaku-gui/src/tabbar.rs
    - kaku-gui/src/termwindow/mod.rs
    - .planning/phases/02-workspace-metadata-plane/02-RESEARCH.md
    - docs/KAKU_CONTROL_PLANE_SPEC.md
  </read_first>
  <behavior>
    - Test 1: when a tab has `workspace_status = Some("blocked")` and no progress, `compute_tab_plain_title()` appends ` · [blocked]` after the base title.
    - Test 2: when a tab has `workspace_progress = Some(37)` and no status, `compute_tab_plain_title()` appends ` · 37%`.
    - Test 3: when both exist, `compute_tab_plain_title()` appends ` · [blocked] 37%` and still preserves the existing `! ` unread prefix behavior.
  </behavior>
  <action>Update the default title composition in `kaku-gui/src/tabbar.rs` so workspace metadata is rendered only in the existing default title path, keeping the phase visually additive per D-08. Add a helper that builds a suffix from `TabInformation.workspace_status` and `TabInformation.workspace_progress` using exactly these formats: status only -> ` · [status]`, progress only -> ` · NN%`, both -> ` · [status] NN%`. Apply the metadata suffix before the existing unread prefix helper so unread tabs still render as `! <base> · [status] 37%`. Do not add new colors, panes, overlays, or separate progress bars in this phase; keep the rendering to plain title text and the existing tabbar layout.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui tabbar -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/tabbar.rs` contains `workspace_status` and `workspace_progress`.
    - `kaku-gui/src/tabbar.rs` contains the literal format pieces ` · [` and `%`.
    - `kaku-gui/src/tabbar.rs` keeps `prefix_unread_marker`.
    - `kaku-gui/src/tabbar.rs` contains tests for status-only, progress-only, and combined status-plus-progress title composition.
    - `cargo test --locked -p kaku-gui tabbar -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>The default tabbar path shows workspace status and progress in a compact Kaku-native title suffix while preserving existing unread marker behavior.</done>
</task>

</tasks>

<verification>
Run the focused Kaku GUI tabbar test target and confirm workspace metadata flows from mux into `TabInformation`, refreshes on `WorkspaceMetadataChanged`, and renders in the default tab title path.
</verification>

<success_criteria>
Workspace status and progress are visible on existing Kaku tab surfaces with event-driven updates and without introducing a new shell or overlay workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-metadata-plane/02-workspace-metadata-plane-04-SUMMARY.md`
</output>
