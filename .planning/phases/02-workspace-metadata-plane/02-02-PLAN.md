---
phase: 02-workspace-metadata-plane
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - crates/codec/src/lib.rs
  - crates/wezterm-client/src/client.rs
  - crates/wezterm-mux-server-impl/src/sessionhandler.rs
  - crates/wezterm-mux-server-impl/src/dispatch.rs
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
must_haves:
  truths:
    - "A developer can send typed status, progress, and log requests without scraping human text."
    - "Transport responses preserve stable field names for workspace metadata records and mutation summaries."
    - "Mux-server handling for Phase 2 routes through mux-owned workspace metadata APIs rather than inventing a second state path."
  artifacts:
    - path: "crates/codec/src/lib.rs"
      provides: "Typed workspace metadata request and response PDUs"
      exports: ["SetWorkspaceStatus", "WorkspaceStatusState", "WorkspaceLogState"]
    - path: "crates/wezterm-client/src/client.rs"
      provides: "Client RPC wrappers for workspace metadata flows"
      contains: "set_workspace_status"
    - path: "crates/wezterm-mux-server-impl/src/sessionhandler.rs"
      provides: "Mux-server dispatch into workspace metadata APIs"
      contains: "Pdu::SetWorkspaceStatus"
  key_links:
    - from: "crates/codec/src/lib.rs"
      to: "crates/wezterm-client/src/client.rs"
      via: "rpc! wrappers for workspace metadata commands"
      pattern: "rpc!"
    - from: "crates/wezterm-client/src/client.rs"
      to: "crates/wezterm-mux-server-impl/src/sessionhandler.rs"
      via: "typed request and response dispatch"
      pattern: "Pdu::SetWorkspaceStatus|Pdu::AppendWorkspaceLog"
    - from: "crates/wezterm-mux-server-impl/src/sessionhandler.rs"
      to: "mux/src/lib.rs"
      via: "Mux workspace metadata wrapper methods from plan 01"
      pattern: "set_workspace_status|append_workspace_log"
---

<objective>
Add the typed transport layer for workspace metadata so Phase 2 CLI commands can call mux-owned state through stable machine-readable contracts.

Purpose: Implement D-03, D-04, and D-05 by following the Phase 1 codec/client/server pattern instead of overloading existing pane or tab commands.
Output: Workspace metadata PDUs in `codec`, client RPC wrappers, and mux-server dispatch handlers.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
@.planning/phases/02-workspace-metadata-plane/02-RESEARCH.md
@.planning/phases/01-notification-routing/01-notification-routing-02-SUMMARY.md
@crates/codec/src/lib.rs
@crates/wezterm-client/src/client.rs
@crates/wezterm-mux-server-impl/src/sessionhandler.rs
@crates/wezterm-mux-server-impl/src/dispatch.rs

<interfaces>
From `crates/wezterm-client/src/client.rs`:
```rust
rpc!(create_notification, CreateNotification, CreateNotificationResponse);
rpc!(list_notifications, ListNotifications, ListNotificationsResponse);
```

From `crates/codec/src/lib.rs`:
```rust
pub struct ListNotificationsResponse {
    pub notifications: Vec<NotificationRecordState>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed workspace metadata PDUs and stable payload shapes</name>
  <files>crates/codec/src/lib.rs</files>
  <read_first>
    - crates/codec/src/lib.rs
    - .planning/phases/01-notification-routing/01-notification-routing-02-SUMMARY.md
    - .planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
    - mux/src/workspace_state.rs
  </read_first>
  <behavior>
    - Test 1: status responses serialize exactly `workspace`, `status`, and `updated_at`.
    - Test 2: progress responses serialize exactly `workspace`, `value`, and `updated_at`, with `value` represented as an integer percentage.
    - Test 3: log responses serialize exactly `workspace`, `seq`, `message`, and `created_at`, and list responses keep append order.
    - Test 4: clear responses return deterministic mutation summaries instead of empty success payloads.
  </behavior>
  <action>Add serializable Phase 2 payloads to `crates/codec/src/lib.rs` with RFC 3339 timestamp strings. Define `WorkspaceStatusState { workspace: String, status: String, updated_at: String }`, `WorkspaceProgressState { workspace: String, value: u8, updated_at: String }`, and `WorkspaceLogState { workspace: String, seq: u64, message: String, created_at: String }`. Add requests and responses for the exact Phase 2 command set from D-05: `SetWorkspaceStatus { workspace: String, status: String }`, `SetWorkspaceStatusResponse { status: WorkspaceStatusState }`, `ClearWorkspaceStatus { workspace: Option<String> }`, `ClearWorkspaceStatusResponse { cleared_count: usize, workspaces: Vec<String> }`, `ListWorkspaceStatus { workspace: Option<String> }`, `ListWorkspaceStatusResponse { statuses: Vec<WorkspaceStatusState> }`, `SetWorkspaceProgress { workspace: String, value: u8 }`, `SetWorkspaceProgressResponse { progress: WorkspaceProgressState }`, `ClearWorkspaceProgress { workspace: Option<String> }`, `ClearWorkspaceProgressResponse { cleared_count: usize, workspaces: Vec<String> }`, `AppendWorkspaceLog { workspace: String, message: String }`, `AppendWorkspaceLogResponse { entry: WorkspaceLogState }`, `ClearWorkspaceLog { workspace: Option<String> }`, `ClearWorkspaceLogResponse { cleared_count: usize, workspaces: Vec<String> }`, `ListWorkspaceLog { workspace: Option<String>, limit: Option<usize> }`, and `ListWorkspaceLogResponse { entries: Vec<WorkspaceLogState> }`. Register matching `Pdu` variants for every request and response. Do not add a generic `workspace-metadata` multiplexer command or any Phase 3 overlay/event-stream PDUs here.</action>
  <verify>
    <automated>cargo test --locked -p codec -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/codec/src/lib.rs` contains `WorkspaceStatusState`, `WorkspaceProgressState`, and `WorkspaceLogState`.
    - `crates/codec/src/lib.rs` contains `SetWorkspaceStatus`, `ClearWorkspaceStatus`, `ListWorkspaceStatus`, `SetWorkspaceProgress`, `ClearWorkspaceProgress`, `AppendWorkspaceLog`, `ClearWorkspaceLog`, and `ListWorkspaceLog`.
    - `crates/codec/src/lib.rs` contains `SetWorkspaceStatusResponse`, `ClearWorkspaceStatusResponse`, `ListWorkspaceStatusResponse`, `SetWorkspaceProgressResponse`, `ClearWorkspaceProgressResponse`, `AppendWorkspaceLogResponse`, `ClearWorkspaceLogResponse`, and `ListWorkspaceLogResponse`.
    - `crates/codec/src/lib.rs` contains the exact response field names `statuses`, `progress`, `entry`, `entries`, `cleared_count`, and `workspaces`.
    - `cargo test --locked -p codec -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>The transport layer has explicit, stable request and response payloads for every Phase 2 status, progress, and log command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add client wrappers and mux-server dispatch for workspace metadata RPCs</name>
  <files>crates/wezterm-client/src/client.rs, crates/wezterm-mux-server-impl/src/sessionhandler.rs, crates/wezterm-mux-server-impl/src/dispatch.rs</files>
  <read_first>
    - crates/wezterm-client/src/client.rs
    - crates/wezterm-mux-server-impl/src/sessionhandler.rs
    - crates/wezterm-mux-server-impl/src/dispatch.rs
    - crates/codec/src/lib.rs
    - mux/src/lib.rs
    - mux/src/workspace_state.rs
  </read_first>
  <behavior>
    - Test 1: each client wrapper uses the matching request and response type for the corresponding Phase 2 command.
    - Test 2: server dispatch calls the new mux wrapper methods from plan 01 and converts mux records into the exact transport states from task 1.
    - Test 3: the mux-server dispatch loop remains exhaustive after `WorkspaceMetadataChanged` is introduced and ignores that event without breaking pane or notification dispatch.
  </behavior>
  <action>In `crates/wezterm-client/src/client.rs`, add `rpc!` wrappers named `set_workspace_status`, `clear_workspace_status`, `list_workspace_status`, `set_workspace_progress`, `clear_workspace_progress`, `append_workspace_log`, `clear_workspace_log`, and `list_workspace_log`. In `crates/wezterm-mux-server-impl/src/sessionhandler.rs`, add a `Pdu` arm for each request and call the exact mux methods from plan 01. Convert `chrono` timestamps to RFC 3339 strings using `to_rfc3339()` or `to_rfc3339_opts`, not display-oriented `.to_string()`. `ListWorkspaceStatus` and `ListWorkspaceLog` must support optional workspace filters, and `ListWorkspaceLog.limit` must trim results from the newest end after applying the workspace filter. For progress, reject out-of-range values only through the typed `u8` plus mux validation path; do not introduce stringly parsing. In `crates/wezterm-mux-server-impl/src/dispatch.rs`, add an explicit no-op arm for `MuxNotification::WorkspaceMetadataChanged` so transport dispatch stays exhaustive without inventing a streamed Phase 2 event contract.</action>
  <verify>
    <automated>cargo check --locked -p wezterm-client -p wezterm-mux-server-impl</automated>
  </verify>
  <acceptance_criteria>
    - `crates/wezterm-client/src/client.rs` contains `rpc!(set_workspace_status`, `rpc!(clear_workspace_status`, `rpc!(list_workspace_status`, `rpc!(set_workspace_progress`, `rpc!(clear_workspace_progress`, `rpc!(append_workspace_log`, `rpc!(clear_workspace_log`, and `rpc!(list_workspace_log`.
    - `crates/wezterm-mux-server-impl/src/sessionhandler.rs` contains `Pdu::SetWorkspaceStatus`, `Pdu::ClearWorkspaceStatus`, `Pdu::ListWorkspaceStatus`, `Pdu::SetWorkspaceProgress`, `Pdu::ClearWorkspaceProgress`, `Pdu::AppendWorkspaceLog`, `Pdu::ClearWorkspaceLog`, and `Pdu::ListWorkspaceLog`.
    - `crates/wezterm-mux-server-impl/src/sessionhandler.rs` contains `to_rfc3339`.
    - `crates/wezterm-mux-server-impl/src/dispatch.rs` contains `MuxNotification::WorkspaceMetadataChanged`.
    - `cargo check --locked -p wezterm-client -p wezterm-mux-server-impl` exits successfully.
  </acceptance_criteria>
  <done>Phase 2 metadata calls can travel end-to-end through typed codec, client, and mux-server paths without any ad hoc parsing or polling behavior.</done>
</task>

</tasks>

<verification>
Build the client and mux-server crates together and confirm every Phase 2 metadata action has a typed request, typed response, and server dispatch path into mux-owned state.
</verification>

<success_criteria>
Every workspace metadata operation in Phase 2 has a typed codec payload, a client wrapper, and a mux-server dispatch handler wired to the mux-owned store.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-metadata-plane/02-workspace-metadata-plane-02-SUMMARY.md`
</output>
