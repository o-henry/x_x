---
phase: 02-workspace-metadata-plane
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - kaku/src/cli/mod.rs
  - kaku/src/cli/set_status.rs
  - kaku/src/cli/clear_status.rs
  - kaku/src/cli/list_status.rs
  - kaku/src/cli/set_progress.rs
  - kaku/src/cli/clear_progress.rs
  - kaku/src/cli/log.rs
  - kaku/src/cli/clear_log.rs
  - kaku/src/cli/list_log.rs
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
must_haves:
  truths:
    - "A developer can set, clear, and list workspace status through dedicated `kaku cli` subcommands."
    - "A developer can set and clear workspace progress through dedicated `kaku cli` subcommands."
    - "A developer can append, list, and clear workspace-scoped log entries through dedicated `kaku cli` subcommands with stable JSON output."
  artifacts:
    - path: "kaku/src/cli/mod.rs"
      provides: "Phase 2 CLI registration for workspace metadata commands"
      contains: "set-status"
    - path: "kaku/src/cli/list_status.rs"
      provides: "Machine-readable listing for workspace status records"
      contains: "ListWorkspaceStatusCommand"
    - path: "kaku/src/cli/list_log.rs"
      provides: "Machine-readable listing for workspace log entries"
      contains: "ListWorkspaceLogCommand"
  key_links:
    - from: "kaku/src/cli/mod.rs"
      to: "crates/wezterm-client/src/client.rs"
      via: "registered workspace metadata subcommands call typed RPC wrappers"
      pattern: "set_workspace_status|append_workspace_log"
    - from: "kaku/src/cli/list_status.rs"
      to: "crates/codec/src/lib.rs"
      via: "stable JSON rendering of `WorkspaceStatusState`"
      pattern: "WorkspaceStatusState"
    - from: "kaku/src/cli/list_log.rs"
      to: "crates/codec/src/lib.rs"
      via: "stable JSON rendering of `WorkspaceLogState`"
      pattern: "WorkspaceLogState"
---

<objective>
Expose the Phase 2 workspace metadata surface through dedicated `kaku cli` commands with stable machine-readable output.

Purpose: Implement D-03, D-04, and D-05 by following the existing one-subcommand-per-file CLI pattern for status, progress, and log operations.
Output: Eight registered CLI modules with focused contract tests.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
@.planning/phases/01-notification-routing/01-notification-routing-03-SUMMARY.md
@kaku/src/cli/mod.rs
@kaku/src/cli/notify.rs
@kaku/src/cli/list_notifications.rs

<interfaces>
From `kaku/src/cli/notify.rs`:
```rust
pub struct NotifyCommand;
impl NotifyCommand {
    fn to_request(&self) -> CreateNotification;
    pub async fn run(&self, client: Client) -> anyhow::Result<()>;
}
```

From `kaku/src/cli/list_notifications.rs`:
```rust
pub struct ListNotificationsCommand {
    #[arg(long = "format", default_value = "table")]
    format: CliOutputFormatKind,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add status and progress CLI commands with stable mutation and list contracts</name>
  <files>kaku/src/cli/mod.rs, kaku/src/cli/set_status.rs, kaku/src/cli/clear_status.rs, kaku/src/cli/list_status.rs, kaku/src/cli/set_progress.rs, kaku/src/cli/clear_progress.rs</files>
  <read_first>
    - kaku/src/cli/mod.rs
    - kaku/src/cli/notify.rs
    - kaku/src/cli/list_notifications.rs
    - crates/wezterm-client/src/client.rs
    - crates/codec/src/lib.rs
    - .planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `set-status --workspace unity-main --status blocked` sends `SetWorkspaceStatus` and prints a JSON object containing exactly `workspace`, `status`, and `updated_at`.
    - Test 2: `clear-status --workspace unity-main` prints a JSON object containing exactly `cleared_count` and `workspaces`.
    - Test 3: `list-status --format json` prints a JSON array of `WorkspaceStatusState` records and `--format table` renders the columns `WORKSPACE`, `STATUS`, and `UPDATED_AT`.
    - Test 4: `set-progress --workspace unity-main --value 37` sends `SetWorkspaceProgress` and prints exactly `workspace`, `value`, and `updated_at`, while `clear-progress` prints the same mutation summary shape as status clear.
  </behavior>
  <action>Create dedicated CLI modules `set_status.rs`, `clear_status.rs`, `list_status.rs`, `set_progress.rs`, and `clear_progress.rs`, and register them in `kaku/src/cli/mod.rs` under the exact subcommand names `set-status`, `clear-status`, `list-status`, `set-progress`, and `clear-progress` per D-03 and D-05. Use the Phase 1 command pattern: each mutation command builds a typed request in a `to_request()` helper, calls the corresponding `wezterm_client::client::Client` RPC, and prints pretty JSON with a trailing newline. `list-status` must support `--format table|json`; its table mode must include exactly the columns `WORKSPACE`, `STATUS`, and `UPDATED_AT`. `set-progress` must accept `--value <u8>` and not invent a list-progress command in this phase. Add focused contract tests under a shared `workspace_metadata_contracts` naming pattern so `cargo test --locked -p kaku workspace_metadata_contracts -- --nocapture` exercises the new request and JSON/table output shapes.</action>
  <verify>
    <automated>cargo test --locked -p kaku workspace_metadata_contracts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku/src/cli/mod.rs` contains `mod set_status;`, `mod clear_status;`, `mod list_status;`, `mod set_progress;`, and `mod clear_progress;`.
    - `kaku/src/cli/mod.rs` contains subcommand registrations named `set-status`, `clear-status`, `list-status`, `set-progress`, and `clear-progress`.
    - `kaku/src/cli/list_status.rs` contains the exact table column names `WORKSPACE`, `STATUS`, and `UPDATED_AT`.
    - `kaku/src/cli/set_progress.rs` contains `--value`.
    - `kaku/src/cli/clear_progress.rs` renders `cleared_count`.
    - `cargo test --locked -p kaku workspace_metadata_contracts -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>The CLI exposes dedicated status and progress commands with stable JSON contracts and a table listing path for status records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add workspace log CLI commands with append, list, and clear contracts</name>
  <files>kaku/src/cli/mod.rs, kaku/src/cli/log.rs, kaku/src/cli/clear_log.rs, kaku/src/cli/list_log.rs</files>
  <read_first>
    - kaku/src/cli/mod.rs
    - kaku/src/cli/list_notifications.rs
    - kaku/src/cli/clear_notifications.rs
    - crates/wezterm-client/src/client.rs
    - crates/codec/src/lib.rs
    - mux/src/workspace_state.rs
  </read_first>
  <behavior>
    - Test 1: `log --workspace unity-main --message "build failed"` sends `AppendWorkspaceLog` and prints a JSON object containing exactly `workspace`, `seq`, `message`, and `created_at`.
    - Test 2: `list-log --workspace unity-main --limit 20 --format json` sends `ListWorkspaceLog` with `limit: Some(20)` and prints a JSON array of `WorkspaceLogState` entries.
    - Test 3: `list-log --format table` renders exactly the columns `WORKSPACE`, `SEQ`, `MESSAGE`, and `CREATED_AT`.
    - Test 4: `clear-log --workspace unity-main` prints a mutation summary with exactly `cleared_count` and `workspaces`.
  </behavior>
  <action>Create `kaku/src/cli/log.rs`, `kaku/src/cli/clear_log.rs`, and `kaku/src/cli/list_log.rs`, and register the exact subcommand names `log`, `clear-log`, and `list-log` in `kaku/src/cli/mod.rs`. `log` should accept `--workspace <string>` and `--message <string>` and print the appended entry JSON returned from transport. `list-log` must support `--workspace`, `--limit`, and `--format table|json`; its table mode must include exactly `WORKSPACE`, `SEQ`, `MESSAGE`, and `CREATED_AT`. `clear-log` must print the typed mutation summary and should accept an optional `--workspace` filter so the same command can clear one workspace or all workspaces. Keep the output stable and machine-readable; do not add history search, grep-style filtering, or interactive browsing because those are deferred by the phase context.</action>
  <verify>
    <automated>cargo test --locked -p kaku workspace_metadata_contracts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku/src/cli/mod.rs` contains `mod log;`, `mod clear_log;`, and `mod list_log;`.
    - `kaku/src/cli/mod.rs` contains subcommand registrations named `log`, `clear-log`, and `list-log`.
    - `kaku/src/cli/list_log.rs` contains the exact table column names `WORKSPACE`, `SEQ`, `MESSAGE`, and `CREATED_AT`.
    - `kaku/src/cli/list_log.rs` contains `--limit`.
    - `kaku/src/cli/log.rs` renders the fields `workspace`, `seq`, `message`, and `created_at`.
    - `cargo test --locked -p kaku workspace_metadata_contracts -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>The CLI exposes the full Phase 2 log command set with stable JSON contracts and deterministic table output for list operations.</done>
</task>

</tasks>

<verification>
Run the Kaku CLI contract test target and confirm all eight workspace metadata subcommands are registered and emit stable JSON or table output as planned.
</verification>

<success_criteria>
The `kaku cli` surface now includes `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, and `list-log` with stable machine-readable output.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-metadata-plane/02-workspace-metadata-plane-03-SUMMARY.md`
</output>
