---
phase: 02-workspace-metadata-plane
plan: 05
type: execute
wave: 4
depends_on:
  - 02-03
  - 02-04
files_modified:
  - docs/KAKU_CONTROL_PLANE_SPEC.md
  - PLANS.md
  - .planning/phases/02-workspace-metadata-plane/02-CHANGED-FILES.md
  - .planning/phases/02-workspace-metadata-plane/02-LIMITATIONS.md
  - kaku/src/cli/rename_workspace.rs
  - mux/src/lib.rs
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
  - META-05
must_haves:
  truths:
    - "A developer can see the exact Phase 2 command contract documented in the repo after the phase ships."
    - "Workspace metadata behavior remains rename-safe and does not silently regress existing Kaku flows."
    - "Phase 2 closeout includes changed-files and known-limitations artifacts required by the repo rules."
  artifacts:
    - path: "docs/KAKU_CONTROL_PLANE_SPEC.md"
      provides: "Documented Phase 2 command contract and UI surfacing notes"
      contains: "set-status"
    - path: ".planning/phases/02-workspace-metadata-plane/02-CHANGED-FILES.md"
      provides: "Phase-level changed-files audit"
      contains: "workspace_state.rs"
    - path: ".planning/phases/02-workspace-metadata-plane/02-LIMITATIONS.md"
      provides: "Explicit known limitations for the phase"
      contains: "Phase 2"
  key_links:
    - from: "mux/src/lib.rs"
      to: "kaku/src/cli/rename_workspace.rs"
      via: "workspace metadata rename regression coverage"
      pattern: "rename_workspace"
    - from: "docs/KAKU_CONTROL_PLANE_SPEC.md"
      to: "PLANS.md"
      via: "matching Phase 2 command-contract documentation"
      pattern: "set-status|set-progress|list-log"
    - from: ".planning/phases/02-workspace-metadata-plane/02-CHANGED-FILES.md"
      to: ".planning/phases/02-workspace-metadata-plane/02-LIMITATIONS.md"
      via: "required phase closeout artifacts"
      pattern: "Phase 2"
---

<objective>
Close Phase 2 with contract documentation, changed-files and limitations artifacts, and targeted regression coverage for rename-safe workspace metadata behavior.

Purpose: Satisfy the repo quality gates so Phase 2 is only considered done when the command contract, changed files, limitations, and key regression checks are all recorded.
Output: Updated docs/spec entries, phase closeout artifacts, and targeted regression tests for workspace rename safety.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
@.planning/phases/01-notification-routing/01-notification-routing-05-SUMMARY.md
@kaku/src/cli/rename_workspace.rs
@mux/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add targeted rename-safe regression coverage for workspace metadata</name>
  <files>kaku/src/cli/rename_workspace.rs, mux/src/lib.rs</files>
  <read_first>
    - kaku/src/cli/rename_workspace.rs
    - mux/src/lib.rs
    - mux/src/workspace_state.rs
    - .planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
    - .planning/phases/01-notification-routing/01-notification-routing-05-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: renaming workspace `unity-a` to `unity-b` preserves existing status and progress under the new workspace key.
    - Test 2: renaming workspace `unity-a` to `unity-b` preserves log entry order and `seq` values.
    - Test 3: existing rename CLI behavior still targets the same typed request path and does not regress while metadata rename propagation is added.
  </behavior>
  <action>Add focused regression coverage where it best fits current test organization: extend `mux/src/lib.rs` tests or nearby mux tests to assert that `rename_workspace` moves status, progress, and log records from `unity-a` to `unity-b` without loss, and extend `kaku/src/cli/rename_workspace.rs` tests only if needed to keep the existing CLI rename surface covered. Keep this narrowly targeted to rename-safe metadata behavior; do not broaden into Task Center or lifecycle regression work. The verification command for this task must cover both the mux rename behavior and at least one existing CLI regression slice.</action>
  <verify>
    <automated>cargo test --locked -p mux workspace_state -- --nocapture && cargo test --locked -p kaku existing_cli_regressions -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/lib.rs` or a colocated mux test contains assertions for renaming workspace metadata from `unity-a` to `unity-b`.
    - `kaku/src/cli/rename_workspace.rs` remains covered by the existing CLI regression target or contains new rename regression assertions.
    - The verification command exits successfully.
  </acceptance_criteria>
  <done>Phase 2 has targeted regression coverage proving workspace metadata survives rename flows without breaking existing Kaku CLI behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Phase 2 contract, changed files, and known limitations</name>
  <files>docs/KAKU_CONTROL_PLANE_SPEC.md, PLANS.md, .planning/phases/02-workspace-metadata-plane/02-CHANGED-FILES.md, .planning/phases/02-workspace-metadata-plane/02-LIMITATIONS.md</files>
  <read_first>
    - docs/KAKU_CONTROL_PLANE_SPEC.md
    - PLANS.md
    - .planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
    - .planning/phases/02-workspace-metadata-plane/02-RESEARCH.md
    - .planning/phases/01-notification-routing/01-notification-routing-05-SUMMARY.md
  </read_first>
  <action>Update `docs/KAKU_CONTROL_PLANE_SPEC.md` and `PLANS.md` so Phase 2 documents the exact command list `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, and `list-log`, plus the stable JSON fields for status, progress, and log records and the clear-mutation summaries. Create `.planning/phases/02-workspace-metadata-plane/02-CHANGED-FILES.md` listing the exact files created or modified across plans 01-05, grouped by mux, transport, CLI, GUI, and docs. Create `.planning/phases/02-workspace-metadata-plane/02-LIMITATIONS.md` documenting only real remaining limits from the implemented phase, such as bounded in-memory log retention or the tabbar-only default metadata surfacing if that remains true. Keep the limitations concrete and phase-bounded; do not speculate about Phase 3 or Phase 4 work beyond brief scope notes.</action>
  <verify>
    <automated>cargo fmt --all --check && cargo test --locked -p kaku workspace_metadata_contracts -- --nocapture && cargo test --locked -p kaku-gui tabbar -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `docs/KAKU_CONTROL_PLANE_SPEC.md` contains the exact command names `set-status`, `clear-status`, `list-status`, `set-progress`, `clear-progress`, `log`, `clear-log`, and `list-log`.
    - `PLANS.md` mentions Phase 2 command-contract details for status, progress, and log metadata.
    - `.planning/phases/02-workspace-metadata-plane/02-CHANGED-FILES.md` exists.
    - `.planning/phases/02-workspace-metadata-plane/02-LIMITATIONS.md` exists.
    - `cargo fmt --all --check` exits successfully.
  </acceptance_criteria>
  <done>Phase 2 ends with documented command contracts, a changed-files audit, explicit limitations, and final formatting plus targeted verification output.</done>
</task>

</tasks>

<verification>
Run the closeout verification commands, confirm the docs list the exact Phase 2 command contract, and ensure the required phase artifacts exist before declaring the phase done.
</verification>

<success_criteria>
Phase 2 has documented contracts, required closeout artifacts, and targeted regression coverage proving the workspace metadata plane ships without breaking the fork’s existing behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-metadata-plane/02-workspace-metadata-plane-05-SUMMARY.md`
</output>
