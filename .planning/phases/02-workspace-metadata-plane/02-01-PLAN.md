---
phase: 02-workspace-metadata-plane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mux/src/lib.rs
  - mux/src/workspace_state.rs
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
must_haves:
  truths:
    - "A developer can rely on one mux-owned workspace metadata source of truth instead of separate CLI or GUI caches."
    - "Workspace status, progress, and log state survives normal workspace renames because records stay keyed to workspace identity."
    - "Status, progress, and log mutations emit a dedicated metadata change event so later CLI and GUI work can refresh without polling."
  artifacts:
    - path: "mux/src/workspace_state.rs"
      provides: "Workspace-scoped status, progress, and bounded log storage"
      exports: ["WorkspaceStateStore", "WorkspaceStatusRecord", "WorkspaceProgressRecord", "WorkspaceLogRecord"]
    - path: "mux/src/lib.rs"
      provides: "Mux-owned workspace metadata APIs and change notifications"
      contains: "WorkspaceMetadataChanged"
  key_links:
    - from: "mux/src/lib.rs"
      to: "mux/src/workspace_state.rs"
      via: "Mux workspace_state field and wrapper methods"
      pattern: "workspace_state"
    - from: "mux/src/lib.rs"
      to: "MuxNotification::WorkspaceMetadataChanged"
      via: "event-driven metadata refresh fan-out"
      pattern: "WorkspaceMetadataChanged"
    - from: "mux/src/lib.rs"
      to: "rename_workspace"
      via: "workspace metadata rename propagation"
      pattern: "rename_workspace"
---

<objective>
Create the mux-owned workspace metadata foundation for Phase 2 so status, progress, and logs share one authoritative runtime model.

Purpose: Implement D-01, D-02, D-09, and D-10 by keeping workspace metadata in `mux` rather than pane state or GUI-local caches.
Output: A new `workspace_state` module plus `Mux` wrapper APIs and a dedicated metadata notification event.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
@.planning/phases/02-workspace-metadata-plane/02-RESEARCH.md
@.planning/phases/01-notification-routing/01-notification-routing-01-SUMMARY.md
@mux/src/lib.rs
@mux/src/notification_store.rs

<interfaces>
From `mux/src/lib.rs`:
```rust
pub enum MuxNotification {
    NotificationsChanged,
    PaneFocused(PaneId),
    WorkspaceRenamed { old_workspace: String, new_workspace: String },
}
```

From `mux/src/notification_store.rs`:
```rust
#[derive(Default)]
pub struct NotificationStore { /* mux-owned state */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the workspace metadata store contract and focused tests</name>
  <files>mux/src/workspace_state.rs</files>
  <read_first>
    - mux/src/workspace_state.rs
    - mux/src/notification_store.rs
    - mux/src/lib.rs
    - .planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
    - .planning/phases/02-workspace-metadata-plane/02-RESEARCH.md
    - docs/KAKU_CONTROL_PLANE_SPEC.md
  </read_first>
  <behavior>
    - Test 1: setting status for workspace `unity-main` stores exactly one record for that workspace and replacing the status updates `updated_at` instead of creating duplicates.
    - Test 2: setting progress accepts `0..=100`, rejects values outside that range at the API boundary, and clearing progress removes the workspace progress record entirely.
    - Test 3: appending more than the per-workspace log limit keeps deterministic append order by `seq`, drops the oldest entries first, and clearing logs empties only the targeted workspace.
    - Test 4: renaming workspace `old-name` to `new-name` updates status, progress, and log records without losing existing values or sequence order.
  </behavior>
  <action>Per D-01, D-02, D-09, and D-10, add `mux/src/workspace_state.rs` as a focused store module that mirrors the Phase 1 `notification_store` pattern. Define exactly these serializable records: `WorkspaceStatusRecord { workspace: String, status: String, updated_at: DateTime<Utc> }`, `WorkspaceProgressRecord { workspace: String, value: u8, updated_at: DateTime<Utc> }`, and `WorkspaceLogRecord { workspace: String, seq: u64, message: String, created_at: DateTime<Utc> }`. Implement a `WorkspaceStateStore` with `status_by_workspace`, `progress_by_workspace`, `logs_by_workspace`, and `next_log_seq` fields plus a hardcoded per-workspace log cap constant of `200`; when appending log entry `201`, drop the oldest stored entry for that workspace so behavior stays deterministic instead of unbounded. Expose methods named `set_status`, `clear_status`, `list_status`, `status_for_workspace`, `set_progress`, `clear_progress`, `progress_for_workspace`, `append_log`, `clear_log`, `list_log`, and `rename_workspace`. Store timestamps with `chrono::Utc` values in the mux layer so later transport can serialize them as RFC 3339 strings without using display-oriented `.to_string()` output.</action>
  <verify>
    <automated>cargo test --locked -p mux workspace_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/workspace_state.rs` contains `pub struct WorkspaceStatusRecord`, `pub struct WorkspaceProgressRecord`, and `pub struct WorkspaceLogRecord`.
    - `mux/src/workspace_state.rs` contains `pub struct WorkspaceStateStore`.
    - `mux/src/workspace_state.rs` contains method names `set_status`, `clear_status`, `list_status`, `set_progress`, `clear_progress`, `append_log`, `clear_log`, `list_log`, and `rename_workspace`.
    - `mux/src/workspace_state.rs` contains the numeric log cap `200`.
    - `mux/src/workspace_state.rs` contains tests covering rename propagation and bounded log truncation.
    - `cargo test --locked -p mux workspace_state -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>`mux/src/workspace_state.rs` exists with a tested, rename-safe, bounded store for workspace status, progress, and logs that later plans can reuse directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire workspace metadata into Mux with dedicated change events</name>
  <files>mux/src/lib.rs</files>
  <read_first>
    - mux/src/lib.rs
    - mux/src/workspace_state.rs
    - mux/src/notification_store.rs
    - .planning/phases/01-notification-routing/01-notification-routing-01-SUMMARY.md
    - .planning/phases/02-workspace-metadata-plane/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `Mux` exposes wrapper methods for set, clear, list, and lookup operations so CLI and GUI callers never touch `WorkspaceStateStore` directly.
    - Test 2: metadata mutations emit `WorkspaceMetadataChanged` only when the store actually changes.
    - Test 3: `Mux::rename_workspace(old, new)` propagates the rename into the workspace metadata store and emits the metadata event when at least one metadata record moved.
  </behavior>
  <action>Add `pub mod workspace_state;` to `mux/src/lib.rs`, add `use crate::workspace_state::{WorkspaceLogRecord, WorkspaceProgressRecord, WorkspaceStateStore, WorkspaceStatusRecord};`, add a `workspace_state: RwLock<WorkspaceStateStore>` field on `Mux`, and initialize it in the `Mux` constructor alongside `notification_store`. Introduce a new `MuxNotification::WorkspaceMetadataChanged` variant instead of reusing `NotificationsChanged`, per D-04 and the research warning about ambiguous events. Expose wrapper methods named `set_workspace_status`, `clear_workspace_status`, `list_workspace_status`, `workspace_status_for_workspace`, `set_workspace_progress`, `clear_workspace_progress`, `workspace_progress_for_workspace`, `append_workspace_log`, `clear_workspace_log`, and `list_workspace_log`. Each mutation method must emit `WorkspaceMetadataChanged` only when the underlying store returns a real change. Update `Mux::rename_workspace` so it also calls `workspace_state.rename_workspace(old_workspace, new_workspace)` after the existing window/client rename path and emits `WorkspaceMetadataChanged` when metadata records were updated; keep this additive and do not move notification ownership out of the existing `notification_store` path.</action>
  <verify>
    <automated>cargo test --locked -p mux workspace_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/lib.rs` contains `pub mod workspace_state;`.
    - `mux/src/lib.rs` contains `WorkspaceMetadataChanged`.
    - `mux/src/lib.rs` contains a `workspace_state` field on `Mux`.
    - `mux/src/lib.rs` contains methods named `set_workspace_status`, `clear_workspace_status`, `list_workspace_status`, `set_workspace_progress`, `clear_workspace_progress`, `append_workspace_log`, `clear_workspace_log`, and `list_workspace_log`.
    - `mux/src/lib.rs` references `workspace_state.rename_workspace`.
    - `mux/src/lib.rs` does not replace `NotificationsChanged`; it adds `WorkspaceMetadataChanged` as a separate variant.
    - `cargo test --locked -p mux workspace_state -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>`Mux` owns workspace metadata behind stable wrapper APIs and emits a dedicated metadata notification that later transport and GUI work can subscribe to without polling.</done>
</task>

</tasks>

<verification>
Run the mux-focused workspace metadata test target and confirm the new metadata wrappers and event variant exist before any transport, CLI, or GUI plan starts.
</verification>

<success_criteria>
`mux` has a dedicated workspace metadata store, stable wrapper APIs, rename propagation, and a metadata-specific change event that keeps Phase 2 event-driven.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-metadata-plane/02-workspace-metadata-plane-01-SUMMARY.md`
</output>
