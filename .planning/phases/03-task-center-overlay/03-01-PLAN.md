---
phase: 03-task-center-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mux/src/lib.rs
  - mux/src/task_center.rs
autonomous: true
requirements:
  - TASK-01
  - TASK-02
must_haves:
  truths:
    - "Task Center entries are aggregated from mux-owned truth rather than a GUI-local parallel model."
    - "One normalized snapshot model covers workspaces, tabs, panes, and unread/failure/running flags so filter logic stays deterministic."
    - "Phase 3 uses current Kaku signals for failed/running classification without claiming Phase 4 lifecycle guarantees."
  artifacts:
    - path: "mux/src/task_center.rs"
      provides: "Normalized task-center entry model and mux-side aggregation helpers"
      contains: "TaskCenterEntry"
    - path: "mux/src/lib.rs"
      provides: "Public mux snapshot access for Task Center"
      contains: "task_center_snapshot"
  key_links:
    - from: "mux/src/task_center.rs"
      to: "mux/src/notification_store.rs"
      via: "unread counts and notification-backed entry flags"
      pattern: "unread"
    - from: "mux/src/task_center.rs"
      to: "mux/src/workspace_state.rs"
      via: "workspace metadata enrichment for workspace/tab entries"
      pattern: "workspace"
---

<objective>
Create the mux-side read-only aggregation foundation for Task Center.

Purpose: Satisfy the locked ownership decisions by composing one normalized snapshot from existing mux state instead of inventing a new store.
Output: A dedicated `task_center` helper module and stable `Mux` wrapper access for later GUI overlay work.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/03-task-center-overlay/03-CONTEXT.md
@.planning/phases/03-task-center-overlay/03-RESEARCH.md
@mux/src/lib.rs
@mux/src/notification_store.rs
@mux/src/workspace_state.rs
@mux/src/tab.rs
@mux/src/window.rs

<interfaces>
From `mux/src/lib.rs`:
```rust
pub fn list_notifications(&self) -> Vec<NotificationRecord>;
pub fn list_workspace_status(&self) -> Vec<WorkspaceStatusRecord>;
pub fn list_workspace_progress(&self) -> Vec<WorkspaceProgressRecord>;
```

From `mux/src/pane.rs`:
```rust
fn is_dead(&self) -> bool;
fn get_progress(&self) -> Progress;
fn copy_user_vars(&self) -> HashMap<String, String>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the normalized Task Center entry model and snapshot tests</name>
  <files>mux/src/task_center.rs</files>
  <read_first>
    - mux/src/lib.rs
    - mux/src/notification_store.rs
    - mux/src/workspace_state.rs
    - mux/src/pane.rs
    - .planning/phases/03-task-center-overlay/03-CONTEXT.md
    - .planning/phases/03-task-center-overlay/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: snapshot output can represent workspace, tab, and pane-backed rows using one typed entry model with stable filter flags.
    - Test 2: unread workspace/tab/pane state is derived from notification records instead of a second unread cache.
    - Test 3: failed/running flags use current pane liveness/progress/user-var signals and stay explicitly optional/limited rather than pretending Phase 4 semantics already exist.
  </behavior>
  <action>Add `mux/src/task_center.rs` with a focused read-only model: `TaskCenterSource`, `TaskCenterKind`, optional target ids, unread count, failed/running flags, workspace name, optional kind/source labels, and a `rerun_available: bool` field that is computed only from already-present pane metadata. Implement aggregation helpers that accept the existing mux/window/tab/pane traversal inputs and produce deterministic snapshots in display order. Treat `pane.is_dead()` and current pane progress/user-vars as the Phase 3 limited-mode signal source for failed/running/rerun availability; do not introduce new lifecycle state or persistence here.</action>
  <verify>
    <automated>cargo test --locked -p mux task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/task_center.rs` contains `TaskCenterEntry`.
    - `mux/src/task_center.rs` contains `TaskCenterSource` and `TaskCenterKind`.
    - `mux/src/task_center.rs` contains `rerun_available`.
    - `mux/src/task_center.rs` contains tests for unread aggregation and failed/running classification.
    - `cargo test --locked -p mux task_center -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>`mux/src/task_center.rs` exists with a tested, read-only normalized entry model for Task Center.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose mux-level Task Center snapshot wrappers without creating a new store</name>
  <files>mux/src/lib.rs</files>
  <read_first>
    - mux/src/lib.rs
    - mux/src/task_center.rs
    - .planning/phases/03-task-center-overlay/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `Mux` exposes a stable read-only `task_center_snapshot` wrapper for GUI consumers.
    - Test 2: snapshot wrappers do not create or retain new task-center-owned state on `Mux`.
  </behavior>
  <action>Add `pub mod task_center;` to `mux/src/lib.rs`, wire in any required imports, and expose a wrapper such as `task_center_snapshot(&self) -> Vec<TaskCenterEntry>` that composes the new helper using existing windows/tabs/panes/notification/workspace lookups. Keep this additive: Task Center must read through existing state and traversal paths, not add another owned store field to `Mux`.</action>
  <verify>
    <automated>cargo test --locked -p mux task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `mux/src/lib.rs` contains `pub mod task_center;`.
    - `mux/src/lib.rs` contains `task_center_snapshot`.
    - `mux/src/lib.rs` does not add a new long-lived `task_center` store field to `Mux`.
    - `cargo test --locked -p mux task_center -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>`Mux` exposes Task Center snapshots through a thin wrapper while keeping ownership in existing stores.</done>
</task>

</tasks>

<verification>
Run the mux task-center test slice and confirm Task Center aggregation remains read-only, deterministic, and mux-backed.
</verification>

<success_criteria>
Task Center has a normalized mux-backed snapshot model that later GUI overlay work can consume without polling or parallel state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-center-overlay/03-task-center-overlay-01-SUMMARY.md`
</output>
