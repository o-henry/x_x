---
phase: 03-task-center-overlay
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - kaku-gui/src/termwindow/mod.rs
autonomous: true
requirements:
  - TASK-01
must_haves:
  truths:
    - "Task Center data reaches the GUI through existing TermWindow snapshot/action paths."
    - "Overlay refresh remains event-driven on mux notifications rather than polling."
    - "Task Center actions are coordinated through TermWindow helpers instead of direct deep overlay mutations."
  artifacts:
    - path: "kaku-gui/src/termwindow/mod.rs"
      provides: "Task Center snapshot cache and action helpers"
      contains: "task_center"
  key_links:
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "mux/src/lib.rs"
      via: "read-only task_center_snapshot usage"
      pattern: "task_center_snapshot"
    - from: "kaku-gui/src/termwindow/mod.rs"
      to: "MuxNotification::NotificationsChanged|WorkspaceMetadataChanged"
      via: "event-driven invalidation for overlay source data"
      pattern: "NotificationsChanged|WorkspaceMetadataChanged"
---

<objective>
Bridge mux task-center snapshots into `TermWindow` so overlays and actions can consume a stable per-window view.

Purpose: Keep Task Center presentation and actions on the GUI side while preserving mux-owned truth and existing overlay lifecycle patterns.
Output: `TermWindow` helpers for snapshot retrieval, refresh, focus-target execution, and unread-clearing dispatch.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/03-task-center-overlay/03-CONTEXT.md
@.planning/phases/03-task-center-overlay/03-RESEARCH.md
@.planning/phases/03-task-center-overlay/03-01-PLAN.md
@kaku-gui/src/termwindow/mod.rs
@kaku-gui/src/overlay/mod.rs
@mux/src/lib.rs

<interfaces>
From `kaku-gui/src/termwindow/mod.rs`:
```rust
fn get_tab_information(&mut self) -> Vec<TabInformation>;
fn emit_status_event(&mut self);
fn update_title(&mut self);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Task Center snapshot refresh and cache helpers to TermWindow</name>
  <files>kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - kaku-gui/src/termwindow/mod.rs
    - mux/src/lib.rs
    - .planning/phases/03-task-center-overlay/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: TermWindow can fetch or refresh Task Center entries from `Mux::task_center_snapshot`.
    - Test 2: notifications and workspace metadata changes trigger Task Center data invalidation along the same event-driven path as other lightweight UI refreshes.
  </behavior>
  <action>Extend `kaku-gui/src/termwindow/mod.rs` with a narrow Task Center cache/refresh helper that reads `Mux::task_center_snapshot()` on demand and invalidates it on `NotificationsChanged` and `WorkspaceMetadataChanged`. Keep the data flow additive and per-window; do not introduce polling or a second authoritative task-center model.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/termwindow/mod.rs` references `task_center_snapshot`.
    - `kaku-gui/src/termwindow/mod.rs` references `NotificationsChanged`.
    - `kaku-gui/src/termwindow/mod.rs` references `WorkspaceMetadataChanged`.
    - `cargo test --locked -p kaku-gui task_center -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>`TermWindow` can supply fresh Task Center data using existing mux-driven update triggers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add narrow TermWindow action helpers for focus and clear-unread</name>
  <files>kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - kaku-gui/src/termwindow/mod.rs
    - mux/src/lib.rs
    - .planning/phases/03-task-center-overlay/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a selected Task Center entry can resolve to an existing focus target using current pane/tab/window identities.
    - Test 2: clear-unread uses existing notification mutation paths by collecting the selected entry's notification ids instead of mutating overlay-local state.
  </behavior>
  <action>Add focused helpers on `TermWindow` for `focus_task_center_entry(...)` and `clear_task_center_entry_unread(...)` or equivalent. Focus must route through existing pane/tab/window activation behavior; clear-unread must resolve matching notification ids from mux-backed entry data and call the existing notification mark-read or clear path. Do not put mutation logic inside the overlay renderer.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/termwindow/mod.rs` contains focused action helpers for Task Center selection.
    - clear-unread is implemented via existing notification mutation APIs rather than overlay-local mutation.
    - `cargo test --locked -p kaku-gui task_center -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>`TermWindow` owns the operational glue for Task Center focus and unread-clearing actions.</done>
</task>

</tasks>

<verification>
Run the GUI task-center test slice to prove the GUI can fetch, refresh, and act on Task Center entries without polling or state duplication.
</verification>

<success_criteria>
Task Center data and actions are wired through existing `TermWindow` coordination paths, ready for overlay rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-center-overlay/03-task-center-overlay-02-SUMMARY.md`
</output>
