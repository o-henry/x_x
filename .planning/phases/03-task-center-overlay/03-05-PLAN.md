---
phase: 03-task-center-overlay
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified:
  - kaku-gui/src/overlay/task_center.rs
  - kaku-gui/src/termwindow/mod.rs
  - docs/KAKU_CONTROL_PLANE_SPEC.md
  - PLANS.md
  - .planning/phases/03-task-center-overlay/03-CHANGED-FILES.md
  - .planning/phases/03-task-center-overlay/03-LIMITATIONS.md
autonomous: true
requirements:
  - TASK-05
must_haves:
  truths:
    - "Task Center exposes rerun only when rerun metadata is actually available."
    - "Phase 3 closes with explicit docs, changed-files audit, limitations, and targeted verification."
    - "The phase does not claim Phase 4 lifecycle guarantees it does not yet implement."
  artifacts:
    - path: "kaku-gui/src/overlay/task_center.rs"
      provides: "Rerun-if-available action gating and dispatch"
      contains: "rerun"
    - path: ".planning/phases/03-task-center-overlay/03-LIMITATIONS.md"
      provides: "Phase 3 known limitations"
      contains: "Known limitations"
  key_links:
    - from: "kaku-gui/src/overlay/task_center.rs"
      to: "mux/src/task_center.rs"
      via: "rerun_available gating on normalized entries"
      pattern: "rerun_available"
    - from: "docs/KAKU_CONTROL_PLANE_SPEC.md"
      to: "PLANS.md"
      via: "documented Task Center actions and limitations"
      pattern: "Task Center|rerun failed pane"
---

<objective>
Finish Phase 3 by adding rerun-if-available behavior and explicit closeout artifacts.

Purpose: Satisfy TASK-05 without pulling Phase 4 lifecycle ownership forward, then close the phase with the repo's documentation and verification gates.
Output: Conditional rerun action support plus Phase 3 closeout docs, changed-files audit, and limitations.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/03-task-center-overlay/03-CONTEXT.md
@.planning/phases/03-task-center-overlay/03-RESEARCH.md
@.planning/phases/03-task-center-overlay/03-VALIDATION.md
@.planning/phases/03-task-center-overlay/03-04-PLAN.md
@kaku-gui/src/overlay/task_center.rs
@kaku-gui/src/termwindow/mod.rs

<interfaces>
From `mux/src/task_center.rs`:
```rust
pub struct TaskCenterEntry {
    pub rerun_available: bool,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate and dispatch rerun only when metadata exists</name>
  <files>kaku-gui/src/overlay/task_center.rs, kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - kaku-gui/src/overlay/task_center.rs
    - kaku-gui/src/termwindow/mod.rs
    - mux/src/task_center.rs
    - .planning/phases/03-task-center-overlay/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Task Center offers rerun only for failed rows with `rerun_available = true`.
    - Test 2: rows without rerun metadata remain focus/clear-unread only.
    - Test 3: rerun dispatch uses existing spawn/action plumbing instead of inventing a new daemon or lifecycle subsystem.
  </behavior>
  <action>Implement the Task Center rerun affordance as a consumer-side action only. The overlay should expose rerun when a failed row carries `rerun_available = true`; otherwise it must hide or disable that action. Dispatch rerun through existing spawn/action plumbing in `TermWindow` using whatever metadata is already present on the selected entry, but do not invent new lifecycle persistence or remain-on-exit semantics here. If no current runtime metadata can safely support a real rerun for a selected row, treat that path as unavailable and document it in the limitations file rather than guessing.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/overlay/task_center.rs` references `rerun_available`.
    - rerun action is conditional rather than always-on.
    - `cargo test --locked -p kaku-gui task_center -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>Task Center supports rerun only when existing metadata makes it safe and honest to do so.</done>
</task>

<task type="auto">
  <name>Task 2: Close Phase 3 with docs, changed-files, limitations, and targeted verification</name>
  <files>docs/KAKU_CONTROL_PLANE_SPEC.md, PLANS.md, .planning/phases/03-task-center-overlay/03-CHANGED-FILES.md, .planning/phases/03-task-center-overlay/03-LIMITATIONS.md</files>
  <read_first>
    - docs/KAKU_CONTROL_PLANE_SPEC.md
    - PLANS.md
    - .planning/phases/03-task-center-overlay/03-RESEARCH.md
    - .planning/phases/03-task-center-overlay/03-VALIDATION.md
    - kaku-gui/src/overlay/task_center.rs
  </read_first>
  <action>Update `docs/KAKU_CONTROL_PLANE_SPEC.md` and `PLANS.md` so Phase 3 documents the concrete Task Center behavior that ships: searchable overlay, filters, focus target action, clear unread action, and rerun-if-available semantics. Create `.planning/phases/03-task-center-overlay/03-CHANGED-FILES.md` with a `Changed files` section that lists every Phase 3 implementation file touched across plans 01-05 and why. Create `.planning/phases/03-task-center-overlay/03-LIMITATIONS.md` with a `Known limitations` section that explicitly calls out any remaining limited-mode failed/running heuristics or rerun metadata dependencies deferred to Phase 4. Finish with the targeted verification commands from 03-VALIDATION plus `cargo check --locked -p kaku-gui`.</action>
  <verify>
    <automated>cargo check --locked -p kaku-gui && cargo test --locked -p mux task_center -- --nocapture && cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `docs/KAKU_CONTROL_PLANE_SPEC.md` documents Task Center filters and actions.
    - `PLANS.md` mentions Task Center overlay as Phase 3 concrete scope.
    - `.planning/phases/03-task-center-overlay/03-CHANGED-FILES.md` exists and contains `Changed files`.
    - `.planning/phases/03-task-center-overlay/03-LIMITATIONS.md` exists and contains `Known limitations`.
    - the targeted Phase 3 verification command exits successfully.
  </acceptance_criteria>
  <done>Phase 3 closes with explicit Task Center docs, a changed-files audit, a written limitations list, and targeted verification coverage.</done>
</task>

</tasks>

<verification>
Run the targeted Phase 3 mux and GUI task-center test slices plus `cargo check --locked -p kaku-gui`, then record limitations honestly where Phase 4 still owns lifecycle guarantees.
</verification>

<success_criteria>
Phase 3 finishes with a Kaku-native Task Center overlay, filter/action coverage, guarded rerun behavior, and explicit closeout artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-center-overlay/03-task-center-overlay-05-SUMMARY.md`
</output>
