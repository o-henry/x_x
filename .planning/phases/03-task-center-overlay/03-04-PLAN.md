---
phase: 03-task-center-overlay
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - config/src/keyassignment.rs
  - kaku-gui/src/commands.rs
  - kaku-gui/src/termwindow/mod.rs
  - kaku-gui/src/overlay/task_center.rs
autonomous: true
requirements:
  - TASK-03
  - TASK-04
must_haves:
  truths:
    - "Task Center can be opened as a native Kaku command rather than only through ad hoc code paths."
    - "A selected Task Center row can focus its target directly."
    - "Unread clearing is available from Task Center without inventing a second unread system."
  artifacts:
    - path: "config/src/keyassignment.rs"
      provides: "Native Task Center key assignment surface"
      contains: "ShowTaskCenter"
    - path: "kaku-gui/src/commands.rs"
      provides: "Command palette / menubar Task Center entry"
      contains: "Task Center"
  key_links:
    - from: "kaku-gui/src/commands.rs"
      to: "config/src/keyassignment.rs"
      via: "native command definition and default key assignment"
      pattern: "ShowTaskCenter"
    - from: "kaku-gui/src/overlay/task_center.rs"
      to: "kaku-gui/src/termwindow/mod.rs"
      via: "row action dispatch for focus and clear unread"
      pattern: "focus_task_center_entry|clear_task_center_entry_unread"
---

<objective>
Wire Task Center into native Kaku command surfaces and enable focus/clear-unread actions from the overlay.

Purpose: Make Task Center discoverable and operational through existing command/keybinding patterns.
Output: A first-class Task Center command plus working focus and clear-unread actions from overlay rows.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/03-task-center-overlay/03-CONTEXT.md
@.planning/phases/03-task-center-overlay/03-RESEARCH.md
@.planning/phases/03-task-center-overlay/03-03-PLAN.md
@config/src/keyassignment.rs
@kaku-gui/src/commands.rs
@kaku-gui/src/termwindow/mod.rs

<interfaces>
From `config/src/keyassignment.rs`:
```rust
pub enum KeyAssignment {
    ShowLauncher,
    ShowLauncherArgs(LauncherActionArgs),
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a native Task Center command and palette/keybinding wiring</name>
  <files>config/src/keyassignment.rs, kaku-gui/src/commands.rs, kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - config/src/keyassignment.rs
    - kaku-gui/src/commands.rs
    - kaku-gui/src/termwindow/mod.rs
    - .planning/phases/03-task-center-overlay/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Task Center has a first-class `KeyAssignment` and command definition, so it appears in palette/menubar discovery.
    - Test 2: invoking the command opens Task Center on the active window using the standard overlay lifecycle.
  </behavior>
  <action>Add a dedicated `ShowTaskCenter` key assignment or equivalent first-class command in `config/src/keyassignment.rs`, register it in `kaku-gui/src/commands.rs` with sensible palette/menubar metadata, and handle it in `TermWindow` by launching the Task Center overlay on the active tab/window. Keep this consistent with the established launcher/quickselect command style instead of hiding Task Center behind a generic launcher flag.</action>
  <verify>
    <automated>cargo check --locked -p kaku-gui</automated>
  </verify>
  <acceptance_criteria>
    - `config/src/keyassignment.rs` contains `ShowTaskCenter`.
    - `kaku-gui/src/commands.rs` contains a Task Center command definition.
    - `kaku-gui/src/termwindow/mod.rs` handles `ShowTaskCenter`.
    - `cargo check --locked -p kaku-gui` exits successfully.
  </acceptance_criteria>
  <done>Task Center is a native Kaku command that can be opened through normal palette/keybinding surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect overlay row actions for focus target and clear unread</name>
  <files>kaku-gui/src/overlay/task_center.rs, kaku-gui/src/termwindow/mod.rs</files>
  <read_first>
    - kaku-gui/src/overlay/task_center.rs
    - kaku-gui/src/termwindow/mod.rs
    - mux/src/lib.rs
  </read_first>
  <behavior>
    - Test 1: selecting a row and triggering the primary action focuses the resolved pane/tab/workspace target.
    - Test 2: the overlay exposes a clear-unread action only when unread state is present and routes that mutation through existing notification APIs.
  </behavior>
  <action>Wire Task Center row actions so the overlay invokes `TermWindow` helpers for focus and clear-unread. Focus should resolve pane/tab/workspace targets using existing IDs and activation paths. Clear-unread should only appear when the selected row has unread state and should mark or clear the matching notification ids via existing mux mutation helpers rather than directly mutating overlay state.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/overlay/task_center.rs` contains focus and clear-unread action wiring.
    - clear-unread is conditional on unread state.
    - `cargo test --locked -p kaku-gui task_center -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>Task Center is operational for focus and clear-unread actions through existing Kaku behavior paths.</done>
</task>

</tasks>

<verification>
Compile the GUI crate and run Task Center tests that prove the overlay opens from a native command and can act on focus/unread behavior.
</verification>

<success_criteria>
Task Center is discoverable through normal Kaku commands and can focus targets or clear unread state directly from the overlay.
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-center-overlay/03-task-center-overlay-04-SUMMARY.md`
</output>
