---
phase: 03-task-center-overlay
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - kaku-gui/src/overlay/mod.rs
  - kaku-gui/src/overlay/task_center.rs
autonomous: true
requirements:
  - TASK-01
  - TASK-02
must_haves:
  truths:
    - "Task Center is a dedicated overlay module, not a launcher flag overload or a new shell."
    - "Search and filter behavior operate on one normalized entry list."
    - "Overlay rows stay visually close to existing Kaku overlays and launcher behavior."
  artifacts:
    - path: "kaku-gui/src/overlay/task_center.rs"
      provides: "Task Center overlay renderer, search state, and filter controls"
      contains: "TaskCenterOverlay"
    - path: "kaku-gui/src/overlay/mod.rs"
      provides: "Task Center overlay bootstrap export"
      contains: "task_center"
  key_links:
    - from: "kaku-gui/src/overlay/task_center.rs"
      to: "kaku-gui/src/overlay/launcher.rs"
      via: "reused keyboard-driven filter and row-selection pattern"
      pattern: "matcher_pattern|matcher_score"
---

<objective>
Implement the visible searchable Task Center overlay using existing overlay patterns.

Purpose: Deliver the actual Phase 3 UX surface while keeping interaction and look aligned with current Kaku overlays.
Output: A dedicated `task_center` overlay module with search/filter support over normalized entries.
</objective>

<execution_context>
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/workflows/execute-plan.md
@/Users/henry/Documents/code/vibe/hybrid/x_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/KAKU_CONTROL_PLANE_SPEC.md
@PLANS.md
@.planning/phases/03-task-center-overlay/03-CONTEXT.md
@.planning/phases/03-task-center-overlay/03-RESEARCH.md
@.planning/phases/03-task-center-overlay/03-02-PLAN.md
@kaku-gui/src/overlay/launcher.rs
@kaku-gui/src/overlay/mod.rs
@kaku-gui/src/overlay/selector.rs

<interfaces>
From `kaku-gui/src/overlay/mod.rs`:
```rust
pub fn start_overlay<T, F>(...) -> (...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a dedicated Task Center overlay module with normalized row rendering</name>
  <files>kaku-gui/src/overlay/task_center.rs, kaku-gui/src/overlay/mod.rs</files>
  <read_first>
    - kaku-gui/src/overlay/launcher.rs
    - kaku-gui/src/overlay/mod.rs
    - .planning/phases/03-task-center-overlay/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: overlay rows can render workspaces, tabs, panes, and unread/failure/running hints from one entry model.
    - Test 2: opening the overlay with no entries still shows a valid empty state rather than crashing or falling back to launcher UI.
  </behavior>
  <action>Add `kaku-gui/src/overlay/task_center.rs` and export it from `overlay/mod.rs`. Reuse launcher-style matching helpers where it saves work, but keep Task Center semantics in this dedicated module. The overlay should render a compact row with label plus enough metadata to distinguish source, workspace, unread, failed, and running state without turning into a dashboard. Keep the color/layout approach aligned with existing overlay surfaces.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/overlay/task_center.rs` exists.
    - `kaku-gui/src/overlay/task_center.rs` contains `TaskCenterOverlay`.
    - `kaku-gui/src/overlay/mod.rs` exports `task_center`.
    - `cargo test --locked -p kaku-gui task_center -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>Task Center exists as a first-class Kaku overlay with dedicated rendering logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement search and spec-required filters on the normalized entry list</name>
  <files>kaku-gui/src/overlay/task_center.rs</files>
  <read_first>
    - kaku-gui/src/overlay/task_center.rs
    - .planning/phases/03-task-center-overlay/03-CONTEXT.md
    - docs/KAKU_CONTROL_PLANE_SPEC.md
  </read_first>
  <behavior>
    - Test 1: free-text search narrows results using the normalized row label/metadata text.
    - Test 2: filter toggles or selectors cover unread, failed, running, workspace, source, and kind.
  </behavior>
  <action>Implement local overlay filtering over the normalized entry list, using one state model that supports search text plus the spec-required filters `unread`, `failed`, `running`, `workspace`, `source`, and `kind`. Keep the interaction keyboard-friendly and simple; do not add secondary pages or panels.</action>
  <verify>
    <automated>cargo test --locked -p kaku-gui task_center -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `kaku-gui/src/overlay/task_center.rs` contains filtering support for `unread`, `failed`, `running`, `workspace`, `source`, and `kind`.
    - `kaku-gui/src/overlay/task_center.rs` contains tests for search and filter narrowing behavior.
    - `cargo test --locked -p kaku-gui task_center -- --nocapture` exits successfully.
  </acceptance_criteria>
  <done>Task Center rows are searchable and filterable according to the Phase 3 contract.</done>
</task>

</tasks>

<verification>
Run the GUI task-center test slice and confirm Task Center opens, renders, searches, and filters as a dedicated overlay rather than a launcher overload.
</verification>

<success_criteria>
Task Center is a searchable Kaku-native overlay over normalized work entries with the required filter surface.
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-center-overlay/03-task-center-overlay-03-SUMMARY.md`
</output>
